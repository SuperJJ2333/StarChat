<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <!-- 相册视频选择必需：Android 13+ 图片/视频权限分离，缺失此声明时
         系统只授予图片读取，媒体选择器将看不到任何视频文件 -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- 通话：震动提醒、来电全屏覆盖（含锁屏）、通话中保活 -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA"/>
    <!-- BUG 2 后台/锁屏通知：dataSync 前台服务保活消息同步长连接 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <!-- BUG2 加固：电池优化白名单引导（厂商 ROM 息屏清理的必要条件） -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <!-- 个推 SDK 合并权限剥离（隐私红线，docs/PUSH_SETUP.md）：
         SDK AAR 默认合入位置/设备标识/应用列表/任务等采集类权限——
         本应用推送不需要任何该类数据（服务端只发通用文案通知），
         全部以 tools:node="remove" 剥离，采集在权限层即被封死。 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" tools:node="remove" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.GET_TASKS" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" tools:node="remove" />
    <uses-permission android:name="android.permission.BLUETOOTH" tools:node="remove" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.DUMP" tools:node="remove" />
    <!-- 原生通话层（微信级后台来电）：phoneCall 前台服务 + CallStyle -->
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:label="畅聊 ChatFlow"
        android:name=".ChatFlowApplication"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        tools:replace="android:label,android:usesCleartextTraffic">

        <!-- 个推推送（隐私合规配置，docs/PUSH_SETUP.md）：
             客户端只含公开 AppID（占位符来自 app/build.gradle.kts）；
             AppKey/AppSecret/签名密钥只存服务端 getui-bridge 环境变量。
             合规：preInit 于 Application（同意前、不采集不联网），
             initialize 由 Dart 在用户同意隐私政策后经 chatflow/getui 触发；
             严禁调用个推的别名/标签绑定接口（不以手机号/用户名做标识）。 -->
        <meta-data
            android:name="GETUI_APPID"
            android:value="${GETUI_APPID}" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- 通话保活：本地通知插件的前台服务，声明麦克风/摄像头类型 -->
        <service
            android:name="com.dexterous.flutterlocalnotifications.ForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync|microphone|camera" />

        <!-- 原生通话层组件：phoneCall 前台服务 + CallStyle 通知 -->
        <service
            android:name=".call.CallForegroundService"
            android:exported="false"
            android:foregroundServiceType="phoneCall" />
        <service
            android:name=".call.CallConnectionService"
            android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.telecom.ConnectionService" />
            </intent-filter>
        </service>
        <receiver
            android:name=".call.IncomingCallReceiver"
            android:exported="false" />

        <activity
            android:name=".call.CallActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/ChatFlow.CallActivity" />
        <service
            android:name=".call.CallOverlayService"
            android:exported="false" />
        <!-- 个推：消息回调服务（CID/点击）与推送服务（官方要求空实现）。 -->
        <service
            android:name=".ChatFlowGetuiIntentService"
            android:exported="false" />
        <service
            android:name=".ChatFlowGetuiPushService"
            android:exported="false"
            android:label="PushService"
            android:process=":pushservice" />
        <!-- 裁剪头像页：状态栏避让由 Ucrop.CropTheme 主题处理
             （statusBarColor + 退出 edge-to-edge 强制），确定按钮不再被遮挡 -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Ucrop.CropTheme" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- 个推 SDK 包可见性（官方建议；唯一放开的可见性，不查询应用列表）。 -->
        <intent>
            <action android:name="com.getui.sdk.action" />
        </intent>
    </queries>
</manifest>
