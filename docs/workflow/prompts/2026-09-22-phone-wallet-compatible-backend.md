# 下一步：准备与线上刷新恢复兼容的手机/钱包后端候选

先读 AGENTS.md、mobile-delivery-workflow、current-state、本轮 Mi6 followup 任务和 phone-wallet-release-readiness 审计报告。本任务是隔离候选整合与验证，不是生产发布授权。

1. 以审计核实的已发布提交28bdfc48为基线建立 codex/ 前缀独立工作树；先复核发布身份及实际工作区，禁止覆盖主工作区其他任务修改。
2. 逐文件审查差异清单，合入手机认证、短信、FX60分钟持久缓存、客服人工充值与提现、取消申请的必要代码及其显式依赖。保留已上线 refresh operation_id 恢复协议、模型字段和兼容测试；不得复制整份旧 identity/tokens 文件覆盖新协议。
3. 保留线上0080_refresh_recovery与本地0072至0082原始迁移历史。确认DDL无冲突后新增合并迁移；禁止重编号、stamp伪装完成或删除历史。证明从线上历史和空库两条路径均可升级，且保留原有数据、审计、幂等记录及刷新恢复。
4. 在隔离PostgreSQL完成迁移、恢复、取消与到账并发、同键并发、人工充值登记和身份回归。取消不退款、不改余额；迟到实际收款保留义务并转人工核对。外部款项只使用USDT，红包/转账使用点钻，参考值不成为记账权威。不得恢复用户端兑换或自动充值。
5. 对配置、锁文件、API/worker镜像、OpenAPI、后台客服批准入口进行完整依赖核验，确认每一项客户端功能有可用接口和对应业务行为。真实短信已完成供应商验证，不重复发码；没有真实凭据不伪造生产启动通过。
6. 先做规格审查，再做质量/安全审查；运行必要专项及完整门禁。记录失败与skip，禁止改断言掩盖回归。输出冻结commit/文件hash、候选镜像digest、迁移演练日志、部署与恢复步骤、仍未完成项目。
7. 只有形成经过验证的具体候选后，才提出精确的生产发布申请。Mi6 Debug安装授权不等于后端发布授权；生产操作前按持久工作流核验已有授权。未获授权不得改线上容器、数据库、配置或开关。

交付结论必须分别报告：本地代码通过、候选镜像通过、已部署接口、Mi6实际通过；不能用其中一个替代另一个。
