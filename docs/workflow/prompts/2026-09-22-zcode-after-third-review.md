# ZCode：Flutter 契约复审后的 UI 批次

先读取 docs/verification/2026-09-22-phone-client-code-review.md、AGENTS.md、mobile-delivery-workflow.md、current-state.md，以及 docs/verification/2026-09-22-sms-workbench-code-review.md、docs/verification/2026-09-22-review-flows-code-review.md 和对应任务记录。以主工作区实际代码、applied-changes.json 与 final-inputs.json 为基线，保留其他任务修改，不重复实施已修正项。

最新状态：用户已确认真实短信供应商验收完成，本轮已修正 SDK 分类与工作台异步/重复提交问题；不要重复请求 RAM 授权或重新发码。后台已有时间线及转让处置入口，先检查剩余缺口，不重做已完成工作。

目标：核对后台剩余验收，再独立实施 Flutter 批次；不部署、不开启生产开关、不发送真实短信、不处理真实资金。

1. 先核对现有 OpenAPI 与真实 AdminApi。补齐充值历史审计时间线详情、群转让意图查询与人工处置 UI。权限、状态、错误及分页必须使用真实接口契约；不能仅用具备额外方法的假客户端证明接线完成。
2. 严格保留本轮资金与状态机边界：充值记录缺失不等于未入账；release 需要 binding_id 与 Idempotency-Key，并由服务端核证。旧群主仍持权不能证明请求未发送；未知结果继续待核对，界面不得自行释放或改变财务状态。转让未 COMPLETED 不显示换主成功。
3. 管理端完成后，再独立实现 Flutter 手机注册/验证码登录/换绑、客服目录、关闭兑换入口、点钻与 USDT 应付展示、红包费用及转让阶段显示。先按仓库当前 UI 交付工作流核对设计及契约；不改已批准费率、计价、精度、审计、审批或加密规则。
4. 每批写有意义的失败回归，随后最小实现、专项验证；做规格符合性审查，再做质量/权限/并发审查。前端须实际 AdminApi + 浏览器验证；Flutter 用 profile/release 真机证据，缺设备标未验证。
5. 按变更影响运行必要门禁，复用同输入的现有证据，不重复运行不相关长测试。记录版本哈希、失败记录、最终结果和剩余事项，不以测试总数或占位用例代替链路验收。

生产备份副本恢复演练及转让开关启用分别依赖对应环境与既有授权；Flutter 注册/登录/换绑仍须应用全链路验收，不能用已完成的供应商短信验收替代；不要为通过测试降低权限、限流或默认为成功。


## Flutter 契约当前状态（第五轮复审后）

PhoneAuthGateway/RechargeGateway 与 17 个实际方法已在位；手机号登录清理旧 Matrix 冷却，公开请求使用 8 秒预算。17 专项、Flutter3702/analyze/边界门禁已通过。不得重建接口替身或重复实现契约层；先按报告核对回填哈希。

UI 批次须覆盖：手机号/邮箱注册选择、手机验证后等待 Matrix 开通、短信登录、旧凭证→新手机两步换绑、隐私开关/搜索、客服目录和充值/历史/取消，以及已有批准的钱包展示要求。遵守现行 UI delivery 工作流，不扩大到未批准商业规则。

超时是结果未知，不能显示“未发送/未执行”或自动重发短信。保存注册会话并通过既有 registrationStatus 恢复；注册验码和充值取消当前服务端不按传入幂等键重放，遇响应丢失先查权威状态。提交充值复用同一业务操作键，SUBMITTED 不得显示到账。完成后按平台列真实验收证据，缺设备仍标未验证。不要再要求用户重做已完成的短信供应商授权/验证码测试。
