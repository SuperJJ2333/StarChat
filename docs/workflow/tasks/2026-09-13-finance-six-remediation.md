# 财务六项缺口与拍一拍修复

## 恢复入口

- 授权：用户在六项审计后要求全部修复，追加金额右对齐、备注昵称、统一群转账/专属红包选择器及头像、每用户每房间滚动 60 秒三次拍一拍与 toast。
- [计划](../../superpowers/plans/2026-09-13-finance-six-remediation.md)，审计源报告位于主工作区 docs/verification/2026-09-13-finance-six-audit.md。
- 状态：源码实现与定向验证完成；Astra 主审，显式 gpt-5.6-terra 两个执行子代理。模型参数已在 spawn_agent 指定，未静默替换。
- 工作树：.worktrees/finance-six-remediation；分支 codex/finance-six-remediation-20260913；基线 c5cd589c。主目录其他任务未覆盖；仅补齐已审查的缺失 support identity 编译依赖，哈希详见验证报告。
- 文件所有权：详见计划 T1/T2/T3，共享文件串行。
- 下一步：集成主目录其他任务并按正式移动交付流程打包；当前未发布。完整验收记录见 docs/verification/2026-09-13-finance-six-remediation.md。

## 验收台账

| ID | 场景 | 状态 |
| --- | --- | --- |
| F1 | 金额整行尾部对齐、不同标题与大字体不漂移 | 定向通过；真机待验收 |
| F2 | 状态正确且名称按备注/昵称/账号显示，异步更新立即可见 | 定向通过；真机待验收 |
| F3 | 详情 demo 实装并保留真实金额/时间/账单复制与导航 | 定向通过；真机待验收 |
| F4 | 两个群选择器统一样式、HTTP/MXC 正常、仅本群成员 | 定向通过；真机待验收 |
| F5 | Matrix→业务 ID 正确、失败不提交、空群不回退全好友 | 定向通过；真机待验收 |
| F6 | 红包数量默认空与人数上限 | 定向通过；真机待验收 |
| N1 | 当前用户当前房间三次后拒绝任何目标并 toast | 定向通过；真机待验收 |
| N2 | 滚动边界、房间/用户隔离、页面重进、异步失败令牌归还 | 定向通过；真机待验收 |

## 环境与证据

- Flutter C:/src/flutter/bin/flutter.bat；pub get --offline 已成功，锁文件无差异。第一次在仓库根目录调用失败（找不到 Flutter 项目），切换 apps/mobile_flutter 后成功；不隐瞒重试。
- Python 3.12.10 / pytest 8.4.2；SQLAlchemy/FastAPI/Alembic/httpx 导入通过；Docker Compose v5.0.2。
- verify 环境仅使用 .env.example 的本地测试值，不复制生产配置。运行日志：docs/verification/artifacts/2026-09-13/finance-six-remediation/。
- 本轮未安装 APK 或部署生产；实际此前设备是 debug 2105，不能称包含当前源码修复。

## 阶段计时

| 阶段 | 时间 | 说明 |
| --- | --- | --- |
| 调查/分工 | 首次精确采样 2026-09-13T12:50:45+08:00 | 此前调查起点未采样，不编造耗时；工作树/计划建立 |
| 定向实现与基线验证 | 2026-09-13 12:50–13:35 +08:00（分钟级） | 两个执行批次、串行 UI 收口，Astra 复核及定向重跑；全部基线失败保留 |

## 交接

未改服务端账本、财务状态机、支付授权或加密协议。不把客户端拍一拍配额声称为跨设备强制配额。真机交互等待用户后续验收。
