# Flutter 手机认证 / 钱包 UI 与 Mi 6 debug 验收

用户授权：将已确认 HTML 布局落到 Flutter，手机号两步验证复用充值/提现鱼骨步骤条，构建 debug 并安装 Mi 6 本人测试。无生产部署或真实短信、资金操作。

设备：Mi 6 / cbd0156b，原 com.liuhetong.mobile 0.3.103+2156；原包签名 75b31c66… 与固定身份相同。目标 0.3.103+2157 debug，保留数据覆盖安装。

范围：auth 登录/注册/验证/换绑、钱包人工充值及 FX、红包权威费用展示、原群主转让选择页面、host/API 接线、注册表与 HTML 步骤示意。复用 walletStepIndicator 的原代码，不新增注册组件。

阶段：源码与设备预检 → 并行独立模块专项测试 → Flutter 全量/analyze/UI 契约 → 固定签名重建 → Mi6安装与启动。任务证据全部位于 ../../verification/artifacts/2026-09-22/flutter-phone-mi6。

门禁：首轮全 Flutter 仅旧群红包固定费率源码断言失败，已按群主豁免更新；最终全量在跑。后端输入与前次门禁存在 8 项内容变化（其他任务），不复用历史全量，scripts/verify.ps1 已启动。独立 mobile/infra 重复检查与全门禁重叠，结果如实记录。

下一步：完成当前运行门禁及 APK 重建，核对签名/ABI/版本/清单/资产后 adb install -r -t；不卸载、不清数据、不降级。若短信/转让服务器未启用，界面提示不可用，不能将本地测试称真实通道验收。

已于20:07完成2157 debug安装与启动，27317类/339资产及清单等价验证通过。20:09实际打开换绑页，步骤条显示正常，未发短信。Flutter最终3732 passed；完整verify待结束（旧邀请码时钟用例已定向修复、12通过）。
