# 任务记录：Android 0.3.93/2127 发布 + 更新弹窗（含 app-updates 平台标记生产修复）

## 恢复入口

- 目标、用户授权来源及边界：用户 2026-09-17「请你推送Android新版本更新弹窗」。
  用户确认参数：版本 **0.3.93 + 2127**、更新说明用助手草案、**不强制更新**、允许本地 commit（不 push）。
  边界：只做 Android 一端发布 + 更新弹窗；iOS 行、manifest.plist、iOS 包不动；
  未要求真机安装与 APK 分发之外的改动。
- 关联计划/ADR：无独立计划或 ADR。发布按
  [mobile-delivery-workflow](../../runbooks/mobile-delivery-workflow.md)、
  [android-apk-rebuild](../../runbooks/android-apk-rebuild.md)、
  [app-release-deployment](../../runbooks/app-release-deployment.md)、
  [admin-production-workflow](../../runbooks/admin-production-workflow.md)。
  证据：[2026-09-17-android-0393-2127-release](../../verification/2026-09-17-android-0393-2127-release.md)、
  验证产物目录 `docs/verification/artifacts/2026-09-17/release-2127/`。
- 当前状态：**已上线**（Android 包已发布并公网验证；更新弹窗设置已发布；业务 API 平台标记修复已切换）。
  真机安装与弹窗观感由用户验收。
- 负责人、工作树、文件所有权、源码 commit：主工作树 `D:\pythonProject\outsource\StarChat`（分支 `main`）。
  候选源码 commit **`d30bd051`**（本地，未 push；基线 `647109e3`）。本轮该任务拥有并已提交：
  `apps/mobile_flutter/pubspec.yaml`、`apps/mobile_flutter/lib/core/app_config.dart`（版本同步）。
- 最后更新时间（含时区）：2026-09-17 13:0x +08（Asia/Hong_Kong）
- 下一条具体操作、必要输入、阻断的验收 ID：用户在 Android 真机覆盖安装
  `https://www.liuhetong888.com/downloads/ChatFlow-0.3.93-build2127-arm64.apk`（或等弹窗）验收 A1/A2；
  无阻断项。

## 验收台账

| ID | 场景及预期 | 实现 | 测试及证据 | 发布 | 真机反馈/缺口 |
| --- | --- | --- | --- | --- | --- |
| A1 | Android 用户收到 0.3.93 更新弹窗（可关闭，不强制） | 设置 `app_latest_version=0.3.93` / `app_latest_build=2127` / `app_min_supported_build=3`（沿用）/ notes / apk_url → 版本化 2127 包 | preflight+apply 断言：读回一致、iOS 行不变、审计 5 行、Android 投影 `platform:"android"` 且 `latest_build=2127`；公网未授权 401 | **已上线** 2026-09-17 04:56:14 UTC（12:56 +08） | 待用户真机 |
| A2 | 新包可下载且与构建产物一致 | 16MiB 分块上传 + 服务端合并 SHA 门 + `install -m 0644` 不可变版本文件 + `latest-arm64.apk` 原子切换 | 服务器与工作站双侧 200/206 + `application/octet-stream`；**公网全量下载 SHA256 = `E1C34A03…03BD40F`** 与本地一致；2121 保留 200 | **已上线** 2026-09-17 04:55 UTC（12:55 +08） | 同上 |
| A3 | 弹窗对现网 Android 客户端真正可见（此前被服务端缺陷吞掉） | 生产 API 覆盖修复：`app_update.py` 恢复对称平台标记（`platform_fields={"platform":platform}` + `download_url`），基于在线镜像单文件覆盖 | 基线镜像投影无 `platform`（客户端判 configured=false）vs 候选镜像 `platform:"android"`；演练先锁定导入路径（ADR-0071 假绿陷阱）；切换后 61/61 env、3 mounts、端口不变；日志无 error | **已上线** 2026-09-17 04:53 UTC（12:53 +08） | 同上 |
| A4 | iOS 一端配置与包不受影响 | 未改 `app_ios_*`、未改 manifest.plist/IPA；apply 断言 iOS 行逐键不变、iOS 投影仍 0.3.92/2120 | `settings-publish-apply.log` 中 `ios_rows_untouched` 与投影读回 | iOS 保持 2026-09-15 状态 | — |

## 版本与证据

| 平台/服务 | 实际版本/build/镜像 | 来源commit | 包名/签名渠道 | 文件位置及SHA | 发布观察时间/链接 |
| --- | --- | --- | --- | --- | --- |
| Android 正式 ARM64 | **0.3.93+2127** | `d30bd051` | `com.liuhetong.mobile`，固定证书 `75b31c66476cd8e2c9319551b49405a1de1e5c23e9a0dbdcc9eb76b52ba61fff` | `/opt/starchat/frontend/downloads/ChatFlow-0.3.93-build2127-arm64.apk`，79,408,158 字节，SHA256 `E1C34A03F42BFE83A3F7E3F67E60010D8CB1754D9F708B727F0DB4AE903BD40F`（本地/服务器/公网下载三方一致） | 2026-09-17 12:55 +08 上线；`latest-arm64.apk` → 2127 |
| 业务 API（平台标记修复） | 镜像 `starchat-business-api:app-update-platform-20260917`（digest `sha256:16522404d919…`；基座 `redpacket-total-20260917` = `be11636c…`） | payload = 仓库 HEAD `app_update.py` sha256 `6b85f462…` | — | 容器 `starchat-business-api-1`，healthy；61 env / 3 mounts / `127.0.0.1:8082` 与切换前一致 | 2026-09-17 12:53 +08 切换 |
| iOS | 未改动（0.3.92/2120） | — | — | — | 未发布 |

测试记录：

- `flutter test`（全量，冻结候选 0.3.93+2127）：退出码 0，**2835 通过 / 0 失败**，
  日志 `artifacts/2026-09-17/release-2127/android/flutter-full-candidate-2127.txt`。
- `flutter analyze`：`No issues found!`。
- `pytest tests/mobile/test_app_build_contract.py`：2 通过（版本与 app_config 同步）。
- `pytest tests/business_api/test_app_update_settings.py`：18 通过（修复所用 app_update.py 的契约测试）。
- 构建门禁：`verify_android_release.py`（源 + 最终）、apksigner（v2/v3 + 固定证书）、zipalign `-c`、
  aapt 身份（2127/0.3.93/arm64）、`verify_rebuild.py` 语义核对（25345/25345 类、338 项原生资产零变化、
  清单语义一致）。日志同上目录。
- 生产演练：基线镜像行为复现缺陷、候选镜像 `REHEARSAL_OK`（模块路径 + payload sha + 双平台标记 + 模型校验），
  日志 `artifacts/2026-09-17/release-2127/api-rehearsal.log`。
- 公网：服务器 + 工作站（jumper SOCKS）双侧检查，公网整包下载 SHA 一致。
- 未执行项：未构建 iOS、未做真机安装、未改 iOS manifest/设置；未跑后端全量 pytest
  （本次后端改动为单文件响应投影，已跑其专属契约测试 + 镜像内演练；全量 pytest 上次基线
  1812 通过 / 58 跳过，见 2124 记录）。

## 阶段计时

| 阶段 | 开始（含时区） | 结束 | 主动/工具/外部等待/返工 | 并行组 | 结果/耗时来源 | 下一步 |
| --- | --- | --- | --- | --- | --- | --- |
| 读手册 + 实时基线 + 发现平台标记缺陷 | 12:4x | 12:4x | 主动 | — | 生产只读探针 | — |
| 版本冻结 + 候选全量门禁 + commit | 12:4x | 12:5x | 主动 | — | 2835 通过；`d30bd051` | — |
| APK 构建（源码→重建→签名→门禁） | 12:4x | 12:5x | 工具等待（Gradle 184s） | 与 API 候选并行 | 退出码 0 | — |
| API 覆盖修复（候选镜像 + 演练 + 切换） | 12:4x | 12:53 | 主动（含一次演练返工：导入路径假绿被断言拦下） | 与 APK 构建并行 | `REHEARSAL_OK`，切换后 healthy | — |
| 分块上传 + 合并 SHA 门 + 安装 + 符号链接 | 12:54 | 12:55 | 外部等待（scp 79MB） | — | 三方 SHA 一致 | — |
| 更新弹窗 inspect/apply + 备份 | 12:56 | 12:56 | 主动 | — | `PUBLISH_PASS`，审计 5 | — |
| 公网双侧验证 + 整包下载哈希 | 12:56 | 13:0x | 外部等待（79MB 下载） | — | 全部通过 | — |
| 文档与索引 | 13:0x | 13:0x | 主动 | — | — | — |

总墙钟：约 35 分钟（未逐段精确计时，不估成精确值）；其中「APK 构建」与「API 候选/演练」并行。

## 交接与回退

- 已确认根因：① 更新弹窗对 Android 不可见 = 服务端平台标记缺失（客户端 `4c199ae1` 起强制校验）；
  ② 本次「新版本」= 2121 之后累积的客户端修复（含 DirectMessageOpenGate 生命周期修复）。
- 待办及验收失败项：A1/A2 待用户真机（覆盖安装或等弹窗）；无已知失败项。
- 已发布与仅候选的区别：APK、更新设置、API 镜像**均已上线并完成公网验证**；
  未做真机安装验证，未做正式应用商店发布（本项目一直为官网 APK 分发，非商店渠道）。
- 生产备份位置、恢复操作、漂移检查、可重试阶段：
  - 设置备份 `/opt/starchat/releases/app-update-platform-20260917/backup/settings-before-2127.json`（0600），
    回退执行 `publish_settings_2127.py rollback`；
  - API 前态镜像 `sha256:be11636c…` 与旧文件 `backup/app_update.py.pre`（sha `65a806b3…`），
    回退用 `api-rollback.json`；
  - 符号链接回退 `ln -sfn ChatFlow-0.3.92-build2121-arm64.apk latest-arm64.apk`（2121 包原位）。
- 运行中 CI/命令/自己创建的隧道（无凭据）：工作站 jumper SOCKS 隧道（loopback 18944）
  已在验证结束后关闭；无其他常驻进程。
- 下次恢复先检查的事实：`GET /api/v1/app-updates/latest?platform=android` 是否仍带
  `platform:"android"`；`app_latest_build` 是否仍为 2127（或更高）；`latest-arm64.apk` 指向是否正确；
  `releases/app-update-platform-20260917/` 是否为当前 API 的释放目录。
