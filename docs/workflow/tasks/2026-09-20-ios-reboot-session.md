# 2026-09-20 iOS 重启退出与持续 L07

## 恢复入口与授权
- 用户已明确批准方案并要求执行；无需再次询问实现权限。未授权公开发布。
- 设备：iOS 16.7.16、畅聊 0.3.96，精确 build 未确认；整机重启解锁首次打开异常，杀 App 重开正常。
- 独立工作树 `.worktrees/ios-reboot-session`，分支 `codex/ios-reboot-session-20260920`，基线 `83c2f196`。主工作区其他改动未覆盖。
- [批准计划](../../superpowers/plans/2026-09-20-ios-reboot-session.md)、[ADR](../../adr/2026-09-20-ios-reboot-session-preservation.md)、[验证记录](../../verification/2026-09-20-ios-reboot-session.md)。

## 已完成
安装标记重新加载；未注册安装检查保护数据可用性，未知目录状态不再作为清密钥依据。启动失败可重试/回到前台重试；已有安装保留锁屏后台路径。
有效业务会话保留，通过同一身份恢复聊天授权；恢复失败及退出竞争有回归覆盖。密码登录前只读预检本地元数据，不可恢复的本地错误不发送密码登录请求。用户提示按安全类别细分，不展示 L04/L07 或原始异常。
TDD 红绿证据已保存；Flutter 最终全量 3681 项通过，analyze 无问题；先规格符合性再质量/安全审查均通过。

## 尚未完成
仓库级 verify 与 iOS 原生 CI 结果待记录。iOS 16.7.16 真机整机重启、历史可读、锁屏来电、覆盖升级及账号切换未验收，无新 IPA/发布。没有受影响手机日志，不能认定唯一触发根因或历史物理删除，也不能声称恢复已丢失密钥。
A1/A5/A6 的真机部分待验收；A2/A3/A4 已有单元回归，A3 仅保证预检可确定的本地错误不触发远端登录，不承诺所有网络/目标库故障均不顶号。

## 阶段计时
调查开始精确时间未知；19:41 +08 仍在调查。约 19:45—20:07 实现、红绿测试与两轮独立审查，精确主动工时未知。最终 Flutter 全量耗时 2 分 35 秒；先前全量 3679 项通过后新增两项竞争回归，最终重新执行 3681 项。
verify 首次缺少隔离工作树 .env；后续两次慢运行主动取消，不记为通过。当前采用 .env.example 的非 BUSINESS 模板字段生成本地渲染环境，无生产秘密。
下一步：收集完整 verify、原生 CI 的退出码及输入身份，记录真机待验收边界。
