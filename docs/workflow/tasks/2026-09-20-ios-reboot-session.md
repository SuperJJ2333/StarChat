# 2026-09-20 iOS 重启退出与持续 L07

## 恢复入口与授权
- 用户已明确批准方案并要求执行；无需再次询问实现权限。未授权公开发布。
- 设备：iOS 16.7.16、畅聊 0.3.96，精确 build 未确认；整机重启解锁首次打开异常，杀 App 重开正常。
- 独立工作树 `.worktrees/ios-reboot-session`，分支 `codex/ios-reboot-session-20260920`，基线 `83c2f196`。主工作区其他改动未覆盖。
- [批准计划](../../superpowers/plans/2026-09-20-ios-reboot-session.md)、[ADR](../../adr/2026-09-20-ios-reboot-session-preservation.md)、[验证记录](../../verification/2026-09-20-ios-reboot-session.md)。

## 已完成
安装标记重新加载；未注册安装检查保护数据可用性，未知目录状态不再作为清密钥依据。启动失败可重试/回到前台重试；已有安装保留锁屏后台路径。
有效业务会话保留，通过同一身份恢复聊天授权；恢复失败及退出竞争有回归覆盖。密码登录前只读预检本地元数据，不可恢复的本地错误不发送密码登录请求。用户提示按安全类别细分，不展示 L04/L07 或原始异常。
TDD 红绿证据已保存；Flutter 最终全量 3681 项通过，analyze 无问题；先规格符合性再质量/安全审查均通过。

## 尚未完成
仓库级 verify 退出 0、iOS 完整原生编译及 iOS18/26 模拟器均通过。iOS 16.7.16 真机整机重启、历史可读、锁屏来电、覆盖升级及账号切换未验收，无新 IPA/发布。没有受影响手机日志，不能认定唯一触发根因或历史物理删除，也不能声称恢复已丢失密钥。
A1/A5/A6 的真机部分待验收；A2/A3/A4 已有单元回归，A3 仅保证预检可确定的本地错误不触发远端登录，不承诺所有网络/目标库故障均不顶号。

## 阶段计时
调查开始精确时间未知；19:41 +08 仍在调查。约 19:45—20:07 实现、红绿测试与两轮独立审查，精确主动工时未知。最终 Flutter 全量耗时 2 分 35 秒；先前全量 3679 项通过后新增两项竞争回归，最终重新执行 3681 项。
verify 首次缺少隔离工作树 .env；后续两次慢运行主动取消，不记为通过。最终采用 .env.example 的非 BUSINESS 模板字段生成本地渲染环境，无生产秘密。
下一步：使用同签名渠道候选保留数据覆盖安装，并在用户 iOS16.7.16 上执行 A1/A5/A6 真机验收。

## 原生检查与真机验收台账
- 冻结源码：`2d46320791f2d51f71b7143a4403acf12a314356`，独立分支已推送；未合并 main。
- 无签名 iOS CI：[35510017099](https://github.com/SuperJJ2333/StarChat/actions/runs/35510017099)，20:12 +08 左右启动，精确时间以 CI 为准；完整生产编译、iOS 18/26 模拟器持久化检查。此工作流不发布安装包。
- 输入 SHA256：验证目录 `final-input-hashes.json`，包含修改源码/测试及 pubspec.lock、Podfile.lock。

| ID | 验收操作 | 已有证据 | 尚需证据 |
| --- | --- | --- | --- |
| A1 | 在原安装保留数据升级，确认旧聊天可读；整机重启、解锁后首次打开仍同账号且旧聊天可读 | 安装/启动/会话单元回归通过 | iOS 16.7.16 实际版本/build、升级前后可读结果 |
| A2 | 本地聊天恢复失败但业务鉴权有效时，保留凭据并可重试 | bootstrap 与 login 回归通过 | 真机故障与恢复结果 |
| A3 | 安全存储不可访问/元数据损坏时，密码登录前停止 | 预检失败零业务登录请求断言通过 | 测试账号双设备结果；不对预检后的服务端失败作全称保证 |
| A4 | 各类错误显示位置、原因类别和下一步，无 L04/L07 及原始秘密 | 分类、包装、敏感字符串断言通过 | 设备实际错误截图或脱敏文字 |
| A5 | 同账号恢复、切换账号不串库；恢复期间退出，迟到结果无效；首次解锁后的锁屏来电 | 身份隔离/恢复竞争/已注册安装旁路回归通过 | 真机账号切换与锁屏来电 |
| A6 | 无签名原生编译与模拟器持久化，再对企业签名覆盖安装验收 | 原生编译与 iOS18/26 模拟器通过 | 同身份签名包及用户手机验收 |

设备验收不卸载、不清除 App 数据；记录版本/build 和结果即可，不收集消息正文、令牌、密钥或数据库。若历史仍不可见，先保留现场，按新的具体错误类别检查签名/钥匙串/本地库，不把“重新登录”当成数据恢复保证。

20:30 +08 仓库 verify 退出 0；API/Worker 2209 通过/58 环境跳过，耗时 21:55。完整日志见验证记录。

20:42 +08 确认 CI 三项 job 均 success。实现/本地测试与原生 CI 完成；外部剩余为签名候选及用户设备验收。无运行中的本地检查，临时 .env 已清理。主动执行精确工时未知；原生 CI 墙钟约29分钟，与本地检查部分重叠，不相加为总工时。
