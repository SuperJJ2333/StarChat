# 2026-09-24 iOS 2144 旧账号 L07 真机诊断

## 恢复入口

- 目标与授权：用户要求解释同企业签名的 2144/2172 登录故障、修复源码、以同企业签名覆盖升级并保留旧数据。用户随后明确接受“保留旧库后建立新设备，并承担部分旧消息可能无法解密的结果”。不清理手机数据、不绕过 Matrix/E2EE 连续性校验；新版回签与真机验收前不发布新 IPA。
- 关联：[2172 启动身份故障及停发](2026-09-24-ios-047-reinstall-identity-failure.md)、[2144 发行源码与签名证据](../../verification/2026-09-20-ios2144-permissions.md)、[9 月 20 日会话修复](2026-09-20-ios-reboot-session.md)。
- 当前状态：故障 iPhone 上的 2144/L07 **直接失败条件**已证实为旧绑定与实际 Olm Ed25519 指纹不等；分叉起因和 2172 启动错误是否同根仍未知。生产 iOS 2172 已停发，iOS 入口仍为 2144。用户同意归档旧库后建立新设备。`0.4.7+2173` 源码已实现只读预检、原身份候选、可重放归档、业务授权后显式确认、跨进程恢复及冷启动安全壳；Windows 全量 Flutter **4230 通过、9 跳过**，静态分析无问题，移动边界 **217 通过、1 跳过**。Domain 最终源码审查通过，Quality/Security 源码复核通过；Mac 模拟器 SQLCipher/WAL 4/4 与 Keychain 1/1 原生门禁、2173 CI IPA 构建与包内校验均通过。候选 SHA256 `d05e4ea1178121fa37d5db7a85e2d0e901b1ae57ea5fb9eb5df64544488b63c1` 已交接；企业回签、健康 2144 不卸载覆盖和故障机登录仍待完成。
- 负责人、工作树、文件所有权：`codex/online-room-refresh` 独立工作树。核心/认证实现由此前独立 agent 提交；root 负责集成、恢复边界、CI/发布门禁与证据；贯穿测试和 Quality/Security 审查分别由只编辑新测试文件/只读的 agent 执行。当前无人并发编辑相同文件。
- 最后更新时间：2026-09-25 03:35 +08。
- 下一条具体操作：请签名方以 2144 旧企业身份对 `0.4.7/2173` CI 候选 IPA 纯回签并返回最终文件；本机对最终 IPA 核对 Team、旧已签 App ID、Keychain、生产 APNs、版本和非签名 Payload，随后在健康 2144 iPhone 不卸载覆盖验证旧数据，并在故障机测试新设备恢复。两个设备及最终同 SHA 发布门禁通过前不恢复 iOS 更新入口。

## 真机事实与代码定位

- 用户确认：iPhone 为 iOS 16.7.16；2144 的旧账号 L07 在首次安装 2172 **之前**已经出现；同机从未登录过的新账号可正常登录；2144 在覆盖安装和删除 App 后安装两种情形均出现过旧账号 L07。这些是用户反馈，不把删除 App 等同于 Keychain 已清。
- 2144 对应发行源码 `7d1f15b5`。它已经包含 9 月 11 日卸载重装清理和 9 月 16 日设备轮换修复；9 月 20 日会话恢复增强 `2d463207` 尚未合入 2144。不能笼统归因于 2144 缺少此前 L07 修复。
- 2144 `login_controller.dart` 在业务登录成功后，于旧账号的 `matrix.userId != target` 分支调用 `selector.selectAccount` 并将阶段映射为 L07。该标签本身不含底层异常。
- 2026-09-24 **20:19:57 设备日志时间**，在连接本机 Windows 的故障 iPhone 上，用户按提示点一次旧账号登录并再次看到 L07。通过 `idevicesyslog --match E2EE_` 仅采集到按顺序的允许列表事件：`E2EE_ACCOUNT_SELECT_BEGIN`、`E2EE_CONTINUITY_FINGERPRINT_MISMATCH`、`E2EE_ACCOUNT_SELECT_CONTINUITY_MISMATCH`。三个事件的脱敏 Matrix 用户关联值相同；开始与选择失败共用一个 trace，连续性子操作使用另一 trace。
- 2144 `MatrixClientFactory.continuityMetadata` 中，`E2EE_CONTINUITY_FINGERPRINT_MISMATCH` **仅在保存的 `MatrixLocalBinding.ed25519Fingerprint` 与刚打开的 Matrix 客户端实际 Olm Ed25519 指纹不等时**记录，随后抛出身份错配并拒绝发布该客户端。这证实 L07 的直接原因是**旧账号本地密码学身份与绑定不一致**，不是业务密码登录失败，也不是单纯由 2172 注入动态库造成的旧版 L07。
- 真正导致两份指纹分叉的更早事件仍未知。可能的来源涉及旧数据库/密钥/绑定的不同安装世代或早先写入中断；现有日志不能区分，更不能证明企业签名或源码某一提交是唯一根因。2172 启动前的“本地聊天身份校验未通过”尚无同级真机事件码，不直接合并为本次同一故障。
- 源码顺序风险：vendored Matrix SDK `client.dart` 在 `Client.init` 中读取 `box_client.olm_account`；若其为 null，`olm_manager.dart` 会创建新 Olm 账号并上传设备公钥，随后写回本地库。应用 `MatrixClientFactory.create()` 先调用该初始化，`continuityMetadata` 才检查保存的 Ed25519 指纹。缺失 pickle 是一种能造成持续 L07 的条件，但无事前数据库快照，**不能断言故障手机曾满足该条件**。现有启动前预检不得调用会生成数据库密钥、建表、迁移文件或初始化 SDK 的接口。
- 发行签名风险：归档 2144 已签 App ID 为 `ZXB3TS7QD4.cn.edu.buaa.wxwork.notifyext`，与 Bundle ID 不符；仅沿用证书不能证明新的正确 App ID 可无损覆盖。当前发布门禁对旧/新 App ID 字面相等的条件与归档旧包不兼容。需要在仍可登录且持有旧数据的真机上对最终包做不卸载覆盖，不能用本故障机填写 `login_preserved=true`。
- 2144 与 2172 企业证书、Team ID、Keychain 组相同，但已签 App ID 不同；“同一企业证书”不能证明两个包的完整签名身份相同。2144 的旧账号 L07 先于 2172 且新账号可登录，也排除了 2172 回签变动作为**此旧版 L07 的必要原因**。

脱敏证据：[可提交的取证记录](../../verification/2026-09-24-ios2144-old-account-l07.md)、[本地 JSON](../../verification/artifacts/2026-09-24/ios2144-l07-diagnostic/sanitized-evidence.json)。仅保留设备日志时间、事件码、阶段/结果及同一用户布尔值；原始设备日志已在本地覆盖为说明文字，未上传、未提交账号、钥匙串值、数据库、消息或恢复密钥。采集结束后已停止 syslog 进程和临时 SOCKS 隧道。用于采集的第三方 Windows 工具包来自 `L1ghtmann/libimobiledevice` release `suite-exe-21d57a9`，下载字节对照 GitHub 资产 SHA256 `948d20c5f6460ab9d9ac6b5fdeba00ddde0c62f59882e1e1e577ed16b3ae8abe`；工具仅留在忽略的验证目录，没有纳入源码。

## 验收台账

| ID | 场景及预期 | 实现 | 测试及证据 | 发布 | 真机反馈/缺口 |
| --- | --- | --- | --- | --- | --- |
| L07-CAUSE | 旧账号 L07 应定位到具体安全分支 | 只读调查 | 本机 iPhone 同次复现三事件；源码中唯一触发条件是指纹不等 | 无 | 直接失败条件已确认；最初分叉时刻未知 |
| L07-DATA | 保留现存聊天身份与数据 | 未改数据 | 无清库/钥匙串写入/安装操作 | 无 | 不可在未知来源下自动重置指纹或生成新身份 |
| L07-RECOVERY | 在不弱化 E2EE 下恢复旧账号可用 | 只读预检、旧库归档、原身份候选、新设备显式确认、授权后 pending-journal 重放及安全启动壳已实现 | Domain 最终源码审查通过；Windows 全量 Flutter 4230/4230、9 skipped，原生 SQLCipher/WAL 4/4、Keychain 1/1 通过，真机未验 | 无 | 原始数据库/密钥对是否仍存在未知；故障手机实测待新包 |
| IOS2172-START | 判定 2172 启动身份错误是否与本次同根 | 未定 | 现仅有用户界面文案，无对应设备事件码 | 2172 已停发 | 需独立复现及诊断 |

## 阶段计时及交接

- 本轮调查开始精确时间未知；设备日志复现点 20:19:57 +08，脱敏和关闭采集约 20:23 +08。主动分析、工具运行和等待用户操作有交叠，不伪造总耗时。
- 当前尚无运行中的 iOS CI、设备日志采集器或临时 SOCKS 隧道；2173 CI 候选已构建并交接，未写生产。原始手机密钥/数据库没有被复制到工作站。`verify.ps1` 本轮 exit 1 的准确原因和后续逐项通过门禁见[验证记录](../../verification/2026-09-24-ios2144-old-account-l07.md)。
- 成功的 iOS CI `36044338640`：模拟器预检 2026-09-25 02:53:18–03:16:35 +08（23 分 17 秒）；signed build 03:16:45–03:28:12 +08（11 分 27 秒）。两项顺序执行，未并发相加。候选下载与交接约 03:30–03:35 +08，精确结束时间未单独采集。
- 下一步按 CI 原生结果和最终回签 IPA 验证故障机及健康 2144 机器；不要求用户再卸载旧版。若原始数据库/私钥早已丢失，新源码也无法凭空恢复旧加密聊天记录。
