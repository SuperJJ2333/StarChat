# 后台返回提示其他设备登录：只读调查

## 恢复入口

- 用户反馈：Android/iOS 偶发退登录；从后台返回出现“其他设备登录”，没有同时在两平台登录。最新事件时间、账号、安装 build 尚未提供，已发出补充问题。
- 授权范围：定位原因，生产只读核查；本轮未修改认证行为、部署或构建。认证改动仍需批准 ADR 与领域/质量安全评审。
- 状态：调查，等待事件匹配信息；不是已修复。
- 工作树：`.worktrees/chat-reliability-diagnostics`，源码 ca4a306a；本任务只拥有此记录和 session-exit-audit 证据目录。
- 更新时间：2026-09-21 22:48 +08:00。
- 下一步：用用户提供的账号与事件时段匹配会话撤销原因、创建时间及设备标识；不要打印令牌、密码、原始个人数据。确认安装 build 是否包含独立 iOS2145 修复。

## 已确认事实与限制

1. 客户端回前台主动检查会话，服务端 SESSION_REPLACED 导致清除业务登录态并显示“其他设备登录”。发现时间不等于失效时间。
2. 生产 tokens.py 的 issue_pair 在同一设备重新登录时也撤销旧会话为 SESSION_REPLACED；本地既有参数化测试覆盖同设备与跨设备，复现成立。不能据提示断言真的有第二台手机。
3. Matrix 授权/绑定的部分校验将其他已撤销或不存在会话也归入 SESSION_REPLACED，存在原因映射过宽。未证明该分支触发本次用户事件。
4. 22:44 +08 生产聚合：过去七天 SESSION_REPLACED 39 条，其中 3 条替代会话设备键相同、36 条不同；过去24小时无该原因撤销。不同设备键不等于不同物理设备。另有 TOKEN_REUSE 12 条，尚未关联用户事件。
5. 最新服务启动于 22:32:14 +08；JWT 密钥、issuer、数据库地址与更新前相同，91个当前有效会话全部早于本次更新。没有本次更新批量清除会话证据，不代表已排除所有历史更新或响应丢失窗口。
6. tokens.py、api/identity.py、matrix_login.py、matrix_sessions.py 本地 SHA256 与生产一致。生产镜像 sha256:1e3f3cd887cd15db708160caa0eb79f201db061fc51d1627f2b21048fb965f42。
7. 令牌轮换提交后响应丢失，再次使用旧刷新令牌会触发重用保护；这是待事件证据支持的独立候选，不能当成“其他设备登录”的已确认根因。
8. 既有 iOS2145 重启/权限修复在另一工作树，本轮未确认用户已安装；此前聊天诊断客户端也尚未构建分发，不应宣称这些日志已在用户手机生效。

## 验证及证据

- [只读生产结果](../../verification/artifacts/2026-09-21/session-exit-audit/production-result.json)，包含观察时间、聚合、环境比较布尔值及服务文件 SHA。SQL 使用只读事务与5秒超时；无生产登录或造会话操作。
- [生产探针](../../verification/artifacts/2026-09-21/session-exit-audit/production_probe.py)。
- Windows / Python3.12.10，PYTHONPATH=services/business-api，命令：`../../.venv/Scripts/python.exe -m pytest tests/business_api/identity/test_mobile_sessions.py tests/business_api/identity/test_tokens_and_recovery.py -q`。
- 退出码0，16 passed /0 failed /0 skipped，1.68秒；[日志](../../verification/artifacts/2026-09-21/session-exit-audit/focused-tests.log)。这是既有行为复现，不是修复红绿证据。
- 本轮无可执行代码改动，不重跑全量发布门禁。没有新的生产变更、回退或持续后台命令。

## 阶段计时

- 初始源码追踪起止未完整记录，不估算总耗时。
- 生产观察：2026-09-21 22:44:39 +08。
- 本地聚焦验证：22:47 +08，pytest1.68秒，命令墙钟3.23秒。
- 22:48 +08 记录现状；待账号/时间/build，不能将候选路径确认为用户事件根因。

## 22:50 +08 账号事件已匹配

用户补充 Android v0.3.102、9月21日北京时间19–21时及登录用户名。查询只在内存使用用户名，输出不含用户名、用户ID、真实设备ID或令牌。

- 19:55:49.770990：旧会话最后一次成功轮换刷新令牌。
- 19:57:55.009348：旧会话撤销原因为 **TOKEN_REUSE**。
- 19:58:19.683704：同一设备标识创建新会话；19:58:20完成聊天登录和绑定。
- 22:47:38仍有新会话成功轮换，22:49观察时未撤销。
- [账号事件证据](../../verification/artifacts/2026-09-21/session-exit-audit/account-result.json)，[只读参数化探针](../../verification/artifacts/2026-09-21/session-exit-audit/account_probe.py)。本次查询退出码0，墙钟2.68秒。

因此此次退登录的直接原因已定位到刷新令牌重用保护，不能归因于该时段其他设备新登录。它早于22:32的最新更新，不能由该次更新触发。失效与最后成功轮换相隔约125秒；未记录具体被重用的令牌标识，不能据间隔断言重用了最后一枚令牌。

当前源码的刷新请求8秒超时，服务端先提交轮换、客户端收到响应后再保存；结果丢失/本地保存失败后再次提交旧令牌有触发此保护的机制。客户端已有单实例刷新合并，生产主入口只创建一个 BusinessApiClient；尚无现场证据确定响应丢失、保存失败或其他重复请求中的哪一种发生。用户看到“其他设备登录”与数据库原因不一致；Matrix泛化映射已发现，但仍需安装包精确build/客户端事件确认本次提示来自哪条路径。

下一步改为：针对刷新提交后响应/持久化失败窗口建立故障注入复现，区分终止原因，设计保持重放防护的恢复机制和无敏感内容诊断；认证行为改动按批准ADR与评审要求实施。此记录不表示已经修复或发布。
