# 跨会话工作流配置与耗时复盘

授权：用户2026-09-10要求设置BUG修复、新增功能、Android/iOS新版本发布流程，要求跨聊天会话使用，并说明超过两小时的原因。范围仅文档/规则；不自动部署新版本。

工作树：.worktrees/ios-enterprise-2085，分支codex/mobile-delivery-workflow，基线910f653e。复用干净工作树，未复制.env或生成新的构建产物。所有权：实施计划列明的文档；其他任务的文档整理保持原状。

## 验收

| ID | 要求 | 实现与验证 |
| --- | --- | --- |
| W01 | 跨会话恢复 | Codex从AGENTS、Claude Code从CLAUDE→AGENTS进入当前状态→独立任务模板/计划/证据，不依赖聊天记忆 |
| W02 | BUG与功能流程 | 定义/定位设计/失败用例/最小实现/相关回归/两层审查 |
| W03 | 双端发布 | 签名与账号数据连续性、平台独立配置、旧版桥接、包→入口→设置→验收顺序 |
| W04 | 减少耗时 | 环境前置检查、按影响复用证据、同源不重复构建、持久备份和恢复 |
| W05 | 时间透明 | 必填阶段计时表；主动/工具/外部等待/返工区分，缺记录写未知 |

## 可核对的历史耗时

| 证据 | 耗时 | 含义 |
| --- | --- | --- |
| 2085 repository-verify.log及候选报告 | 937.26秒（15分37秒） | 该次API/Worker全量1714通过；不是整个交付时间 |
| 钱包CI修复Linux成功日志/run34490176702 | 361.62秒（6分2秒） | 另一轮后端全量1815通过；不能与上一轮当作同一两小时会话简单相加 |
| 钱包CI专项green.log | 7.67秒 | 68项受影响用例；说明先专项能够快速确认修复 |

其余曾执行：Flutter全量、两端原生/发行构建、iOS18/26模拟器、企业签名交接、APK重建签名、顺序上传和两侧完整下载校验。旧记录没有完整阶段起止，无法精确拆出用户所述两小时，也不把外部签名间隔算成编码工时。

可避免返工：错工作树的editable Python导入；干净工作树测试目录/.env预检遗漏；--no-pub复用了测试插件注册文件；本地与CI重复全量等待；容器/tmp唯一设置备份被另一任务重建清除；在只需查具体文件时过宽扫描历史artifact目录。以上原因已逐项映射到新工作流，不将其全部归咎于测试或网络。

## 本任务状态与交接

文档已实现；2026-09-10T23:21:14+08:00进入验证阶段，完成前校验全部本次本地Markdown链接、git diff --check和独立规则一致性审查。此次不变业务代码，按新规则不构建APK/IPA、不运行业务全量、不改生产。

2026-09-10T23:23:18+08:00验证结束：8份文档、26个本地链接均通过，git diff --check通过；独立规格/质量审查通过。审查发现Claude Code发现入口缺口，补最小CLAUDE.md后复核通过。可计时验证/审查阶段2分4秒，包含并行审查，不再另行相加。

本任务开始时尚未建立精确起始计时，缺失段记为未知；后续任务须从第一步记录。下一步是按此流程执行新需求；iPhone验收待办和其他任务后续客户端修复见current-state.md，不能因本工作流交付而标记完成。
