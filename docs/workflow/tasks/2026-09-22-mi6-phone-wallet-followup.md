# Mi 6 手机换绑、钱包与转让反馈修复

## 恢复入口

- 授权：用户反馈四项缺陷并要求继续；承接此前 Flutter Debug 覆盖安装 Mi 6 授权。没有生产部署、真实短信或资金操作授权。
- 关联：既有 pricing-auth-redpacket-group 计划、ADR-0077 取消申请补充；本轮保留其他任务修改。
- 状态：客户端验证通过、2158 已覆盖安装 Mi6并启动；后端本地实现，生产不具备配套接口。
- 工作树：主工作区，存在未提交修改；禁止整树当发布候选。
- 下一步：检查 mi6-followup/verify-exit.txt 与 verify-full.log（运行会话65623）；收集用户2158反馈。后端候选整合按关联Prompt继续。APK重解包与隔离PostgreSQL验证已通过。

## 验收台账

| ID | 场景 | 本地实现与验证 | 线上/真机限制 |
| --- | --- | --- | --- |
| M1 | 旧客户端转入群主 | 无业务意图且路由不支持时使用兼容说明，保持实际 Matrix 权限；已知待处理意图不放宽。29 项测试通过 | 不代表业务群主登记已同步；旧客户端直改权限治理仍待服务端 |
| M2 | 换绑请求报错 | 非 JSON 错误保留 HTTP 状态，区分未开放/关闭/供应商不可用；未知 channel 不猜测。手机 8 项、错误处理组合 11 项通过 | 线上 old-request 路由实测 404；历史短信供应商验收通过不等于此部署包含手机 API |
| M3 | 参考汇率与预计到账 | 两种钱包模式均获取参考汇率；BigInt 计算充值预计点钻、提现参考 USDT；不进入实际记账请求 | 线上 fx 路由 404，不伪造参考值；实际金额仍由客服批准 |
| M4 | 取消充值 | 旧充值新增真实幂等取消 API 和按钮，迁移0082；零余额变动，取消后实际收款转人工核对且保留义务。后端123项、钱包121项通过 | 未部署；线上无取消路由。真实PostgreSQL16.9迁移、触发器、两赢家并发、同键幂等通过，见cancel-pg-verification.md |

## 版本与证据

- 目标 APK：0.3.103+2158 Debug、arm64、com.liuhetong.mobile；最终签名沿用已验证身份，禁止清除数据。
- Flutter 全量3751通过；analyze无问题。证据目录：`docs/verification/artifacts/2026-09-22/mi6-followup/`。
- 完整 verify 已启动，结果待回填；不得引用历史 PASS 冒充本轮通过。上轮4项 TRON/储备观察时间失败需与当前结果区分。
- Mi6重连后于2026-09-22T23:43:16+08完成2158覆盖安装并启动，未清数据；installed-verification.json记录最终身份。
- 线上只读核验：business-api:refresh-040-20260922 缺少 phone/fx/recharge/transfer-intents/旧充值取消路由。
- [后端发布就绪审计](../../verification/2026-09-22-phone-wallet-release-readiness.md)：本地缺少线上刷新恢复协议，迁移链分叉；67项源码差异不能直接部署。

## 阶段计时与交接

- 调查、子任务实施起止跨上下文，未保留完整计时，不估算精确总时长。
- Flutter 全量日志实际耗时2分46秒，analyze30.2秒；构建/verify以日志时间为准。
- 未部署、未真实短信、未真实资金操作。下一批服务端候选必须保留已发布 refresh recovery，合并两条迁移历史，并完成隔离迁移/恢复和领域安全复审后再提出具体发布申请。

