# UI 评审验证记录 — 分割线统一 / 通过朋友验证页间距 / 好友接受招呼幂等（2026-09-19）

**范围：** `apps/mobile_flutter`（朋友圈分割线、朋友圈可见范围页、通过朋友验证页、共享分割线组件）+ HTML 设计演示 `frontend/`（分割线 token 与列表/卡片表面）+ `UI_DESIGN.md` 规范 + 组件注册表。
**不属于本次范围：** 业务逻辑、网络/权限语义、Matrix 协议、账本与钱包；`lib/app_home.dart`、`lib/features/matrix/**` 未改动（缺陷 3 的生产产生点在 `lib/app_home.dart`，按“不修改他人持有文件”的约束以修复建议提交，见 §7）。
**视觉源：** `frontend/` 下的 HTML demo。**Figma 已退役：本次变更仅更新 HTML demo（`frontend/index.html` → `?module=moments`、`?screen=moments-timeline-default`、`?screen=moments-visibility-public`、`?module=contacts`、`?screen=foundation-components-catalog`）。**

---

## 1. 改动清单（需求 → 文件:行）

### 需求 1：统一分割线（朋友圈改用共享渐隐分割线 + 全仓审计 + 规范入库）

| 位置 | 关键内容 |
|---|---|
| `apps/mobile_flutter/lib/ui/moments/wechat_moment_tile.dart:98-114,248-255` | 朋友圈动态行去掉 `BoxDecoration(border: Border(bottom: BorderSide(...)))` 实心线，改为 `Column` 尾项 `const WeChatGradientDivider(key: Key('moment-tile-divider'))`（`:253`）；保留原有 `elevatedSurface` 背景与 `MomentReactionTokens.shadow` 阴影；线画在行内，行高不因画线改变 |
| `apps/mobile_flutter/lib/ui/moments/wechat_moment_reactions.dart:74-77,119-122` | 互动面板内部区块分隔线删掉 `Container(height: 1, color: MomentReactionTokens.divider)` 第二套实现，改为 `_divider(key) => WeChatGradientDivider(key: ValueKey(key))`；`moment-likes-divider` / `moment-comment-divider-*` 键保持不变 |
| `apps/mobile_flutter/lib/features/moments/moment_visibility_page.dart:76-79,92-95` | 「谁可以看」组内行分隔线由 `SizedBox(height: .5, child: ColoredBox(...))` 改为 `WeChatGradientDivider(indent: WeChatSpacing.lg)`（保留 16dp 左缩进），键 `visibility-divider-primary` / `visibility-divider-submenu` |
| `frontend/src/styles/primitives.css` `.c-contact-row:413`、`.c-conversation-row:219`、`.c-moment-card:642`、`.c-ledger-row:866` | 由 `border-bottom: 1px solid var(--color-divider)` 改为 `background-color` + `background-image: var(--divider-fade)` + `background-position: bottom center` + `background-size: 100% var(--size-hairline)`（行高不变） |
| `frontend/src/styles/components.css` `.c-action-sheet__header/__option/__cancel:629`、`.c-transaction-row:809`、`.c-moment-tile:835`、`.c-moment-reactions__comment:953`（+ `__editor:966`） | 同上改为共享渐隐分割线（`.c-moment-reactions__comment[data-selected]` 的 `background` 简写改为 `background-color`，避免覆盖渐变） |
| `frontend/tests/gradient-divider.test.mjs:56-101` | 新增 2 条断言：所有列表/卡片表面必须画 `--divider-fade` 且不得再有 `border-bottom/top`；被保留的实心细线（导航栏/TabBar/弹窗操作区/输入框描边）必须仍是实心 |

### 需求 2：「通过朋友验证」页「已添加」徽标与「打开聊天」按钮间距

| 位置 | 关键内容 |
|---|---|
| `apps/mobile_flutter/lib/features/contacts/friend_request_review_page.dart:177-182` | 状态徽标与「打开聊天」按钮之间新增 `const SizedBox(height: WeChatSpacing.md)`（12dp，md 网格）；徽标（`brandTint` 底 + `brandPrimary` 粗体）与按钮（`brandPrimary` 填充 / 白字 16sp / 48dp 高 / `WeChatRadius.dialog` 12dp 圆角 / busy `CupertinoActivityIndicator` + `onPressed: null`）**均未改动** |

### 缺陷 3：好友接受系统提示与打招呼重复发放

| 位置 | 关键内容 |
|---|---|
| `apps/mobile_flutter/lib/features/contacts/friend_acceptance_greeting_ledger.dart`（新建） | `FriendAcceptanceGreetingLedger`：持久化（SharedPreferences）一次性发放账本，`claim/release/greeted`，账号分区、有界 200 条；`friendAcceptanceGreetingKey(...)` 直接委托 `friendAcceptedTransactionId`，保证本地幂等键与 Matrix 发送事务 id 逐字一致 |
| `docs/verification/2026-09-19-friend-acceptance-greeting-idempotency.md`（新建） | 生产产生点在 `lib/app_home.dart` 的 `_establishDirectChatAndGreet`（当前 `:1032-1047`，行号随并发 outbox 改动漂移）里**无条件**调用 `sendFriendAccepted`，**需由 app_home/matrix 拥有者应用**的精确修复建议（文件:行、根因、最小改法 diff、幂等键设计、纵深防御与补测建议） |

### 追加需求（任务进行中由上层授权）：消息状态词表统一——会话内气泡 `waitingNetwork` 显示「等待网络」

| 位置 | 关键内容 |
|---|---|
| `apps/mobile_flutter/lib/ui/chat/wechat_message_bubble.dart:7-16,95-130` | 等待分支文案由固定 `'等待发送'` 改为 `state == MessageDeliveryState.waitingNetwork ? '等待网络' : '等待发送'`（`:119-121`；行内 `const Row` 相应改为非 const，`Icon/SizedBox/TextStyle` 仍 const）；`MessageDeliveryState` 文档注释登记五状态词表；`key: 'message-delivery-waiting'`、灰色小时钟、`Semantics(label: '立即重试')`、点击立即重试、红色感叹号只属于 `failed` 全部未变 |
| `apps/mobile_flutter/lib/ui/chat/super_emoji_message.dart` | **无需改动**：它只把 `state` 透传给 `WeChatMessageBubble`（`:51`），自动继承新文案 |
| `apps/mobile_flutter/test/features/matrix/offline_send_state_test.dart:297-324` | 既有断言更新：waitingNetwork 由 `find.text('等待发送')` 改为 `find.text('等待网络')`，并新增 `find.text('等待发送') findsNothing`；failed 用例新增 `find.text('等待网络') findsNothing`（原 `等待发送 findsNothing` 保留，断言未放松） |
| `UI_DESIGN.md` §7 消息 | 新增五状态词表（`queued` 等待发送 / `sending` 发送中 / `waitingNetwork` 等待网络 / `failed` 发送失败 / `sent` 正常），并写明：气泡 `waitingNetwork`→「等待网络」+ 灰色小时钟、点击立即重试、绝不红色感叹号；「等待发送」保留给本地排队（`queued`）；红色感叹号只属于 `failed`；实现入口 `OutboxStatus.label`（`lib/core/outbox/outbox_message.dart`）与 `MessageDeliveryState` |

> 说明：会话内气泡当前只渲染 `waitingNetwork`（「等待网络」）与 `failed`（红色感叹号）；`RoomDeliveryState.local/sending/sent` 由房间页统一归为「正常」不显示标记（微信语义，`room_page.dart:3656-3662`，属 message-outbox 持有文件）。三元表达式的 `'等待发送'` 分支即为 outbox `queued` 状态预留，词表与 `OutboxStatus.label` 逐字一致。
> `test/features/matrix/offline_first_opening_test.dart:201` 断言的「等待发送」位于 pending conversation 页的 outbox 排队行（`OutboxStatus.queued` → 「等待发送」，`pending_conversation_page.dart:292`），语义正确，**按实际语义保留、未修改**。

## 2. Token / 共享组件 / registry / HTML demo 改动位置

**Token 对照（Flutter ↔ HTML 同名同值；本次未新增 token，只补对照行与规范）**

| Flutter | HTML (`frontend/src/styles/tokens.css`) |
|---|---|
| `WeChatDividerTokens.hairline = 1.0` | `--size-hairline: 1px`（`:81`，本次新增 `tokenParity` 对照行） |
| `WeChatDividerTokens.edgeAlpha = 0.0` | `--divider-fade-edge-alpha: 0`（`:138`） |
| `WeChatDividerTokens.centerAlpha = 0.5` | `--divider-fade-alpha: 0.5`（`:137`） |
| `WeChatDividerTokens.coreStart = 0.18` | `--divider-fade-core-start: 18%`（`:139`） |
| `WeChatDividerTokens.coreEnd = 0.82` | `--divider-fade-core-end: 82%`（`:140`） |
| `WeChatColors.divider` / `darkDivider` | `--color-divider` + `.ui-screen[data-theme="dark"]` 的 `--divider-fade` |

**共享组件（复用，无第二份实现）**
- Flutter：`apps/mobile_flutter/lib/ui/components/wechat_gradient_divider.dart` → `WeChatGradientDivider(height, indent, endIndent)`
- HTML：`frontend/src/components/divider.js` → `app-divider`（根 class `.c-gradient-divider`，`components.css:221`）
- 本次新增使用点：`WeChatMomentTile`、`WeChatMomentReactions`、`MomentVisibilityPage`；HTML：`.c-conversation-row`、`.c-transaction-row`、`.c-moment-tile`、`.c-moment-card`、`.c-ledger-row`、`.c-action-sheet__*`、`.c-moment-reactions__comment/__editor`

**Registry**（`packages/ui-contracts/changliao-component-registry.json`）
- `gradient-divider` 新增 `consumers`（6 个使用点，便于审查覆盖范围）
- `moment-interactions`：`variants` 由 `{}` 改为 `{"divider": ["gradient"]}`，`tokens` += `divider`
- `moment-reactions`：`variants` += `{"divider": ["gradient"]}`，`tokens` += `divider`
- `tokenParity` 新增 `{"category":"dividers","html":"--size-hairline: 1px","flutter":"static const hairline = 1.0;"}`
- 新增修订块 `dividerUnification20260919`（需求、覆盖范围、保留例外、三个验证摘要）
- **未新增组件、未新增 screen**：`screens.expectedCount` 仍为 372，`tests/mobile/test_ui_component_registry.py` 无需改动

**UI_DESIGN.md**
- 头部新增「最近修订：2026-09-19」行；§2 `divider` 行用途指向 §19；§6 复用组件清单新增 `WeChatGradientDivider`，`WeChatListTile` 补 `showDivider`
- §11.2 `WeChatContactTile` 行由「底部分隔线左缩进 68dp」修订为「行底共享渐隐分割线（整行宽）」
- §12.1 / §12.2 注明朋友圈时间线与互动区块使用共享渐隐分割线
- **新增 §19「列表与卡片分割线（渐隐分割线，2026-09-19 生效）」**：共享组件与 HTML 对应物、几何/alpha/stops 表（含 `--size-hairline` 行）、5 条硬性约束（禁止第二套实心实现、深浅色按主题解析、行高不因画线改变、缩进用 `indent`、registry+demo 同步）、6 条允许保留实心线的例外及逐条理由

## 3. Token 表新增行（UI_DESIGN.md §19 原文摘录）

```
| Flutter token | HTML token | 值 | 用途 |
| WeChatDividerTokens.hairline | --size-hairline | 1.0（1px） | 分割线高度（1 逻辑像素） |
| WeChatDividerTokens.edgeStartStop | --divider-fade 第 1 个色标 | 0.0（0%） | 左端点 |
| WeChatDividerTokens.coreStart | --divider-fade-core-start | 0.18（18%） | 中段起点（0 → 18% 由透明渐入） |
| WeChatDividerTokens.coreEnd | --divider-fade-core-end | 0.82（82%） | 中段终点（82% → 100% 再渐出到透明） |
| WeChatDividerTokens.edgeEndStop | --divider-fade 第 4 个色标 | 1.0（100%） | 右端点 |
| WeChatDividerTokens.edgeAlpha | --divider-fade-edge-alpha | 0.0 | 两端不透明度（完全透明） |
| WeChatDividerTokens.centerAlpha | --divider-fade-alpha | 0.5 | 中段不透明度（低于实心分割线的 1.0） |
| WeChatColors.divider（色源） | --color-divider（配合 --divider-fade） | #D9D9D9 / #2C2C2C | 浅色 / 深色取色 |
```

## 4. 全仓「实心 1px 分割线」审计（`git grep -n "Divider\|Border(bottom\|border-bottom\|hairline" -- lib frontend/src`）

### 4.1 本次已统一为共享渐隐分割线

Flutter：`WeChatMomentTile`（朋友圈动态行）、`WeChatMomentReactions`（互动面板区块）、`MomentVisibilityPage`（组内行）；
HTML：`.c-list-tile`、`.c-contact-row`（上一轮）、`.c-conversation-row`、`.c-transaction-row`、`.c-moment-tile`、`.c-moment-card`、`.c-ledger-row`、`.c-action-sheet__header/__option/__cancel`、`.c-moment-reactions__comment/__editor`。

### 4.2 保留实心细线（逐条理由）

| 位置 | 理由 |
|---|---|
| `lib/features/moments/moment_composer_page.dart:362-366`（`CupertinoNavigationBar.border` 底部 0.5px） | 导航栏 chrome 边缘线，不是列表/卡片分隔；与 HTML `.c-navigation-bar`（`components.css:30`）保持一致 |
| `lib/ui/components/top_more_menu.dart:129-139`（菜单行分隔线，`TopMoreMenuTokens.divider` = `0x3DFFFFFF`） | 深色浮层菜单自带深色专用 token（白色 24%）；共享分割线取 `divider` token（浅灰）在该表面上不可读。HTML 对应 `.c-anchored-menu__item`（`components.css:983`）、UI_DESIGN.md §16「0.5dp 白色 24%」既有规范 |
| `frontend/src/styles/components.css:30`（`.c-navigation-bar`）、`:69`（`.c-tab-bar`） | 导航栏 / TabBar chrome 边界，两端渐隐会让导航区与内容区失去边界 |
| `frontend/src/styles/components.css:592`（`.c-dialog__actions` 横线 + `.c-dialog__button--confirm` 竖线） | 弹窗固定两列按钮，横线必须与中间竖线成对；渐隐横线会让竖线悬空 |
| `frontend/src/styles/components.css:1063`（`.c-image-editor__sheet .c-image-editor__control`） | 图片编辑器底部工具条控件行，属于控件边缘而非列表行 |
| 输入/按钮/色板/图标样例等 `Border.all` 描边（`.c-form-field__input`、`.c-search-field__input`、`.c-forward-picker__search`、`.c-action-button`、`.c-image-editor__crop-chip`、`.c-icon-sample` 等） | 控件轮廓（`border: 1px solid`），不是分隔线 |
| 管理后台 `admin-*.css` / `gallery.css` 的 `--admin-border`（表头/单元格 `border-bottom`、侧栏 `border-bottom`） | 管理后台使用独立的 `--admin-*` token 体系与密集数据表格；表格横竖线必须成对，不属于移动端设计演示组件体系 |
| `lib/ui/chat/message_action_sheet.dart:115-119`（多选操作条顶部 0.5px `CupertinoColors.separator`） | 底部操作条（工具条）边缘，非列表行分隔 |

### 4.3 发现但**未修改**的列表/卡片分隔线（不在本次声明持有的文件内，建议由对应文件持有者统一）

| 位置 | 现状 | 建议 |
|---|---|---|
| `lib/features/profile/profile_page.dart:291-302` | 「我」页面 57dp 可点行，行底 `Border(bottom: .5, darkDivider/divider)` | 改为行内 `WeChatGradientDivider`（行高 57dp 不变） |
| `lib/features/settings/notification/notification_settings_page.dart:322-333` | 设置分组卡片 `Border(top + bottom)` 0.5px | 卡片上下边界改用共享渐隐分割线（需把卡片拆成「线 + 内容 + 线」） |
| `lib/features/transfer/chat_transfer_detail_sheet.dart:301-304,322-327` | 转账详情卡片行分隔线 `const BoxDecoration(border: Border(top: BorderSide(color: WeChatColors.divider)))` | 改用共享组件；顺带修掉**深色模式缺陷**——这里用的是未解析的浅色 `divider`，深色下会画出浅灰线 |
| `lib/ui/chat/chat_search_page.dart:503-512,1204-1212` | 搜索结果行 / 搜索页行底部 0.5px | 改为行内 `WeChatGradientDivider` |
| `lib/app_home.dart`（约 `:2891`，行号随并发 outbox 改动漂移） | 0.5px 行底分隔线 | **该文件由 message-outbox 特性负责人持有**，需其应用（改为共享组件） |
| `lib/features/matrix/chat_red_packet_sheet.dart`、`direct_chat_info_page.dart`、`group_chat_info_page.dart` 的分隔线 | matrix 域文件的实心线（`BoxDecoration` 系列） | **`lib/features/matrix/**` 非本次持有**，建议由 matrix 持有者按 §19 统一 |

## 5. 红 → 绿证据（真实输出）

**红（先写测试，2026-09-19 04:2x）** `flutter test test/features/moments/moment_divider_unified_test.dart test/features/contacts/friend_request_review_spacing_test.dart test/features/contacts/friend_acceptance_greeting_ledger_test.dart`
完整输出：`docs/verification/artifacts/2026-09-19/red-focused.txt`

```
test/features/contacts/friend_acceptance_greeting_ledger_test.dart:2:8: Error: Error when reading
  'lib/features/contacts/friend_acceptance_greeting_ledger.dart': 系统找不到指定的文件。
  Error: Method not found: 'FriendAcceptanceGreetingLedger'.        （×13）
  Error: Method not found: 'friendAcceptanceGreetingKey'.           （×2）

朋友圈动态行必须由共享 WeChatGradientDivider 画分隔线
  Expected: exactly one matching candidate
    Actual: _KeyWidgetFinder:<Found 0 widgets with key [<'moment-tile-divider'>]: []>

visibility-divider-primary 必须存在
  Expected: exactly one matching candidate
    Actual: _KeyWidgetFinder:<Found 0 widgets with key [<'visibility-divider-primary'>]: []>

moment-likes-divider 不得再自行实现一条实心分割线
  Expected: <Instance of 'WeChatGradientDivider'>
    Actual: Container:<Container-[<'moment-likes-divider'>](bg: CupertinoDynamicColor(...alpha: 1.0000,
            red: 0.8510, green: 0.8510, blue: 0.8510...), h=1.0))>

徽标与按钮之间的实际间距是 0.0dp，必须 ≥ WeChatSpacing.md(12.0)
  Expected: a value greater than or equal to <12.0>
    Actual: <0.0>

00:00 +2 -10: Some tests failed.
```

**绿 1（本次新增 17 条）** `flutter test test/features/moments/moment_divider_unified_test.dart test/features/contacts/friend_request_review_spacing_test.dart test/features/contacts/friend_acceptance_greeting_ledger_test.dart`

```
00:01 +17: All tests passed!
```

**绿 2（受影响既有套件）** `flutter test test/features/contacts test/ui test/features/moments test/features/matrix/offline_send_state_test.dart --timeout 120s`

```
00:57 +759: All tests passed!
```

**绿 3（全量）** `flutter test --timeout 120s` → **`03:09 +3294: All tests passed!`**（见 §6）

**绿 4（HTML demo 契约）** `cd frontend && npm test`

```
ℹ tests 216
ℹ pass 216
ℹ fail 0
```

**红 5（追加需求：气泡 `waitingNetwork` 文案）** `flutter test test/features/matrix/offline_send_state_test.dart`
完整输出：`docs/verification/artifacts/2026-09-19/red-delivery-wording.txt`

```
00:00 +10: 气泡外观 waitingNetwork 显示小时钟 + 等待网络，绝不显示红色感叹号
Expected: exactly one matching candidate
  Actual: _TextWidgetFinder:<Found 0 widgets with text "等待网络": []>
00:00 +11 -1: Some tests failed.
```

**绿 5（追加需求）** 同一命令 →

```
00:00 +12: All tests passed!
```

## 6. 门禁结果（真实输出）

| 命令 | 结果 |
|---|---|
| `cd apps/mobile_flutter && C:/src/flutter/bin/flutter.bat analyze lib test` | **`No issues found! (ran in 8.5s)`，exit 0**（首次运行曾报并发 message-outbox 测试文件的 `unused_import`，已由其作者修复；本次改动文件始终 0 issue） |
| `flutter test test/features/contacts test/ui test/features/moments test/features/matrix/offline_send_state_test.dart` | `00:57 +759: All tests passed!`，exit 0（完整输出 `docs/verification/artifacts/2026-09-19/focused-green-final.txt`） |
| `flutter test --timeout 120s`（全量） | 见下方「全量」小节 |
| `python scripts/verify_ui_contract.py` | `UI contract drift: PASS (32 components, 372 screens)`，exit 0 |
| `cd frontend && npm test` | `tests 216 / pass 216 / fail 0`（基线 214 → +2 新断言），exit 0 |
| `pwsh -NoProfile -File scripts/verify.ps1` | 见下方说明（既有失败，与本改动无关） |

**全量测试（真实输出，共四次运行）：**
1. `02:14 +3272 -1`（1 失败：`direct_message_open_lifecycle_test.dart` Test 5）；
2. `02:50 +3291 -3`（3 失败：`test/core/outbox/message_send_scheduler_test.dart` 2 条 —— 并发 message-outbox 作者当时正在改该模块 —— + 上述 Test 5）；
3. `02:40 +3293 -1`（1 失败：仅上述 Test 5；`docs/verification/artifacts/2026-09-19/full-suite-final.txt`）；
4. **`03:09 +3294: All tests passed!`，exit 0**（`docs/verification/artifacts/2026-09-19/full-suite-final2.txt`）—— 最终全绿。

第 3 次运行中唯一反复出现的那条经 §10.4 的归因实验确认是负载相关的时序抖动（无本次改动时同样通过、有本次改动时连续 3 次通过），第 4 次在系统空闲时全量运行即全绿。逐次输出见 `docs/verification/artifacts/2026-09-19/full-suite*.txt`。

**`scripts/verify.ps1` 既有失败（非本次引入）：** 脚本在 `tests/mobile` 步骤停止：`tests/mobile/test_android_ci_workflow.py::test_android_ci_workflow_exists_and_pins_flutter` 断言 `assert "run: flutter test" in ci` 失败，原因是 `.github/workflows/android-ci.yml` 在提交 `bd28dbff` 后使用 `run: |` + `flutter test 2>&1 | tee "$log"`，不再含字面量 `run: flutter test`。`git status --porcelain -- .github tests/mobile` 为空（两侧文件均未被本次改动触碰）。与 `docs/verification/2026-09-18-ui-review.md` §4 记录一致，本次不修复。

## 7. 缺陷 3：根因、幂等方案与测试

**根因（file:line）**
- 触发点：`lib/features/contacts/contacts_page.dart:1712 _openAcceptedRequest` → `:1786 _initializeAcceptedRequest` → `:1815 _onFriendAccepted` → `lib/features/friendship/friend_acceptance_coordinator.dart:77 establishWithRequest`；
- 产生点（**不在本次持有范围**）：`lib/app_home.dart` 的 `_establishDirectChatAndGreet`（当前 `:1032-1047`，行号随并发 outbox 改动漂移）**无条件** `await widget.matrix.sendFriendAccepted(...)` → `lib/features/matrix/matrix_e2ee_client.dart:6378 sendFriendAccepted` → `room.sendEvent(..., txid: friendAcceptedTransactionId(...))`；
- 事务 id 已经是确定性一次性键（`matrix_room_timeline_adapter.dart:64` → `friend-accepted-request-<requestId>`），但**没有任何一处判断/持久化“是否已发放”**；Matrix txid 去重只覆盖服务端有限缓存窗口，进程重启/窗口过期/多端登录都会重复投递，所以内存 bool 也不够。

**幂等方案（已实现并测试）**
- `lib/features/contacts/friend_acceptance_greeting_ledger.dart`：`FriendAcceptanceGreetingLedger(preferences, accountKey)`，`claim(key)` 原子认领并落盘、发送失败 `release(key)`、`greeted(key)` 只读；键 = 服务端权威好友申请 id（直接复用 `friendAcceptedTransactionId`，与发送事务 id 逐字一致）；按 Matrix 用户 id 分区；单账号最多 200 条。
- 生产接线：**需由 app_home/matrix 拥有者应用**（本次不得修改 `lib/app_home.dart`）。建议全文与最小 diff 见 `docs/verification/2026-09-19-friend-acceptance-greeting-idempotency.md`（要点：`directChats.open` 与 `_openConversationFromNotification` 仍每次都执行，只有 `sendFriendAccepted` 被账本门控）。

**红 → 绿测试** `test/features/contacts/friend_acceptance_greeting_ledger_test.dart`（红：类不存在；绿：7 条全过）
1. 同一申请只允许认领一次，重复认领 `false`；
2. 发送失败 `release` 后重试仍能补发，且只补发一次；
3. **模拟进程重启**（同一持久化存储上新建账本）后仍判定已发放；
4. 不同账号分区互不误判；
5. 记录有界（205 次认领后只保留最近 200 条）；
6. 幂等键与 `friendAcceptedTransactionId` 在 `null/''/'  '/'r2'` 输入下逐字一致；
7. 编排级：同一好友重复「打开聊天」4 次（含重启后一次）→ 打开 4 次、系统提示与打招呼只发放 1 次。

## 8. 更新过的既有测试（逐条原因）

1. `apps/mobile_flutter/test/features/matrix/offline_send_state_test.dart`
   - `气泡外观 waitingNetwork 显示小时钟 + 等待发送，绝不显示红色感叹号` → 用例名与断言改为「等待网络」：产品要求状态词表统一（`waitingNetwork` = 等待网络；等待发送只属于 `queued`），原断言 `find.text('等待发送') findsOneWidget` 表达的是被本次刻意纠正的旧文案。
   - 同一用例新增 `expect(find.text('等待发送'), findsNothing)`（防止两个词再混用）；`failed` 用例新增 `expect(find.text('等待网络'), findsNothing)`；原有「红色感叹号只属于 failed」「时钟只在 waitingNetwork」「点击立即重试」断言**全部保留未放松**。

**其余既有测试无改动**（未删除、未弱化任何断言）：
- `test/features/contacts/friend_acceptance_retry_test.dart`（断言「打开聊天」会打开会话、重试只重试加密会话初始化）保持原样并通过 —— 缺陷 3 的幂等门控建议落在 `app_home`（未应用前行为不变）。
- `test/ui/moment_reactions_test.dart`、`test/features/moments/moment_visibility_page_test.dart`、`test/ui/wechat_gradient_divider_test.dart` 等均未改动并通过（互动面板与可见范围页的分隔线键名保持不变，仅实现换成共享组件）。
- `test/features/matrix/offline_first_opening_test.dart:201` 的「等待发送」属于 pending conversation 页的 outbox 排队行（`OutboxStatus.queued`），语义正确，未修改。
- 新增测试 4 个文件共 17 + 0 条（3 个新文件 17 条 + `offline_send_state_test.dart` 的更新）；`frontend/tests/gradient-divider.test.mjs` 新增 2 条断言（原 5 条未改动）。

## 9. 功能闭环结论

- 本次只改视觉/布局、共享组件与状态文案，未改任何 gateway、路由或状态机：「通过验证 / 拒绝」仍只经 `POST /friends/requests/{id}/accept|reject`；「打开聊天」仍先 `GET /friends` 校验当前好友关系权威，再建立/取得加密私聊。
- 深浅色：分割线色源一律经 `WeChatColors.resolve(context, WeChatColors.divider)` / `--divider-fade` 主题变量解析，无硬编码浅色值（新增测试在深色下断言 `#2C2C2C`）。
- 状态可见性：`已添加` 徽标（`brandTint` + `brandPrimary` 粗体）与「打开聊天」按钮（品牌填充 + `busy` 加载态 + 禁用态）未改动，仅新增 12dp 间距；窄屏（320dp）、极窄容器（200dp）、2× 字号长文案均断言无溢出且徽标文字换行。
- 状态文案：会话内气泡 `waitingNetwork` →「等待网络」+ 灰色小时钟（点击立即重试），红色感叹号仍只属于 `failed`；pending/outbox 行显示完整五词词表；`UI_DESIGN.md` §7 已登记词表，杜绝各页自造。
- 行高契约：朋友圈动态行、可见范围页行、列表行均保持原行高（线画在行内/由 `background-position` 承担，不额外占高）；`test/ui/wechat_gradient_divider_test.dart` 的 56dp 行高断言继续通过。

## 10. 未完成 / 待确认

1. **缺陷 3 的生产修复尚未应用**（`lib/app_home.dart` 由他人持有）：已交付幂等账本 + 完整修复建议（`docs/verification/2026-09-19-friend-acceptance-greeting-idempotency.md`）；在应用前，重复点击仍会重复发放系统提示（缺陷仍在）。
2. §4.3 列出的 4 处 Flutter 文件（profile / notification settings / transfer / ui/chat）与 `lib/app_home.dart`、`lib/features/matrix/**` 的实心分隔线未修改（不在本次持有范围）。
3. `flutter analyze lib test` 当前 exit 1 来自并发 message-outbox 测试文件的 `unused_import`（`test/core/outbox/persistent_outbox_manager_test.dart:1:8`），非本次改动。
4. **全量测试的 1 条失败经归因确认为既有的时序抖动（flaky），不属于本次改动**：`test/features/matrix/direct_message_open_lifecycle_test.dart` 的 `Test 5: directChats.open 失败不阻塞进入（pending），重试后进入房间` 曾在两次全量运行与一次单独运行中于 `expect(find.byType(RoomPage), findsOneWidget)`（:171）失败。归因实验与结论：
   - 把本次改动的 7 个已跟踪文件临时还原到 HEAD（先备份到 `docs/verification/artifacts/2026-09-19/backup/`）后单独运行该文件 → `+6: All tests passed!`；随后从备份还原本次改动（`git diff --stat` 复核一致），再连续单独运行 3 次 → 3 次全部 `+6: All tests passed!`。
   - 即：该用例在**无本次改动时也会通过、有本次改动时也通过**，失败只出现在负载较高（全量套件 / 并发跑测试）时 → 属时序抖动，与本次改动无因果关系。
   - 该用例所在链路由并发进行的 message-outbox 特性正在改写（`pending_conversation_page.dart` / `app_home.dart` / `room_page.dart` / `room_timeline_controller.dart` 被其修改，`git status` 可见），建议由其拥有者在改动落地后连同负载场景复跑。
   - 本次改动文件清单（见 §1）不含该链路任何一环。
5. `scripts/verify.ps1` 的既有失败（`android-ci.yml` 与 `test_android_ci_workflow.py` 漂移，最后一次相关提交 `bd28dbff`）未修复。
6. HTML demo 深色效果由 token 定义与 Flutter 深色 widget 测试覆盖，未做深色逐像素截屏校验（与 2026-09-18 记录一致）。
