# iOS 0.3.53（58）交付记录

Apple 上传确认：2026-09-07 20:57:31 UTC（香港 2026-09-08 04:57:31）。altool 返回 `UPLOAD SUCCEEDED with no errors`。上传任务 [34161245562](https://github.com/SuperJJ2333/StarChat/actions/runs/34161245562) 成功；此记录不等于 Apple 已完成处理或用户已安装。

IPA：`artifacts/2026-09-08/ios-0353/ChatFlow-0.3.53-build58.ipa`，58,335,593 字节，SHA256 `6428335ad38f7ecc24e25463b43f74bacf7626a65e63f032c1fca3c378c0778c`。最低 iOS/iPadOS 16；团队、Bundle ID及生产 APNs 签名保持不变。

实际 IPA 校验通过：签名、production aps-environment、关闭调试权限、iPad设备族、麦克风/摄像头用途说明、audio/voip/remote-notification后台模式、SQLCipher框架存在及先于系统SQLite链接。构建任务34160519073成功；24项原生核心测试、1333项Flutter测试、最终Analyze和仓库verify通过。网关53项测试通过。

实现：普通APNs消息提醒；真实显式来电经PushKit唤醒并立即上报CallKit；后台通话音频；真实远端视频PiP；room/call和会话owner绑定；冷启动动作保留；挂断使待接听失效；网关故障不能授权媒体；Keychain仅两项受审查迁移。iOS不提供任意跨应用悬浮窗或无限后台常驻；系统静音、专注与用户通知选择仍生效。

生产独立唤醒服务已健康运行，配置和部署源码SHA一致。公网health200、无授权401、原admin入口404。私网admin只读、缺失设备拒绝、APNs密钥加载/签发验证通过。未修改业务服务版本、资金设置或数据库。

## iPad验证步骤（待用户执行）

1. 在TestFlight查看畅聊，待Apple处理后更新至0.3.53（58）；不要卸载或清除数据。更新后先解锁并打开一次，允许通知、麦克风；视频通话允许摄像头。
2. 先确认启动进入登录/聊天页面，没有一直白屏。随后验证前台、后台和锁屏的消息横幅/声音/通知中心。
3. Android呼叫锁屏iPad时，Android测试端必须使用本次配套0.3.53（56），因为既有0.3.53（55）未发送新唤醒请求。测试来电接听、拒绝、主叫取消、超时；后台双向声音；视频PiP；重新启动设备并首次解锁后再测试冷启动。
4. 多设备只允许一个设备接听；退出账号后不应再响铃；拒绝权限、静音/专注场景的结果应符合用户系统设置。

真机阶段进展：用户已通过TestFlight升级，USB查询确认版本0.3.53（58）；用户明确反馈“已更新，能正常进入聊天页面”。启动首屏通过，当前脱敏采集中未发现旧SQLCipher不可用、Unhandled Exception或secure_session_status错误。生产聚合查询确认1台设备完成VoIP与普通APNs路由注册，尚无呼叫记录。锁屏消息/来电、双向声音与PiP等其余场景尚未验收，不能标记为“所有权限与通话均正常”。如出现异常，保留安装和数据继续定位。

后续用户测试：普通消息正常；锁屏来电没有界面或铃声，尚未解决。网关出现3条由注册iPad参与方预留的结束墓碑，投递尝试仍为0。这只能证明没有进入投递，不能排除发起端没有请求、401/网络失败等原因。已启用临时Nginx诊断，仅固定路由标签/状态码/时间，不记录IP、URL参数、令牌、账号或通话标识；结束排障后须关闭并恢复原access_log off。仍待确认发起呼叫设备和其实际构建版本。

配套Android最终包：`artifacts/2026-09-08/ios-0353/android-companion/ChatFlow-0.3.53-build56-arm64-companion.apk`。SHA256 `dd6e2d8efea8b22cb4201739f3bfb98aa0031ad3da54cdb6488530dbf3deae2e`。固定签名、ARM64、16KiB对齐、重建清单/smali/原生资产核验通过；未发布公众更新。

源码：安全发布分支 `codex/ios-0353-testflight`，发布提交f42a4b060ef8e8fc7d810782634ef9048d511bc9；构建源码5d23aa80d390d48c2da5884060af440b91d752fc。本地隔离工作区`artifacts/2026-09-08/ios-0353/source`保存实现与评审提交，未推送包含历史验证产物的原始分支。

第二次复现的状态码证据call-status-repro-01.log：仅end200，没有create请求；投递数0。用户确认Android发起，但Android实际构建号仍待澄清（58可能指iPad，尚不能断定）。临时状态码诊断现已关闭并恢复access_log off，Nginx检查/重载成功。启动采集已结束，仅保留21条已脱敏的Flutter启动诊断；设备随后断开，第二次来电USB日志未采集成功。来电问题尚未解决，不得对外标记全部功能通过。
