# 客服充值提现工作台 UI 验证（2026-09-23）

## 交付范围
基线 1baaf36eaa02f4ee903f09591b694cac1a90695b，隔离工作树 `.worktrees/staff-workbench-ui-20260923`。用户八项 UI 要求，以及后续“所有充值提现处理在弹窗内”追加要求。实际后台共享模块与 HTML demo 同步实现，未部署生产，未执行真实资金或短信操作。

- 结算参考使用信息卡片，刷新采用右上角图标，忙碌状态防止重复点击。
- 筛选更名“待处理请求 / 我正在处理”，选中态和说明可见；mine 在服务端分页前按当前客服筛选。
- 普通客服只保留订单主流程和查询；目录、转让排障及异常登记收进管理员工具。
- 铃铛+侧通知仅新增充值/提现申请计数；首次历史回放静默，已读游标按账号保存，断连可恢复。
- 充值显示权威昵称与畅聊号。处理入口使用主色按钮，备注置于按钮下。
- 充值、提现及管理员待核对充值操作迁入原生弹窗，复用 admin-proof-dialog 和既有控件；保持租约、权限、幂等和审批接口。
- 汇率手工输入/快捷调整草稿保留；未编辑的自动参考不会在汇率失效后伪装为有效基准。
- 提示按订单 ID 归属，避免订单 A 的延迟成功回执写进 B 弹窗。

## 可交互 demo
http://127.0.0.1:8155/admin-workbench-demo.html
管理员视角追加 `?role=admin`。所有订单、金额、身份及通知均为模拟数据，无真实 API 写调用。顶部按钮可模拟新充值、新提现及断网恢复。
浏览器实测：Edge 中点击已核验的充值打开单笔弹窗，显示基准、±1%/±5%、最终点钻预览；关闭后可正常切换提现；提现缺少汇率的校验提示在弹窗内可见。实测发现的关闭窗口残留已通过隐藏缓存容器与关闭移回机制修正。未凭单元测试推断页面可用。

## 自动化证据
目录 `artifacts/2026-09-23/staff-workbench-ui/`：
- `identity/red.log`：4 failed（新增字段/服务端 mine 过滤缺失）；`identity/green.log`：15 passed / exit 0，50.52s。只读投影无迁移或金融写变更。
- `notifications/`：历史基线、空基线、opaque cursor 等红绿记录；10 项专项通过。
- `modal-red.log`：操作弹窗缺失，34 passed / 1 failed。
- `frontend-final.log`：293 passed / 0 failed，exit 0；包含草稿刷新保留、充值/提现跨弹窗延迟回执隔离。
- UI contract：PASS，32 components / 398 screens；OpenAPI --check：PASS。
- 首次 `verify-full.log` 在后端进度36%中止，执行会话不再存在，无最终退出码，不能计为通过。其 Infra144/Getui28/Bot9 阶段通过。
- `verify-retry.log` 为重新启动的完整门禁；最终状态以 `verify-exit.txt` 为准，未出现该文件前不得宣称全门禁通过。

## 复审与发布边界
先完成规格复审，再完成质量/安全复审。复审发现管理员异常登记仍内联、跨订单延迟提示串单，均修正并增加回归。保留当前财务审批及权威核验纪律，关闭弹窗不会伪造释放处理权或成功状态。
本批只交付本地候选与 demo；主工作区其他任务修改未覆盖。完整门禁最终结果及候选合入 main/生产发布须另记录事实，不能沿用历史 PASS。

后续交付状态：本批已完成生产发布及Mi6 2163安装，上文未发布/未安装为实现阶段记录；以[最终发布记录](2026-09-23-staff-direct-release.md)为准。
