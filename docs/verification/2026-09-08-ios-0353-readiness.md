# iOS 0.3.53 同步准备

本轮用户要求同步Android正式0.3.53，并保证消息提醒、后台与锁屏来电和通话小窗。

发布源已定位至 artifacts/2026-09-08/wallet-activation-address/release/source。逐文件核验正式发布source-manifest.json：1102个文件全部SHA256一致，pubspec为0.3.53+55。证据 artifacts/2026-09-08/ios-0353/android-source-integrity.json。根工作区仍0.3.47且含未发布钱包修改，禁止直接全量上传根工作区。

已确认与先前签名分支比较，发布源需要合入Runner签名/entitlement、APNs原生桥、权限和SQLCipher加载顺序修复。发布源已有Dart APNs provider，因此后续需按行为与依赖逐项比较，不能全量覆盖新版app_home。0.3.47（7）启动候选此前成功上传，但没有收到真机首屏验收反馈。

现有代码不存在iOS PushKit/CallKit实现，Info.plist仅声明audio和remote-notification。普通推送包含默认sound，但此配置不构成后台锁屏来电接听链路。现有Getui桥以明文事件类型识别call，加密Matrix事件无法据此可靠判定来电，不能直接复用于VoIP分类。

以上为实施前基线。用户已批准方案，当前实施进展如下。

## 实施与验证进展

- 隔离工作区为 `artifacts/2026-09-08/ios-0353/source`（本机 T: 映射）。新版具备 PushKit/CallKit、后台音频、真实远端视频 PiP、会话 owner 隔离、严格 room/call 匹配与两项受限 Keychain 迁移。
- 最新 Flutter 全量 1333 项通过，`flutter-analyze-final.log` 无问题；`scripts/verify.ps1` 完整通过，使用隔离工作区 `.env.example` 渲染测试配置，未读取生产凭据。
- 网关 53 项测试通过。领域、客户端质量、网关质量审核分别有独立记录。部署修复了 Compose 流式列表逗号拆分 tmpfs 的问题，并增加 YAML 回归测试。
- 独立生产容器 `starchat-ios-calls-ios-call-gateway-1` 已健康运行，Nginx 增加 `/ios-call/` 路由并通过配置检查。公网健康 200、无授权状态查询 401、原管理接口仍 404。容器内公共 Matrix 身份读取、私网 admin 读取、无效设备拒绝和 APNs 密钥/JWT 加载均通过。没有向真实设备发送测试推送。
- Android 配套测试包为 0.3.53（56），SHA256 `dd6e2d8efea8b22cb4201739f3bfb98aa0031ad3da54cdb6488530dbf3deae2e`。固定签名、ARM64、16KiB 对齐、清单/代码/资产一致性通过；未发布正式更新。
- iOS 候选源码分支 `codex/ios-0353-testflight`，当前构建提交 `5d23aa80d390d48c2da5884060af440b91d752fc`。Mac 原生核心测试与签名身份检查通过，IPA归档、导出及实际签名内容检查已通过，版本0.3.53（58），SHA256为6428335ad38f7ecc24e25463b43f74bacf7626a65e63f032c1fca3c378c0778c。此前两次云检查因发布清单遗漏新增文件失败，已修复清单并增加必要文件断言；不是已交付 IPA 的崩溃证据。
- 尚未完成：Apple处理与iPad 升级首屏和前后台/锁屏/冷启动通话、普通通知和 PiP 真机验证。不得据静态权限或单测宣称全部正常。

候选云构建34160519073成功；独立下载校验见ipa-verification.json。上传专用工作流34161245562基于发布提交f42a4b060ef8e8fc7d810782634ef9048d511bc9，只上传上述已核验的IPA，未重新编译。隔离源码本地提交7d5d5cfe，工作区clean；未推送含历史验证产物的本地分支。

最终交付：TestFlight上传任务34161245562成功，Apple返回UPLOAD SUCCEEDED with no errors。完整交付与真机待验清单见2026-09-08-ios-0353-delivery.md。

真机首屏已通过：USB确认0.3.53（58），用户反馈正常进入聊天页面。已注册1条VoIP及普通APNs路由。其余双设备/锁屏/视频场景等待用户测试，未宣称通过。
