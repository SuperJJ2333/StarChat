# Main 2082 integration and Redmi delivery

## Baseline and scope

User explicitly requested consolidation onto main, a commit, only main branches, and a Redmi debug delivery based on the current changes plus 0.3.77-debug / 2081. Integration baseline is main `57aa2f83` (2081), not the older root checkout (2049). All three other named branch tips were verified ancestors of that baseline. Their uncommitted increments were compared with the previous integration snapshot; newer main implementations were preserved.

The integration retains the server-coordinated pair claim/publish and persisted creation intent, account-scoped Matrix capability leases, current moments privacy/cache/reaction behavior, and the four-action top-anchored menu. It adds authoritative member rechecks, explicit peer identity on every room-opening route, in-place recovery for unregistered single-member legacy rooms, and fail-closed behavior that never creates a replacement after lookup/open/repair uncertainty. Existing encrypted history and duplicate historical rooms are not deleted or merged by this change.

Moments retain account-specific caching and add scroll-triggered cursor paging, bounded request state, refresh/page race protection, retry controls, and account-switch resets. The shared top menu is narrowed to 176 and action content is centered. Pending approved admin readability, reserve resampling, runbook and HTML-only UI contract increments were merged while retaining main's wallet guards and read-only secret mounts. No backend deployment or real financial operation was performed in this task.

## Verification

- Legacy repair regression: both first-claim and resumed-claim tests failed before the fix because the opener was never called; after the fix both retry the same room, with zero create calls. Focused room/moments regressions: 41 passed.
- Flutter full suite: 1839 passed. Subsequent account-state/version tests: 37 passed. Flutter analyze: no issues.
- Frontend: 135 passed.
- Repository `scripts/verify.ps1`: PASS; infra 131, Getui 28, Matrix bot 9, business API/worker 1647 passed / 39 skipped, mobile boundaries 67 passed. Existing Pydantic deprecation warnings remain in unchanged compatibility code.
- UI contract: 20 components / 331 screens; OpenAPI drift, migration chain, Python parsing, imports and compose render passed.
- Independent specification review followed by quality/security review found two integration gaps, both fixed and re-reviewed successfully.
- Changed-source scan: no conflict markers, private key blocks or full TRON addresses; runtime secrets and generated build material excluded from staging.

Logs and merge inventory: `docs/verification/artifacts/2026-09-10/main-2082/` in the integration worktree. The root original source archive contains 575 entries (133 tracked modifications and 442 untracked source files), with SHA256 manifest at the same artifact path under `root-before-main/`. Old iOS uncommitted work is already represented or superseded on main and remains in its preserved detached worktree.

## APK and device status

Version increments to `0.3.78-debug / 2082` (`pubspec` and fallback app config 0.3.78 / 2082). ARM64 standard debug uses the parallel `com.liuhetong.mobile.debug` package and the same three HTTPS production endpoint defines as 2081. The deliverable must complete conventional apktool 2.12.1 decode/rebuild, build-tools 36.0.0 alignment, fixed-identity signing, manifest/DEX/asset comparison, and final debug identity checks.

Due to D: running out of space, generated APK work is preserved under `C:/Users/Administrator/.codex/docs/verification/artifacts/2026-09-10/main-2082/`; source remains in the main integration worktree. Earlier generated build material was moved rather than deleting source or backups.

Device `cbd0156b` (Redmi Note 7) unexpectedly has debug 0.3.47 / 2049 installed, signed with SHA256 `34999c8b561affc263f11df0a3865e8c03c0386997a8c37bd12110380e5bc1f1`. Required fixed signer is `75b31c66476cd8e2c9319551b49405a1de1e5c23e9a0dbdcc9eb76b52ba61fff`. Android cannot upgrade that installation across these certificates. No uninstall, app-data clearing, or silent return to the obsolete signer is authorized. APK transfer can proceed; installation and real-account friend-message acceptance remain pending resolution of this device conflict.

Remote fetch was attempted with both configured Schannel and OpenSSL transport and failed at TLS. Cached origin/main is the verified 2081 baseline; no force push or remote branch deletion was performed.
