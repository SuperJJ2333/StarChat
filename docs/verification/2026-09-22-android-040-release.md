# Android v0.4.0（2157）与刷新恢复服务器发布

## 实际交付

2026-09-22 01:18+08 已发布Android **0.4.0 / 2157**，ARM64 standard release，包名com.liuhetong.mobile。
[下载最终APK](https://www.liuhetong888.com/downloads/ChatFlow-0.4.0-build2157-arm64.apk)。大小80,522,270字节；SHA256 `4d488ce6985c998053712bd0729a004a51ddf38b8bdb8480c444b85de2ec8776`；证书SHA256 `75b31c66476cd8e2c9319551b49405a1de1e5c23e9a0dbdcc9eb76b52ba61fff`。构建阶段apksigner/zipalign通过，发布阶段未重复下载验包。

服务器镜像`starchat-business-api:refresh-040-20260922`，digest `sha256:b7d38f99879d6dff1375e527e20552e0f04c1d328c4e893330d91ba53b9ad6cc`已运行且healthy；真实数据库head `0080_refresh_recovery`，新增两可空字段类型核对通过。API只覆盖6个源码文件及1个迁移，其他源码与当时生产一致。生产已有会话未清空，JWT及运行环境、挂载、日志轮转不变。其他容器ID未改变。

## 合并与验收

候选源码`28bdfc48`，分支`codex/android-040-release-20260922`。基于刷新修复28287811整合main878dcc37（2156），再合并2755b4a2启动会话保护；合并提交bc4de565、4eaf6161。使用已解析并通过全量的依赖锁，record_linux由2.1.1解析到2.1.2，Android生产原生插件不变；仅规范化镜像URL。

| 用户要求 | 合入/验证 |
| --- | --- |
| 哥们测试群无法进入、快速进出死窗口 | main的onRoomReady解锁和15秒协调器清理保留；导航专项随最终全量通过 |
| 点击消息行互动效果 | 全屏进房遮罩移除保留，行自身按压反馈未覆盖 |
| 每次进入转圈 | 本地缓存冷加载渲染保留 |
| 红包领取提示 | 发送者/领取者可见性逻辑保留 |
| 滑动历史红框 | deactivate取消轮询和元素访问保护保留；历史锚点/搜索预算修复包含 |
| BUG-28/35/40及批次D | 9a77f825及后续2156祖先提交包含，尺寸/视频进度/连播保留 |
| 刷新恢复与错误提示 | 随机操作持久化、原结果恢复、真实重放拒绝、具体退出原因及有界诊断包含 |
| 仅Android | iOS全部设置及download.html/admin-home.js/manifest哈希不变；未构建或发布iOS |

主目录另有未提交金融/手机号/群迁移0072–0080工作，不覆盖或混入本次生产。已完成提交已整合到本发布分支；main脏工作区保留，未强行快进导致并行迁移多头。后续main整合必须由该任务处理未完成迁移图，禁止把整个脏树覆盖本次镜像。

## 验证证据

[证据目录](artifacts/2026-09-22/android-040-release/)，所有日志保留真实失败与重跑结果。

- Flutter3.44.9/Dart3.12.2 Windows：最终3803 passed、0 failed、exit0（4分11秒）；analyze No issues found、exit0（24.3秒）。
- 影响门禁exit0：mobile108 passed/1条件skip；UI32组件/375页面、AST241文件、迁移、OpenAPI、Compose通过。未更改的后端源码/测试/契约完全等同28287811，复用上一轮完整verify及修正后合并证据2301通过/48条件跳过、真实PG8通过，不重复约25分钟后端测试；不声称本轮重新运行整套verify。
- Linux实际候选镜像：生产数据库备份恢复到隔离PostgreSQL16.9；迁移前后User/Device/Family/RefreshToken既有字段与行指纹完全相同；旧镜像仍可读。相同操作4并发仅一个子结果、结果已推进不撤族、真实重放撤销、旧协议请求兼容均通过。使用隔离合成账号，无生产测试登录。
- 独立领域审查通过后质量安全审查通过；后者提出实际schema门禁、匿名数据卷清理和发布后回退边界，全部补齐并复核通过。
- Android源码编译163.1秒一次成功；Apktool2.12.1重建，36.0.0工具16K对齐及固定签名；25,346类、338原生库/资产逐项比较一致，清单语义一致，无debuggable/调试kernel，ARM64 only。
- 上传SHA256核对后不可变文件落位；PUBLISH_PASS、METADATA_CHECK_PASS；工作站经既有跳板HTTPS HEAD200、大小80,522,270、no-store。服务器HTTPS ready200、两端未授权401、新旧刷新无效凭证401（新operation请求不再422）、诊断未授权401。真实路由内部只读投影Android0.4.0/2157，iOS仍0.3.102/2144；未伪造HTTP认证会话。
- 最低版本仍3，iOS设置及更新说明不变；Android仅版本/build/URL通过SettingService审计更新，原更新说明按现行发布流程保留。latest-arm64别名已切换，旧2144包保留。
- 01:19+08最终日志检查：新错误0，服务healthy，运行配置/挂载/日志轮转一致。

## 失败与修正

仅发布辅助脚本：初次容器简称错误改为实际容器名；隔离烟测异常变量遮蔽engine导致清理NameError已修；Postgres初始Unix socket早于数据库创建，改为TCP就绪并重做恢复；公网健康路径由不存在的/health改为实际/api/v1/health/ready；最终环境比较从有序列表改为键值比较（值未变化）。全部重跑通过。首次本地依赖锁检查识别record_linux增量，保留已经测试的解析并冻结，未把差异隐藏为纯URL变动。无因这些验证脚本失败改变生产业务行为。

## 保留、回退与真机边界

服务器备份、原私有配置、数据库dump、发布前设置/别名与审计保留于宿主0700 `/opt/starchat/releases/android-040-20260922/`；敏感配置与DB未下载到工作站。新App发布后不能回退到不接受operation_id的旧API；必须保留本次冻结镜像/配置或前向修复。若撤销分发，只按前态回退Android分发设置，服务器协议保留，不强制已安装用户降级。

隔离演练容器及匿名数据卷、内部网络已删除，删除后验证无保留卷。工作站临时T映射及18945隧道在命令结束后关闭。

没有对用户设备卸载/清数据或自动安装。实际“哥们测试群”进入、连续上滑、弱网与后台返回仍需升级后真机复验；自动化和公网发布通过不冒称用户验收通过。

## 计时

最早可确认工具时间01:00:22+08；01:18+08发布、01:19+08终验，完整最初启动精确时间未知。已知测试4分11秒、analyze24.3秒、源码构建163.1秒；上传与准备/验证并行，不能把时长简单相加。其余按命令日志保留，不编造精确主动/等待时间。
