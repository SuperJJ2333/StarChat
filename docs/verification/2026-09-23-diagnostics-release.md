# 帧预算诊断服务端生产发布（2026-09-23）

用户明确要求发布已完成四项优化的服务器改动，并暂缓安全、账号恢复与容灾提案。**本次服务端接收接口已上线；客户端未构建或发布。**

## 范围与身份

线上客服版本为基底：API e386b73d3a34351363b5248969331eba723b703dd0eac37265ee469198a1281a，代码a70e5191，工作树基线1baaf36e。只覆盖`services/business-api/app/api/client_diagnostics.py`，不包含另一个未发布红包查询改动。335个API源码、迁移、锁及配置文件哈希证明只有这一项变化，依赖未升级。

最终镜像：`sha256:e2577705bc278071d2a8cb6337de2d36a3f6d6eea3bcb3de48dcb17f5681cb57`。文件原始SHA `abe20cbc619a9d42992408ca5f23986cb6d2089f6caa743776ee4b88602d4f3a`。基于正在运行镜像单文件叠加，Docker构建禁网且上下文白名单仅包含Dockerfile与该文件；未从主目录全量重建。

## 验收

| 项目 | 证据与结果 |
|---|---|
| 新旧诊断契约/限流/隐私/非法帧统计 | 44 passed，1.18秒；既有依赖弃用提示1项，exit0 |
| OpenAPI | 隔离候选重新生成并check通过；实际运行接口自动生成新schema |
| 未变业务基线 | 复用客服版本完整verify exit0、后端2684/67条件跳过；本轮单文件增量由44项与镜像证明覆盖，不宣称又跑一次全业务 |
| 源码身份 | 基底与候选335文件及实际导入路径通过；合法帧汇总可解析，bool/越界/额外身份字段拒绝 |
| 回退保护 | 5项测试通过：容器不存在可恢复、未知后续镜像拒绝、Docker故障不冒充容器不存在 |
| Compose | 候选/回退环境变量、命令、挂载、网络、端口、健康检查往返解析一致，和运行环境匹配 |
| 数据库恢复 | 22,269,441字节备份留远端；隔离恢复136表217441行，upgrade head前后原数据哈希一致，schema0087不变 |
| 切换 | 18:58:29+08开始，仅重建business-api；最终healthy，restart0，无新增Traceback/maintenance_failed |
| 其他服务 | 23个原有其他容器ID与启动时间保持，包含worker/聊天/数据库；临时恢复容器已停止 |
| 双侧HTTPS | 服务器与工作站经jumper：health/ready JSON200、admin/overview401、client-diagnostics未登录401、staff-login空请求422；证书正常验证 |

无需伪造生产用户会话：有效帧接收逻辑由定向测试及实际候选镜像离线证明覆盖，生产只检查健康/未授权行为，不声称真实用户帧数据已到达。

## 客户端与暂缓项目

图片缩略解码、SDK上传阶段复用、文本Outbox持久重试及帧统计采集端，仍只存在于本地已验证源码。当前安装的旧APK不会因服务器更新自动获得这些行为；需要后续单独构建/分发客户端。本次没有APK、iOS、官网分发变更。用户明确暂缓的安全、账号恢复、容灾，以及AWS/S3/TURN基础设施均未实施。

## 回退与计时

远端`/opt/starchat/releases/diagnostics-20260923`为0700目录，保留旧镜像、私有Compose、数据库备份及恢复证据。回退命令：`python3 /opt/starchat/releases/diagnostics-20260923/release.py rollback`，检测到后续不同镜像时拒绝覆盖；只恢复API镜像，不回滚/覆盖生产账本或schema。

初始源校验失败原因：从/release执行脚本时Python优先加载site-packages；已显式使用与生产相同的/opt/business-api源码路径，随后导入路径与335文件证明通过。初次失败日志保留；生产当时未切换。独立复审指出容器缺失时自动回退遗漏和Compose插值风险，已修复/验证后再切换。

精确任务起点未知；18:33建立工作树、18:34定向验证；中间有用户继续消息导致工具中断与恢复，不虚算精确主动工时；18:58:29切换，19:00最终观察通过。临时SSH SOCKS19046已关闭；无剩余本任务运行进程。

[证据目录](artifacts/2026-09-23/diagnostics-release/)；`evidence-identity.json`记录输入/日志SHA，远端5份发布输入与本地完全一致。旧payload.tar之后修正两份脚本，最终身份以该记录及final-observation.log为准。
