# Android 0.3.95 / 2132 更新弹窗发布记录（**已上线**）

- 日期：2026-09-17（Asia/Hong_Kong）
- 用户指令：推送 Android 版更新弹窗；确认候选 = 当前 HEAD `82b24ba7`、版本 = `0.3.95 + 2132`、
  **不强制更新**（`min_supported_build` 沿用线上值）、发布后一并 push GitHub。
- 任务目录：`docs/verification/artifacts/2026-09-17/release-2132/`

## 0. 最终状态

**APK 已按固定流程构建并通过全部本地门禁；GitHub 已推送；更新弹窗已由并发工作流在
SSH 恢复后发布完成。** 线上 Android 正式版 **0.3.95 / 2132**（可关闭、不强制），
旧包 2129 原位保留作为回退。

本节记录**构建与门禁**（本工作流）；阻断、恢复与发布执行见
[0.3.95/2132 更新弹窗发布记录](2026-09-17-android-0395-2132-publish-attempt.md)（第 9–11 节）。
本工作流在 SSH 阻断期间**未执行任何服务端写入**（无分块上传、无安装、无符号链接切换、无设置/审计），
因此该阻断不产生半完成态。

## 1. 候选源码（已冻结）

| 项 | 值 |
| --- | --- |
| 候选 commit | `9fa2c96347adf92bcfa07398cb2212753b9dc0e3` |
| 父提交（本轮修复轮） | `82b24ba7`（flash/call 收口） |
| 版本递增 | `pubspec.yaml` 与 `lib/core/app_config.dart`：`0.3.94+2129` → **`0.3.95+2132`** |
| 工作树 | 构建前后 `git status --short` 均为 **0 项**（构建未污染候选源码） |
| GitHub | `b8a7040c..9fa2c963  main -> main`；`origin/main...main` = `0 0` |

候选包含：闪照 route-exit fail-open 修复 + tombstone 生命周期接线 + 真实外设音频路由 +
TURN 措辞纠正 + 搜索索引命名/回填边界；以及 `ad12f92c` 红包弹窗恢复（2131 debug 已在 Mi 6 验收）。

## 2. 门禁（真实执行）

| 门禁 | 命令 | 结果 |
| --- | --- | --- |
| 静态分析 | `flutter analyze` | **No issues found!**（110.3s） |
| 全量测试（冻结候选） | `flutter test --timeout 120s` | **`+3019: All tests passed!`** |
| 版本契约 | `pytest tests/mobile/test_app_build_contract.py -q` | **2 passed**（pubspec 与 app_config 一致） |
| 源包发行门禁 | `python scripts/verify_android_release.py source.apk` | `abis=["arm64-v8a"]`，`aae87ff2…6b1356`，79,453,964 字节 |
| 最终包发行门禁 | `python scripts/verify_android_release.py final.apk` | `abis=["arm64-v8a"]`，`35ca0962…374633`，79,801,374 字节 |
| aapt 身份 | `aapt dump badging final.apk` | `com.liuhetong.mobile` versionCode **2132** / versionName **0.3.95** / native-code `arm64-v8a` |
| apksigner | `apksigner verify --verbose --print-certs` | v2 ✓ v3 ✓；`Signer #1 certificate SHA-256: 75b31c66…ba61fff`（固定身份） |
| zipalign | `zipalign -c -P 16 4 final.apk` | 通过（exit 0，签名后复查） |
| 重建语义核对 | `verify_rebuild.py release-2132/android` | 类数 **25346/25346**；原生资产 **338 项零变化**；`manifest_semantics_identical: true`；`changed_smali_classes: []`；`manifest.diff` 0 字节 |
| 构建流程 | Apktool **2.12.1** 解包/重建 → zipalign **36.0.0** `-P 16 -f 4` → 固定签名 → 重解包核对 | 见 `android/*.log` |

**最终交付 SHA256：`35CA0962E9DDB3655474B2BCC5182DFCEB52D57549020C8680FC2E4BE3374633`（79,801,374 字节）**
（`android/final.apk` 与 `android/ChatFlow-0.3.95-build2132-arm64.apk` 同文件）

## 3. 阻断：生产服务器 SSH 不可达（非本地问题）

| 探测 | 结果 |
| --- | --- |
| `scripts/starchat-server.ps1 -Action Probe`（跳板） | exit 255，`Connection closed by 8.163.93.151 port 22` |
| `ssh -v jumper true` | TCP `Connection established` → **`Connection timed out during banner exchange`** |
| 原始 socket 读 banner（8.163.93.151:22） | **`banner_bytes=0`**（服务端一个字节都不回） |
| 直连 `liuhetong-prod`（207.56.8.8:23421，绕过跳板） | banner 读取超时（连接方无响应） |
| TCP 端口可达性 | 两个端口 `Test-NetConnection` 均为 **True**（端口开放，sshd 不应答协议） |
| 本机互联网 | `https://www.liuhetong888.com/api/v1/health/live` → **200**（健康检查可用） |
| 遗留进程 | 无 `ssh`/`scp` 残留；重试 3 次（含间隔）均同一症状 |

结论：**端口通、sshd 不回 banner**，属跳板机/目标机 sshd 挂起或上游中间设备拦截，
不是本机网络、代理或本次构建产物问题。按交付工作流，未获新的可达路径前
**不执行任何服务端写入**（避免半完成态）。

需要在服务器侧恢复 SSH（重启 sshd / 检查跳板机负载与防火墙）后，从第 4 节继续。

## 4. 待执行步骤（脚本已就绪，**均未运行**）

1. 分块上传（16MiB，逐块远端大小核对）：
   `pwsh -NoProfile -File docs/verification/artifacts/2026-09-17/release-2132/upload-apk.ps1`
   目标目录 `/opt/starchat/releases/android-0395-2132-20260917/apk-chunks`。
2. 服务端合并 + SHA 门 + 不可变安装 + 符号链接原子切换：
   `bash publish-apk.sh`（`BASELINE=35CA0962…374633`，`PREV=ChatFlow-0.3.94-build2129-arm64.apk` 保留回退）。
3. 更新弹窗设置发布（先 `inspect` 再 `apply`，容器内执行）：
   `docs/verification/artifacts/2026-09-17/release-2132/publish_settings_2132.py`
   - trace `android-release-0.3.95-2132-20260917`，actor `ops-codex-android-release`
   - 五键：`app_latest_version=0.3.95`、`app_latest_build=2132`、
     `app_min_supported_build` **沿用线上现值（不强制更新）**、
     `notes`（v0.3.95 三条）、`app_apk_url=https://www.liuhetong888.com/downloads/ChatFlow-0.3.95-build2132-arm64.apk`
   - apply 断言：`app_ios_*` 零改动、`app_min_supported_build` 与 before 相同、该 trace 恰好 **5 条**审计、
     `platform=android` 投影回显且 `latest_build=2132`
4. 发布后验证：带真实会话 token 的 `GET /app-updates/latest?platform=android` → `0.3.95/2132`；
   未授权 401；公网整包下载 SHA256 == `35CA0962…374633`；服务器与工作站双侧 200/206 + MIME。
5. 回退路径：`publish_settings_2132.py rollback`（写 `<trace>-rollback` 审计）
   + `ln -sfn ChatFlow-0.3.94-build2129-arm64.apk latest-arm64.apk`。

## 5. 未验证 / 剩余风险

- **CI 并发测试失败已修复（本工作流后续提交 `0a9e46a3`）**：`flutter test` 并发跑测试文件时，
  `MatrixTestPaths` 返回仓库内**同一个固定绝对路径**，被 `matrix_client_factory_test.dart` 与
  `conversation_optimistic_state_test.dart` 共用 → 两个进程同时写同一 SQLCipher 库 →
  `INSERT OR REPLACE INTO box_client (k, v)` 撞车（CI「synthetic credential write failure」）。
  已改为**进程私有临时目录**（`systemTemp` + `pid`），并加两条回归测试 + 变异探针。
  本地 `flutter analyze` 无问题、定向 80 通过、全量 **3021 通过**。
  **但本仓库为私有仓库，本工作流无法读取 CI 结论（GitHub API 需鉴权 → 403），
  因此「CI 转绿」尚未由 CI 自身证据确认**——需在下一次 CI 运行后核对。
- **真机未验收**：2132 正式包未安装到任何设备（按约定真机由用户执行）。
- **iOS**：本轮未构建（无 Xcode），`app_ios_*` 也明确未改动（仍为 0.3.92/2120）。
- 公网整包 SHA 比对已由发布工作流在**服务器侧与工作站侧双侧**完成并一致
  （`35CA0962…374633`，79,801,374 字节）；本工作流独立 HEAD 校验因本机 `www` 侧 TLS 抖动未复现，
  以发布工作流的双侧记录为准。

