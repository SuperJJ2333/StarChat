# ChatFlow BUG-01～BUG-10 修复报告（源码级）

- 日期：2026-09-18；缺陷清单：《畅聊缺陷清单-0917.xlsx》
- 任务记录：[2026-09-18-chatflow-bug-01-10](../workflow/tasks/2026-09-18-chatflow-bug-01-10.md)
- 起始 HEAD：`61b29917`（分支 `main`）；客户端 `apps/mobile_flutter`

## 1. 逐 BUG 结论

| BUG | 状态 | 修改文件 | Class / Method | 根因 | 修改内容 |
| --- | --- | --- | --- | --- | --- |
| 01 错误邀请码重复提示 | 已修复 | `features/auth/registration_page.dart`、`registration_controller.dart` | `_RegistrationPageState._inviteStatusRow/_scheduleInvitationCheck`、`RegistrationController.clearInvitationCodeError` | 邀请码提示有两个出口：页面防抖校验的内联状态行 + 提交时 `fieldErrors['invitation_code']` 的字段错误行 | 状态行在 controller 已持有该字段错误时让位；用户重新编辑时清除旧字段错误。一次失败 = 一次反馈 |
| 02 用户协议/隐私政策打不开 | 已修复 | `features/auth/legal_documents.dart`（新）、`legal_document_page.dart`（新）、`login_page.dart` | `openLegalDocument`、`LegalDocumentPage`、`LoginPage.build` | `onUserAgreement/onPrivacyPolicy` 是可选回调，生产调用方从未传入；仓库/服务端也没有可打开的协议页面 | 内置正文 + 原生渲染页；登录页在未注入回调时默认打开内置正文（无外部 URL、无 WebView、双端一致、离线可读） |
| 03 右侧字母索引位置错误 | 已修复 | `ui/components/wechat_contact_index.dart`、`features/contacts/contacts_page.dart`、`ui/foundation/wechat_tokens.dart` | `WeChatContactIndex.build`、`_ContactsPageState.build` | 字母用 `Expanded` 撑满整列（与设计契约 20×18/居中不符）；跳转偏移用魔法数字 | 固定 18pt 字母 + 垂直居中 + 上下呼吸间距；偏移量改由 `contactTileHeight` 与分组标题高度计算，入口行高度固定，保证 A-Z 精准滚动 |
| 04 邀请码复制链接 | 废弃 | — | — | 清单标注废弃（入口已删除） | 未做任何修改 |
| 05 保存无成功提示 | 已修复 | `features/profile/profile_controller.dart`、`profile_page.dart`、`ui/components/wechat_toast.dart` | `ProfileController.save`、新增 `ProfileSaveSuccess/ProfileSaveFailure`、`_ProfileDetailsPageState._onSaveEvent`、`showWeChatToast` | 保存成功只改状态、没有结果信号 | 新增结果事件流；UI 监听后一次性浮层提示（成功「保存成功」/失败「资料保存失败，请重试」）；失败文案不再同时写 `state.message`（避免重复） |
| 06 全部账单展示优化 | 已优化（仅展示层） | `features/ledger/ledger_pages.dart`、`ledger_controller.dart` | `LedgerListPage._filters/_dateButton/_body/_emptyState/_listEntries`、`LedgerController.clearFilters/hasFilters` | 日期胶囊显示 `yyyy-MM-dd HH:mm:ss`；空状态不分场景；无筛选摘要/重置；无日期分组 | 日期只到日；按自然日分组；两种空状态 + 「清除筛选」；筛选摘要 + 「重置」；未改转账/钱包/数据模型与查询语义 |
| 07 勿扰模式出现 PRDS30 | 已修复 | `features/settings/notification/notification_settings_page.dart` | `_NotificationSettingsPageState.build` / 新增分区标题常量 | 分区标题写死内部编号「PRD §30」，用户可见 | 标题改为纯用户文案（「勿扰模式」等），并抽取常量供回归断言 |
| 08 减少动态效果无效 | 已修复 | `ui/motion/motion_preferences.dart`（新）、`ui/motion/motion_page_route.dart`（新）、`app_home.dart`、`main.dart` + 42 个路由文件 | `MotionPreferences`、`MotionPageRoute`、`_MotionSettingsTile`、`_LiuhetongAppState.build` | 设置行是不可点击的占位；无持久化、无任何组件读取；`CupertinoPageRoute` 转场时长是固定 500ms 常量 | 真实开关 + 本地持久化；根级投影 `MediaQuery.disableAnimations`（动效组件/图片帧）；页面转场改走 `MotionPageRoute`（开启时压到 5%≈25ms，与框架处理系统开关一致） |
| 09 新增好友标签无法展示 | 已修复 | `features/contacts/request_friend_page.dart`、`contact_models.dart` | `_RequestFriendPageState._createTag`、`AddFriendGateway.createContactTag` | 新建标签只写本地列表，**从未调用 `POST /contact-tags`** ⇒ 通讯录「标签」页永远看不到 | 先落库再刷新并选中；失败给出可读错误且不假装成功 |
| 10 黑名单设置无效 | 已修复 | `core/permissions/blocked_contacts.dart`（新）、`features/contacts/contacts_page.dart`、`contact_models.dart`、`core/business_api_client.dart`、`core/session_bootstrap_controller.dart`、`app_home.dart`、`features/matrix/room_page.dart` | `_ContactMorePageState._loadBlockState/_setBlocked`、`BlockedContacts`、`RoomPage.canSendNow` | 开关初值恒为 false、从不读 `GET /blocks`；只支持「开」不能「关」；确认按钮文案是「删除」；聊天发送门 `isBlocked` 写死 false | 从服务端读回真实状态（未读到前禁用，不用默认值冒充）；双向可操作（DELETE `/blocks/{id}`）；成功后立即更新进程内投影；启动同步一次；发送门读投影 ⇒ 立即生效且重开 App 仍生效 |

## 2. 门禁与证据

| 门禁 | 命令 | 结果 |
| --- | --- | --- |
| 静态分析 | `flutter analyze` | **No issues found!**（0 issue，退出码 0） |
| 全量客户端用例 | `flutter test --timeout 120s` | **3143 通过 / 0 失败（退出码 0）**；结论摘录 `artifacts/2026-09-18/flutter-full-3143.txt` |
| 新增回归用例 | 5 个文件 23 条 | `auth_bug_0917_test.dart`(3)、`profile_save_feedback_test.dart`(4)、`contacts_bug_0917_test.dart`(5)、`settings_bug_0917_test.dart`(6)、`ledger_presentation_test.dart`(5) |
| 既有用例适配 | `request_friend_page_test.dart` | 「new tags can be created inline and selected」原断言只覆盖本地 chip（等于把 BUG-09 的缺陷行为固化）；已补断言服务端创建 |
| 共享契约用例 | `tests/mobile/**` | **70 通过**（15 个文件逐一运行；`test_getui_privacy.py` 单独 7 条通过但耗时 7 分 36 秒，目录级运行在本机超时，非失败） |
| 构建（固定流程） | `build-debug-2133.ps1 -Mode BuildVerify` | 源码 ARM64 debug → Apktool 2.12.1 重建 → zipalign 36.0.0 `-P 16 -f 4` → 固定身份签名；`rebuild-verification.json`：类 **27317/27317**、原生/Flutter 资产 **336 项零变化**、清单语义一致、zipalign 通过、v2+v3 签名 |
| 真机安装 | `-Mode Install` | `versionCode=2133` / `versionName=0.3.95-debug`；**`firstInstallTime` 未变（2026-09-11 00:42:05）⇒ 覆盖安装保留数据** |
| 真机回读 | `-Mode Pull` | 设备 `base.apk` SHA256 == 交付包 SHA256；证书 == 固定身份 `75b31c66…ba61fff` |

### 交付包

| 项 | 值 |
| --- | --- |
| 源码包（重建前） | `ChatFlow-0.3.95-debug-2133-arm64-source.apk`，SHA256 `48525EC8ECE6ED11C73FCA0382674BB8290631660C9A2A0BBB0AD67E53F06F8C` |
| **最终交付包** | `ChatFlow-0.3.95-debug-2133-arm64-rebuilt.apk`，SHA256 **`CE52DEC1C9CDCB2EC4C47B1B76A3BDBB606756F2534FD985F93F2929E52AC296`** |
| 包名 / 版本 | `com.liuhetong.mobile` / `0.3.95-debug`（2133，`application-debuggable`，`arm64-v8a`） |
| 证书 | `75b31c66476cd8e2c9319551b49405a1de1e5c23e9a0dbdcc9eb76b52ba61fff`（RSA 3072，用户已验证身份） |
| 设备 | MI 6（`cbd0156b`），覆盖安装 2131 → 2133，数据保留 |
| 证据目录 | `docs/verification/artifacts/2026-09-18/android-0.3.95-debug-2133/`（`build-verify.log`、`install.log`、`pull.log`、`rebuild-verification.json`、`source-*-sha256*.json`、`source-worktree-status.txt`） |

## 3. 人工测试步骤（Mi 6，`0.3.95-debug/2133`）

1. **BUG-01** 注册页输入错误邀请码：应只出现一条提示；提交后仍只有一条；重新编辑邀请码后提示随新校验更新。
2. **BUG-02** 登录页点《用户协议》《隐私政策》：均应打开内置正文页并可滚动返回（飞行模式同样可打开）。
3. **BUG-03** 通讯录：右侧索引应为紧凑居中字母列，不压住导航栏/底栏；点 `A`/`M`/`Z` 应准确定位到对应分组标题。
4. **BUG-04** 无需验证（入口已废弃）。
5. **BUG-05** 我 → 个人信息 → 修改昵称/签名 → 保存：出现「保存成功」浮层；断网保存出现「资料保存失败，请重试」。
6. **BUG-06** 钱包/账单 → 全部账单：日期胶囊只显示到日；选择类型/日期/关键词后显示筛选摘要与「重置」；无结果显示「没有符合条件的账单」+「清除筛选」；有数据时按日期分组。
7. **BUG-07** 设置 → 消息通知 → 勿扰模式分区：标题不再出现「PRD」等内部编号。
8. **BUG-08** 设置 → 减少动态效果 → 打开：页面转场/菜单/点赞/按钮动效明显减少；杀掉进程重开仍为开启。
9. **BUG-09** 添加朋友 → 申请页新建标签 → 发送申请 → 返回通讯录 →「标签」：新标签应出现在标签列表（可进入查看成员）。
10. **BUG-10** 好友资料 → 好友设置 → 黑名单开关：打开并确认后开关保持开启；进入与该好友的聊天无法发送；杀掉进程重开，开关仍为开启；关闭开关可解除。

## 4. 已知问题与剩余风险

1. **BUG-02 文案需法务确认**：协议/政策正文由本次按当前实现（E2EE、业务 API 边界、个推推送、权限、注销）撰写，属产品/法务文本，发版前需确认措辞与生效日期。
2. **BUG-08 一致性靠约定**：`MotionPageRoute` 替换覆盖了现有 42 个文件；以后新增页面若直接写 `CupertinoPageRoute` 会绕过该设置（本次已用 analyze + 全量用例保证当前一致性）。
3. **BUG-10 不涉及群聊**：群聊对端不唯一，`_peerUserId()` 返回 null，拉黑不拦截群消息（群权限另有体系）。
4. **`tests/mobile` 目录级运行超时**：`test_getui_privacy.py` 会全仓扫描禁用密钥字面量，在本机巨大的 `docs/verification/artifacts/**`（含本次 144MB APK 与历史解包树）上需 7 分 36 秒，导致目录级 `pytest tests/mobile` 超时；**逐文件运行 15 个文件全部通过（70 条）**，与本任务改动无关（未触碰个推/manifest）。
5. **未执行 `scripts/verify.ps1`**：本任务为 Flutter 客户端改动，且主工作树同时存在**他方**未提交的服务端（Media 平台）改动，运行全仓门禁会把他人半成品结果算到本任务；已用 `flutter analyze` + 全量 `flutter test`（3143）+ mobile 契约（63）覆盖本次改动范围。相关服务端接口（`/blocks`、`/contact-tags`）本就存在，无需后端配合。
6. **未推送 GitHub**：本任务未获推送指令；改动仍在本地工作树。
7. **BUG-06 未做 UI demo/registry 同步**：仅为既有页面的展示细节（日期格式/空状态/分组/筛选摘要），未新增视觉组件，HTML demo 与组件注册表契约不受影响（`test_ui_component_registry.py`、`test_figma_ui_contract.py` 均通过）。
8. **并发工作流（影响"重新构建"，不影响已交付包）**：交付包构建**之后**，另一条工作流在同一主工作树继续改了 8 个 Flutter 文件（会话打开策略等，见 `source-input-sha256-after.json` 与当前工作树的差异）。已交付的 `0.3.95-debug/2133` 是**冻结产物**（构建期间仅 `android/local.properties` 变化），身份由 `source-input-sha256-*.json` + 交付包 SHA256 证明；本任务的 10 项修复经 grep 复核在当前工作树中仍完整，且 23 条新增回归在混合树上仍全通过。**若现在重新构建，会把他人半成品打进包**——重新出包前必须先协调冻结工作树。

## 5. 需要后端配合

**无。** BUG-09/BUG-10 依赖的 `POST /contact-tags`、`GET /contact-tags`、`GET /blocks`、`POST /blocks`、`DELETE /blocks/{user_id}` 均已存在（`services/business-api/app/api/friendship.py`），本次只修客户端未调用与状态未读取的问题。
