# 2026-09-24 公告、账单与客服头衔 debug 验证

> 2026-09-26 集成交接：下文为当次交付历史；其中“未提交/未合并/未推送”及旧 Debug 下一步只描述 2026-09-24 的状态。对应移动实现已随分支集成进入 `main f820d704`，见[集成记录](../workflow/tasks/2026-09-26-branch-integration-main.md)。本文此后补入版本控制，不表示重新构建、安装或发布；当前设备及生产状态以最新任务的实测为准。

状态：0.4.8+2167 actual debug 已保留数据安装 Mi6；只读账单接口已上线。正式 APK、官网分发、Android/iOS 更新配置未发布或更改。候选仍位于 `.worktrees/mobile-feedback-2165`，基线 `9cfcd00c7ea5fd0b626a9f432bc8428b0f351b23`，未合并或推送。

## 验收台账

| 项目 | 实施与证据 | 限制 |
|---|---|---|
| 新公告不加密 | [ADR-0087](../adr/0087-public-group-announcements.md)；正文和图片走专用普通公告发布，保留房间权限与引用校验。隔离真实 Synapse 48.87秒：无设备密钥成员可读正文及原始PNG；普通成员更新引用403，Megolm房间配置不变 | 普通聊天保持加密；旧公告不自动重发。旧客户端可能无法显示新普通公告，需要测试新版 |
| 旧公告 | 仍能解密的旧内容兼容；缺密钥显示联系管理员重新发布。管理员明确确认后重新编写；保留旧图片须本地解密再普通上传，缺密钥禁止悄悄发布不可读引用 | 测试群旧内容没有被还原或删除，需群管理员重发 |
| 钱包说明 | 改为 `1 点钻 = 1 CNY`，Flutter及HTML一致 | 仅展示修正，金融记账及结算规则不变 |
| 全部账单 | 右侧金额下显示点钻余额；后端新增可选字符串 balance_after，Decimal/NUMERIC，先按用户完整账本计算再筛选/分页 | 是按(created_at,id)账单顺序累计余额，不声称历史提交时快照；旧接口缺字段显示“—”，不冒用当前余额 |
| 客服头衔 | 每账号共享、持久缓存；取消30秒清空/重复请求；缓存命中立即显示、只在结果变化时通知UI。覆盖我、联系人、聊天信息、消息发送者及动态表情、朋友圈作者/评论等，长名称截断，复用现有组件 | 当前没有客服角色变更推送事件，恢复前台或访问超过5分钟缓存时后台核对；不承诺实时推送。身份权限仍由服务器决定 |

## 测试与审查

- 最终 Flutter 全量 **4016 passed / 9 conditional skipped**，4分47秒，exit0；analyze最终全量0问题（19.1秒）。首次全量4010通过/3失败/9跳过留档，已修复认证夹具与缓存作用域异常，并完整重跑。
- 最终 frontend **302 passed / 0 failed**；UI契约32组件/403屏通过。浏览器检查本人头衔、账单余额位置与旧公告重写提示，无新增组件。
- scripts/verify.ps1 完整 **exit0 / Verification: PASS**：Infra144、Getui28、MatrixBot9、后端2743通过/74条件跳过（37分33.83秒）、mobile108通过/1条件跳过，迁移离线/OpenAPI/Compose门禁通过。警告和跳过保留原记录，不计通过。
- 专项真实 PostgreSQL **2 passed**，覆盖超过2^53的Decimal金额、同笔多分录、时间相同稳定顺序、分页筛选与账户隔离。独立规格/领域审查先于安全审查，无阻断项。账单窗口查询工作量随用户历史增长，后续容量测量再决定索引/投影优化。
- 独立公告证明及审查：`artifacts/2026-09-24/announcement-public/`；账单PG证据：`artifacts/2026-09-24/statement-balance/`；缓存：`artifacts/2026-09-24/support-cache/`。

## 只读后端发布

用户额外明确授权“两份只读账单文件，同时更新，正式APK不发布”。仅 `app/api/ledger.py`、`app/modules/ledger/statements.py`，基于实时生产镜像叠加；API/worker代码身份保持同步，无数据库迁移、资金操作、配置/开关或静态后台修改。

- API镜像 `sha256:8ca2962190f2264ece7500749a7a9087cda1d03b982b2dbf9942356152594d09`；worker `sha256:bdf21c309d89933e7d7b7936509bcb9493318854a22ec4583f9a5e5997fbe5a2`。
- 发布目录 `/opt/starchat/releases/statement-balance-2167-20260924`；旧镜像、私密配置及数据库备份仅保留服务器0700目录；release.py rollback恢复旧镜像，新增响应字段可直接回退，保留数据。
- 隔离备份恢复136表/221598行；20账号120笔实际副本账单通过只读事务查询、详情一致及无关账户隔离；前后所有既有行指纹一致。未将账户/金额/业务正文输出到日志。
- 既有镜像迁移目录缺0087文件，原通用演练的alembic upgrade失败，**未称迁移演练通过**。本次无schema改动，改用已恢复0087副本的只读查询兼容性证明与原始行完整性检查；不扩大授权修改镜像迁移历史。该迁移打包缺口需另行修复，未来迁移发布前必须闭环。
- 初次副本脚本加载site-packages旧模块而非服务工作目录，修正脚本显式使用 `/opt/business-api` 后通过；生产uvicorn启动工作目录经核实正确，实际运行源码身份匹配候选。
- schema仍0087；两服务healthy、restart0、无新Traceback/maintenance_failed；其他22容器ID及启动时间不变。服务器和工作站HTTPS ready=200 JSON，未登录账单=401 JSON。

## APK与真机

最终重建APK：`C:/Users/Administrator/.codex/visualizations/2026/09/21/01a0c24a-b46f-7230-a656-c1ba2b701aec/docs/verification/artifacts/2026-09-24/android-debug2167/final.apk`。

- 实际debug ARM64，0.4.8/2167；源构建、apktool2.12.1常规重建、16K对齐、既有固定签名、DEX/resource/manifest/Flutter debug kernel比对通过；非原始Gradle中间包。
- SHA256 `350d8f585e76c7af6d6625aa83037dc9146bc50a7aba71d4ec2251f52df59e02`，145461547字节，Mi6安装后base.apk SHA读回一致。
- 01:37:01+08构建验证结束；01:48:53+08 Mi6覆盖安装，DEBUGGABLE确认，首次安装仍2026-09-20 09:35:24，启动进程正常。未卸载/清数据。生产准备与APK测试并行，不叠加宣称耗时。
- C盘承载重建中间物，避免D盘空间不足；原产物未删除。Android/iOS更新设置与之前只读快照一致。
- 真实账户UI点击及旧公告重发、弱网长期缓存效果仍需用户在Mi6复验；无生产聊天/公告/资金造数。

下一步：用户测试2167 debug，优先重发测试群旧公告、查看全部账单逐笔余额及客服头衔；不自动正式发布。
