# Mobile parity verification — 2026-09-09

Approved scope: complete Moments caching/reaction state and history-retention verification, then build Android/iOS from one source revision. Based on main be14e743, branch codex/mobile-parity-20260909. Target identity: 0.3.58 (61); both pubspec and runtime fallback agree. Build 61 follows the formal sequence; older 2xxx Android split/debug numbering is not used.

## Implemented and reviewed

- Moments navigation prepares verified account metadata before pushing the page, enabling cached first paint without trusting a stale profile alone. Feed and cover retain cached content during refresh. Likes update names/count/heart, persist confirmed changes and roll back failures. Loaded older pages remain visible. Account replacement and request revisions reject stale updates. Specification review required and verified the stale-profile correction; independent Quality/Security review required and verified field-scoped persistence so a successful comment cannot save an unconfirmed like or be lost during its rollback. Both reviews now pass; final focused suite: 36 passing tests.
- Shared Moments disk image cache across thumbnail, cover and full-screen viewers. Existing layout/gestures are preserved. Cache manager retains up to 200 entries with a seven-day stale/inactivity policy, not a strict byte quota or guaranteed deletion deadline. Root specification review and independent Quality/Security review passed.
- Follow-up caught randomized signed media paths invalidating URL-only caching. The client now consumes the existing server reference digests, scoped to the verified account and exact API origin, across grid/viewers/cover. Legacy or untrusted input retains full-URL fallback. Real loopback HTTP verifies signature rotation reuses both decoded and disk images without another fetch; changing media identity fetches new bytes. Completed uploads become immutable with idempotent completion and PostgreSQL row locks. Separate specification and Quality/Security reviews passed; 35 focused Flutter and 19 backend Moments tests passed. See `artifacts/2026-09-09/mobile-parity/stable-media-verification.md` and `packages/api-contracts/moments-media-cache.md`.
- History validation exercises real application session/bootstrap/login services with synthetic HTTP and a Matrix boundary spy. Six composed cases plus related tests total 126 passing tests. Ordinary logout, Business refresh 401/403 and Matrix token failures retain existing identity material; same-user login reuses the original device; unconfirmed cross-account login keeps history gated. Root specification and independent Quality/Security review passed. No authentication/E2EE production changes in this branch.
- Current main already incorporated ADR-0007 continuity behavior. The older diagnostic branch's token-error reset finding is superseded for this source; it is not attributed to the release candidate.
- Four additional video integrity tests pass through the actual Matrix SDK encryption/decryption and production cache resolver. H264/HEVC bytes survive simulated ciphertext transport; altered/truncated ciphertext is rejected before plaintext caching. Independent Quality/Security review passed. This does not claim live server delivery.

The final signed-media follow-up passed 1,496 full Flutter tests, a clean full analyzer and repository verification (354 backend/worker tests, 19 skipped; 66 mobile boundary tests; infrastructure 17, Getui 28, bot 9; UI/OpenAPI, offline migrations and Compose checks). Existing test-tool deprecation warnings concern Starlette/httpx and Getui's class-based Pydantic config; no dependency migration or warning suppression was introduced. Public HTTPS Matrix versions, client discovery and Business readiness endpoints passed; probes that initially used nonexistent generic health paths returned 404, then the declared `/api/v1/health/ready` route was verified.

Production already contains the additive cache-key fields from a separate deployment. Its Moments service also contains unrelated functionality, so it was not replaced with this branch's narrower service file. Only `media.py` was deployed in a reviewed overlay onto the exact running API image. The deployment retained the current service byte-for-byte and checked expanded configuration, image/container ownership, and rollback readiness. Candidate image: `sha256:11c2c59b95e13344a2c6e34d763859c7d026ed88f3029471b7a4ed3e9a5658de`; previous image: `sha256:3a6632aaeda8e3cc9acb69de7aa77d611cc94466b34d1c0f8bf76ca106cdcff8`. Internal/public readiness, Matrix versions/discovery all returned 200; unauthenticated Moments feed retained 401. No migration, user-content write or notification was performed. Evidence: primary-workspace `media-guard-deployment.json` and `public-endpoints-after-media-guard.json`. Preparation initially stopped on absent historical Compose interpolation variables; these were derived from the running image identities and existing observer mount, without changing their values. Docker reported its existing legacy-builder deprecation; the three-step overlay build succeeded. The user explicitly selected the merged source plus this repair, excluding the unfinished 0.3.66 debug branch.

## UI delivery record

Existing Moments node: https://www.figma.com/design/zpzwTbnj1hqx80tyRygX78/ChatFlow?node-id=19-4.
Local behavior records: `frontend/artifacts/figma-state.json` and `packages/ui-contracts/changliao-component-registry.json`. No typography, geometry or token changes; cached/loading/liked/rollback state handling is repaired.

Remote Figma update: **Deferred — no callable Figma tools in this session**. The production-hotfix deferral in `docs/ui-development-figma-workflow.md` applies to this runtime regression repair. Existing Figma node ID is a reference only; no remote design modification or screenshot validation is claimed. The local ledger explicitly records `remoteNodesModified: false`.

## Evidence and limitations

Focused synthetic logs live below `docs/verification/artifacts/2026-09-09/mobile-parity/` in this isolated checkout; build/download evidence lives in the same named directory below the primary workspace. No credentials or user messages are recorded. The HTML suite passed 26 tests and UI drift verification passed (17 components, 330 screens).

Native runs 34329689530 at ff6c5a77 and final-source 34332857110 at 3239bee7 both passed on exact iPhone 15 runtimes iOS 18.6 and 26.2: 15 seed/media tests and two separate-process verification tests on each. Native Keychain and SQLCipher retained the original synthetic session/key/history, with no create-on-missing getter in verification. These simulator tests exclude only mobile_scanner because its shipped MLKit binary lacks an ARM64 simulator slice; full production plugin compatibility is not proven by that harness. Dart service reconstruction is not an OS restart, and simulated attachment transport is not a live two-account server test. The unavailable iPhone 15 and third-party enterprise re-sign behavior still require real-device acceptance; see `2026-09-09-mobile-parity-device-acceptance.md`. A signed App Store IPA requires TestFlight distribution or appropriate re-signing; generating it does not update the website enterprise package.

Final application source is committed and pushed as `3239bee76fa6a3409b64b2392f166c9902351666`. `build-source-manifest-3239bee7.json` records all 1,147 tracked mobile files before building; all hashes still matched after Android construction. Superseded ff6c5a77 artifacts are retained only as prior candidates.

Final Android: `ChatFlow-0.3.58-build61-arm64.apk`, 77,010,443 bytes, SHA-256 `f8b9b263d852896d4225c84775978e701c06b5a896a83854a74a810abed34c5c`. Complete Apktool 2.12.1 DEX/resources/manifest rebuild, 16KB zip alignment and original user-tested signing identity passed. Certificate SHA-256 `75b31c66476cd8e2c9319551b49405a1de1e5c23e9a0dbdcc9eb76b52ba61fff`; v2/v3 signatures valid. All 24,573 classes and 337 native/assets entries preserved through rebuild; manifest semantics identical. The final Dart library differs from the superseded candidate, confirming the latest cache repair reached the artifact. Package `com.liuhetong.mobile`, version 0.3.58/code 61, ARM64 only. Android emitted the existing Flutter Kotlin plugin migration notice; plugin/toolchain versions were preserved. Not installed or published in this task.

iOS signed workflow [34332857101](https://github.com/SuperJJ2333/StarChat/actions/runs/34332857101), job 102405285610, passed on that source. Final file: `ChatFlow-0.3.58-build61-AppStore.ipa`, 58,849,964 bytes, SHA-256 `244bdd3d7dd2651b92dc3ccb07fa9c1581100dde02f3fe6f2289a500e6d9eb05`. Local download hash equals the actual cloud verification output; ZIP CRC and public provisioning metadata passed. macOS codesign verified the app deeply/strictly and confirmed production APNs, no debug entitlement, Team HY9Q7Q35S5 and the original application identifier. SQLCipher is bundled and loaded before system SQLite. Bundle `com.liuhetong.liuhetongMobile`, version 0.3.58/build 61, minimum iOS 16.0, device families 1/2, background modes remote-notification/audio/voip. All 24 native core Swift tests passed in the macOS preflight; these are distinct from the iOS simulator tests. Existing Flutter launch-image placeholder validation notice remains in the build log; no launch artwork change is part of this runtime repair. The IPA is App Store provisioned, not an enterprise direct-install package; no TestFlight upload or website publication occurred.

Final-source native workflow [34332857110](https://github.com/SuperJJ2333/StarChat/actions/runs/34332857110) passed both jobs. Downloaded selected-runtime metadata and seed/verify logs independently confirm iPhone 15 / iOS 18.6 and 26.2, with 15 + 2 tests on each. Final local delivery evidence: `release-manifest.json`, `SHA256SUMS`, `android-delivery.json`, `ios-delivery.json`, `ios-cloud-signature-proof.json`, and `native-final-verification.json` in the primary-workspace artifact directory. Both delivered file hashes were rechecked against these manifests. Automated verification and both builds are complete; only the explicitly documented real-device/enterprise-signing and Figma limits remain.
