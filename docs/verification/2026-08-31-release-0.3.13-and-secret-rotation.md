# 0.3.13+16 发布 与 Synapse 注册密钥轮换（2026-08-31）

## 1. APP 0.3.13+16 发布

- 内容：巨石文件治理（`matrix_home_page.dart` 2652 → 765 行，聊天页外置 `room_page.dart`）+ 此前全部功能与修复（含服务端点钻并发修复所需的兼容性收敛）。
- 构建：Release 分 ABI 签名（arm64 SHA256 `167D2C193900B2A549689095EF2833D6987B99F50715CE2A34F5B8BA65FE4AA7`，服务端比对一致）。
- 分发：落地页与应用内更新推送已指向 `ChatFlow-0.3.13-arm64.apk`（幂等键 `app-update-publish-0.3.13-20260831`）。
- 回归：客户端 439 项测试 + `analyze` 零问题；外部验证 PASS。

## 2. Synapse `registration_shared_secret` 轮换为强随机值

- **根因背景**：生产曾使用模板默认值 `change-this-registration-shared-secret`（持有者可注册任意用户，安全隐患）。
- 轮换：`/data/synapse/homeserver.yaml` 的 `registration_shared_secret` 替换为 **64 字符强随机值**（`secrets.token_urlsafe(48)`，值不出服务器）；重启 synapse 后 `/_matrix/client/versions` 200。
- 引用面排查：全栈无其它服务依赖该共享密钥（业务侧注册走业务 API+provisioning，轮换无副作用）。
- 双向验证（密钥值不出服务器）：
  - **旧默认密钥注册 → 403**（拒绝 ✓）
  - **新强随机密钥注册 → 201**（接受 ✓，验证用户 `rotate-repl-test` 已清理）
  - 注意事项：admin 注册 HMAC 第四段为 `notadmin`（非 `false`），验证脚本据此修正。
- 残留告警：测试用户 `rotate-verify-*` 已从 synapse 库清理（access_tokens/devices/profiles/users）。

## 3. 关联安全提醒（待办）

- `enable_registration` 当前为 `false` ✅（普通注册必须经业务 API 邀请码链路）。
- Synapse 数据库口令曾出现于 homeserver.yaml（仅服务器本地文件，未入 Git）——建议后续同样轮换。
