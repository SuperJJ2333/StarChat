# 六合通后台管理系统与网站首页视觉设计方案

**版本**：v1.0 · 2026-08-27  
**依据**：产品现代化规格、UI_DESIGN.md、ui-ux-pro-max 规则  
**目标**：为 React/TypeScript 管理后台与响应式官网提供可直接交付的 IA、流程、页面、字段及视觉规范。

## 1. 设计原则与信息架构

- **可信与可审计**：所有资金、客服和封禁操作显示操作者、原因码、时间、幂等键及审计入口；不读取普通 E2EE 正文。
- **任务优先**：侧边栏按“概览 → 用户与客服 → 内容运营 → 财务 → 系统”分组；每个页面只保留一个主 CTA。
- **数据密度可控**：桌面端 12 列栅格，列表默认 20 行，筛选器可折叠；移动端转为卡片和底部抽屉。
- **无障碍**：正文 ≥16px、对比度 ≥4.5:1、所有图标按钮含 aria-label、键盘可达、状态不只依赖颜色。

后台导航：
```
概览 / 用户与安全（用户、IP/用户封禁、客服角色、在线客户）
运营（朋友圈原生广告、官方通知）
财务（点钻发放、点钻流水、USDT提现）
系统（审计日志、角色与权限、配置中心）
```

## 2. 视觉设计系统（ui-ux-pro-max）

### 2.1 品牌与色彩

| Token | 色值 | 用途 |
|---|---|---|
| `brandPrimary` | #16A36A | 主品牌、链接、选中态 |
| `brandPrimaryStrong` | #0F7A4F | 深色按钮/焦点 |
| `surface` | #FFFFFF | 卡片、表格、弹窗 |
| `page` | #F5F7FA | 后台背景 |
| `ink` | #17202A | 标题与正文 |
| `muted` | #667085 | 辅助文字 |
| `border` | #E4E7EC | 分隔线 |
| `success` | #16845B | 成功（同时配 ✓ 文案） |
| `warning` | #B54708 | 待处理（同时配 ⏳ 文案） |
| `danger` | #B42318 | 封禁、失败、撤销 |
| `info` | #175CD3 | 信息提示 |

官网增加深色 Hero：`heroInk #0B1F18`、`heroGlow #D8F5E8`。禁止在组件内写裸 hex，统一引用语义 Token。

### 2.2 字体、栅格与组件

- 中文优先 `PingFang SC`, `Noto Sans CJK SC`, `Inter`, system-ui；数字使用 tabular-nums。
- 类型阶梯：Display 40/48 700、H1 32/40 700、H2 24/32 650、Body 16/24 400、Label 14/20 600、Caption 12/18 400。
- 4px 基线（4/8/12/16/24/32）；卡片圆角 12px，输入 10px，胶囊 999px；普通卡片无阴影，弹层 `0 8px 24px rgba(16,24,40,.12)`。
- 桌面断点 1440/1200/1024，平板 768，手机 375；内容最大宽度 1200px，移动端水平边距 16px。
- 按钮最小 44×44px；主按钮为品牌绿实心白字，危险按钮为白底红边；异步操作显示 spinner 并锁定重复提交。
- 图标统一 Lucide 线性 1.75px；不使用 emoji；表格支持列显隐、复制、导出、空状态和骨架屏。

## 3. 后台通用布局与交互

```
┌──────────┬─────────────────────────────────────────────┐
│ Logo     │ 顶栏：面包屑  全局搜索  通知  帮助  管理员头像 │
│ 侧边导航  ├─────────────────────────────────────────────┤
│ 分组菜单  │ 页面标题 + 描述             [主操作]       │
│           │ 筛选栏（日期/状态/搜索/保存视图）           │
│           │ KPI 卡片 / 图表 / 数据表                   │
└──────────┴─────────────────────────────────────────────┘
```

- 侧栏宽 248px，可收缩为 72px；移动端改为汉堡菜单 + 底部 4 项快捷导航。
- 筛选条件写入 URL，返回时恢复滚动位置；批量操作需二次确认并展示影响数量。
- Toast 使用 `aria-live=polite`，3–5 秒自动消失；错误靠近字段并给出重试。
- 所有敏感写操作要求近期重新认证 + TOTP（按规格），完成后生成审计记录和 Outbox 事件。

## 4. 功能模块设计

### 4.1 发点钻给客服
**流程**：选择客服 → 输入数量/批次备注 → 校验可发额度与余额 → TOTP 确认 → 事务写入发放批次、账本分录、审计、Outbox → 成功回执。失败保持表单可重试且幂等键不变。  
**页面**：`/finance/point-grants` 列表（批次号、客服、数量、发放人、时间、状态、详情）；“新建发放”侧抽屉；详情页含账本交易链接。  
**字段**：`batch_id`、`idempotency_key`、`recipient_user_id`、`recipient_display_name`、`amount_caibi(Decimal,2)`、`reason_code`、`note`、`issuer_admin_id`、`created_at`、`status`、`ledger_transaction_id`、`audit_event_id`。  
**交互**：客服搜索支持编号/昵称；金额实时显示两位小数；提交按钮显示“发放并记录”，成功后可复制批次号。

### 4.2 封禁 IP 和用户
**流程**：输入对象（IP/CIDR 或账号）→ 选择原因、范围和时长 → 预览影响会话 → 确认封禁 → 业务登录、Matrix 账号及房间成员资格同步失效 → 记录审计；解封走相同复核。  
**页面**：Tab“用户封禁/IP 封禁”；表格显示对象、状态、原因、开始/结束、操作者；详情抽屉显示命中次数与关联审计。  
**字段**：`ban_id`、`subject_type`、`subject_value_hash`、`display_masked_value`、`reason_code`、`reason_note`、`scope`、`starts_at`、`expires_at`、`created_by`、`lifted_by`、`lifted_at`、`status`。  
**交互**：永久封禁需输入“PERMANENT”；解封按钮危险样式 + 二次确认；到期自动转为已过期。

### 4.3 设置账户升级为客服
**流程**：搜索普通用户 → 选择角色与技能 → 配置可见用户范围/额度 → 预览权限 → TOTP 确认 → 立即撤销旧 Token、更新房间加入权限 → 通知用户。取消角色需选择交接客服。  
**页面**：客服目录（在线、技能、活跃工单数）；角色编辑页；权限矩阵页。  
**字段**：`user_id`、`support_number`、`role`、`skills[]`、`user_scope`、`daily_quota`、`ticket_permissions[]`、`badge_color_token`、`effective_at`、`revoked_at`、`updated_by`。  
**交互**：权限采用分组复选框；高风险权限（转账/提现复核）旁显示“需主管”；保存前显示差异清单。

### 4.4 统计平台注册用户
**页面**：`/analytics/users`。顶部 KPI“注册总数/今日新增/验证率/30日留存”，下方折线图（注册趋势）+ 堆叠柱（渠道）+ 状态环图；右侧筛选日期、渠道、状态。  
**字段**：`date_from`、`date_to`、`channel`、`account_status`、`registered_count`、`verified_count`、`active_count`、`retention_d30`。  
**交互**：图表 hover 显示绝对值与环比；导出遵循当前筛选；无数据展示解释性空状态。

### 4.5 在线客户数量
**页面**：`/support/online`。KPI 显示在线总数、客服在线数、排队数；列表列出头像、客服编号、技能、最近活跃、当前工单数。  
**字段**：`user_id`、`display_name`、`support_number`、`presence`、`last_active_at`、`active_ticket_count`、`device_count`。  
**交互**：WebSocket/SSE 推送 presence；断线显示“数据更新于 xx 秒前”；支持按技能、在线时长排序。

### 4.6 朋友圈原生广告
**流程**：新建广告位 → 编辑素材/文案 → 设定投放时间与定向 → 预览 → 提交审核/发布 → 实时统计 → 到期归档。  
**页面**：广告位列表、编辑器（左表单/右手机预览）、数据详情。  
**字段**：`ad_id`、`slot`、`title`、`body`、`media_url`、`landing_url`、`start_at`、`end_at`、`target_rules`、`status`、`impressions`、`clicks`、`ctr`、`reviewer_id`。  
**交互**：素材尺寸/文件大小即时校验；定向规则以 Chip 展示；发布前显示频控和预计覆盖人数；CTR 使用公式计算并标注统计窗口。

### 4.7 官方通知公告
**流程**：新建草稿 → 编辑标题/正文/附件 → 选择受众 → 预览 → 立即/定时发布 → Outbox 分发 → 阅读回执统计 → 可撤回（生成新事件，不修改历史）。  
**页面**：公告列表、Markdown 编辑器、受众选择抽屉、阅读分析页。  
**字段**：`announcement_id`、`title`、`body_markdown`、`audience_rule`、`publish_at`、`status`、`created_by`、`published_at`、`read_count`、`total_recipients`、`withdrawn_at`。  
**交互**：离开编辑页自动保存草稿；定时发布显示时区；危险撤回需输入公告编号。

### 4.8 点钻流水
**页面**：`/finance/ledger`。高级筛选（交易 ID、用户、类型、金额区间、日期、原因码）；表格列交易 ID、时间、用户、类型、金额、余额变动、备注、状态；详情展示借贷分录、关联批次及审计。  
**字段**：`transaction_id`、`entry_id`、`occurred_at`、`user_id`、`asset_code=CAIBI`、`transaction_type`、`amount`、`balance_before`、`balance_after`、`reason_code`、`idempotency_key`、`reversal_of`、`actor_id`。  
**交互**：金额右对齐等宽数字；导出异步生成并通知；冲正只能“创建关联冲正”，不可编辑原记录。

### 4.9 USDT 提现和支付地址
**流程**：申请进入队列 → 校验 TRC20 地址/黑名单/限额 → 冻结金额（含费用）→ 财务审核 → ≥1000 USDT 双人审批+TOTP → 调用托管商（稳定 client_order_id）→ 查询确认/失败退款 → 对账。  
**页面**：提现队列、详情审核页、支付地址簿、手续费与限额配置、对账差异页。  
**字段**：`withdrawal_id`、`user_id`、`network=TRC20`、`address_masked`、`address_hash`、`amount_usdt(Decimal,6)`、`provider_fee`、`platform_fee`、`frozen_total`、`min_limit`、`max_limit`、`client_order_id`、`status`、`approver_ids[]`、`tx_hash`、`requested_at`、`confirmed_at`。  
**交互**：地址默认中间省略，详情才可复制完整值；未知托管结果仅提供“查询原订单”；手续费配置变更显示生效时间和历史版本。

## 5. 网站首页视觉方案

### 5.1 桌面线框
```
┌─────────────────────────────────────────────────────────────┐
│ 六合通 Logo   产品  安全  公告        [下载客户端] [登录]    │
├─────────────────────────────────────────────────────────────┤
│ Hero：让每一次连接，都值得信任                              │
│ E2EE 聊天 · 官方客服 · 彩币与 USDT 托管                     │
│ [立即加入] [了解安全]      右侧：设备 mockup + 光晕         │
├─────────────────────────────────────────────────────────────┤
│ 数据概览：100,000+ 注册用户 | 2,000 在线 | 50 TPS 账本峰值   │
├─────────────────────────────────────────────────────────────┤
│ 核心能力  聊天 / 客服 / 彩币 / 红包 / 钱包（5 张卡片）       │
├─────────────────────────────────────────────────────────────┤
│ 公告中心（3 条）                         安全与合规时间线     │
├─────────────────────────────────────────────────────────────┤
│ CTA：下载六合通，开启可信沟通                 QR + 商店入口  │
├─────────────────────────────────────────────────────────────┤
│ 页脚：帮助中心、服务条款、隐私、状态页、备案信息           │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 首页结构与交互

- Hero 使用深墨绿背景、浅绿色径向光晕和静态产品截图；仅做 180ms 淡入，尊重 reduced-motion。
- 公告卡支持置顶、发布日期和“查看详情”；不在首页展示敏感余额或用户隐私。
- 统计概览使用动画计数但不超过 300ms，提供纯文本替代；数据来自公开聚合 API。
- 入口图标采用 Lucide（MessageCircle、Headphones、Coins、Gift、WalletCards），图标+文字并列。
- 下载 CTA 根据 UA 展示 APK/TestFlight/桌面端入口；二维码带固定尺寸避免 CLS。

### 5.3 响应式规则

- 1440px：Hero 双列 52/48，内容最大 1200px；能力卡 5 列。
- 1024px：Hero 55/45，能力卡 3+2；公告与安全区上下排列。
- 768px：导航折叠，Hero 单列，设备图移到文案下方；统计 2×2。
- 375px：单列、边距 16px；CTA 全宽并上下排列；能力卡横向滑动但页面不产生横向滚动；正文最小 16px。

## 6. 关键页面高保真规格

### 概览 Dashboard
- 顶部四张 KPI 卡：注册用户、在线客户、待审核提现、今日彩币流量；每卡含趋势箭头、环比和“查看详情”。
- 中区 8/4 栅格：注册趋势折线（带图例/tooltip）与待处理队列列表；底部为最近审计事件。

### USDT 提现审核
- 左侧申请摘要（用户、金额、网络、脱敏地址、费用明细）；右侧风险检查清单、审批时间线与固定底栏操作。
- 主 CTA“批准并提交托管商”；次操作“退回补充”；危险操作“拒绝并退款”。金额、状态和风险均文字+图标表达。

### 官网首页
- 采用 12 列栅格、8px 间距、64px 区块垂直节奏；Hero 标题最大 56px，移动端 36px。
- 图片使用 WebP/AVIF，声明宽高并 lazy-load 首屏以下资源。

## 7. 交付与验收清单

- React 路由可深链；筛选状态可分享；所有写接口带 `Idempotency-Key`。
- 颜色、字体、间距均来自 Token；通过 axe 检查无严重问题，键盘可完成主流程。
- 表格、图表、表单、空状态、错误态、加载态、权限不足态均有组件稿。
- 高风险动作具备 TOTP、二次确认、审计字段和回滚/冲正入口；不展示 E2EE 消息正文。
- 设计稿需输出桌面 1440、平板 1024、移动 375 三套 Frame，并标注组件变体、状态和断点。
