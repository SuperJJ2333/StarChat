export function presentModuleRows(key, items = []) { return items.map((item) => { if (key === "ledger") return [item.transaction_id, item.time, item.user_id, item.type, item.amount, item.reason_code]; if (key === "wallet") return [item.id, item.user_id, item.amount, item.address, item.status]; if (key === "ads") return [item.id, item.advertiser_name, item.text, item.created_at, item.status]; if (key === "online") return [item.id, item.username, item.status, item.last_seen_at, item.active_tickets ?? "—"]; if (key === "analytics") return [item.created_at, item.username, item.status, item.email_verified_at ?? "—", "—"]; return Object.values(item); }); }
