# 0.3.29 发布验证（2026-09-03）

## 发布内容

- 好友私聊统一同一规范房间（canonical 后端已部署，迁移 0035）
- 好友资料页「发消息」修复（不健康旧房间重邀/绕开新建）
- 群聊非好友成员可进用户资料页并发起「添加到通讯录」（`/users/lookup`）
- 后台/锁屏通知与来电铃声恢复（dataSync 前台服务保活）
- 消息通知锁屏可见性 public

## 发布步骤与证据

| 步骤 | 结果 |
| --- | --- |
| 提交修复 | `ff5c6eb` fix(mobile): shared canonical direct room + background/lockscreen notifications |
| pubspec | `0.3.29+32`（versionCode 2032/1032/4032 per ABI） |
| 构建 | `scripts/build_mobile_public_domain.ps1`（split-abi + 混淆 + 符号分离） |
| 上传 `/opt/starchat/frontend/downloads/ChatFlow-0.3.29-{arm64,arm32,x86_64}.apk` | SHA256 双端一致：arm64 `0791fb59…9f91c19f899`、arm32 `ce804fa9…37b9b36a8`、x86_64 `b4e412c1…327260bc` |
| `latest-*.apk` 别名刷新 | 指向 0.3.29 |
| 发布脚本（容器内 `publish_app_update_0.3.29.py`，幂等键 `app-update-publish-0.3.29-20260903`） | BEFORE=0.3.28/2031 → PUT 200 → LATEST 0.3.29/2032 `PUBLISH_RESULT PASS` |
| 公网抽测 | `ChatFlow-0.3.29-arm64.apk` → 206 + APK 魔数；content-length 58,988,366 与本地一致 |
| **Mi 6 真机（0.3.28/2031 冷启动）** | **弹出「发现新版本 0.3.29」+ 完整更新说明 + 稍后再说/更新**（截图 `artifacts/2026-09-03/mi6-update-popup-0.3.29.png`） |
| 应用内更新链路 | 点「更新」→ 外部浏览器下载 `ChatFlow-0.3.29-arm64.apk`（56.3MB）成功；MIUI 安装确认需人工交互，止步于此 |

## 用户报告「下载链接版本号 0.3.26」排查（同日）

逐入口核验后全部为 0.3.29：
- `app_settings`：latest_version/build/notes/apk_url 全部 0.3.29（DB 直查）
- 公网落地页（www 与 home.html）：仅引用 `latest-arm64.apk` 别名，无版本号文案
- 别名与真实文件：`latest-arm64.apk` → 0.3.29（content-length 一致）
- App 更新弹窗：实时拉取 `/app-updates/latest`，无本地缓存

结论：0.3.26 只能来自**历史直链**（0.3.26 时期的 `apk_url` 即
`/downloads/ChatFlow-0.3.26-arm64.apk`，旧包在发版后仍留在服务器可直接
访问）或**手机下载管理里的旧文件**。处置：
- 清理服务器历史 APK（保留 0.3.28 回滚 + 0.3.29 当前），旧直链现在回落
  到落地页（200 text/html），**不再可能下载到旧版本**；
- `latest-*` 与 0.3.28/0.3.29 直链复测 206 正常。

## 回滚方式

1. `PUT /api/v1/admin/app-update-settings` 指回 0.3.28/2031 +
   `ChatFlow-0.3.28-arm64.apk`（文件保留）。
2. 后端如需回滚：`/opt/starchat/backups/business-api-20260903-pre0035/`
   + 旧镜像 digest `8eb74808a365`（direct_conversations 为纯新增表，
   回滚代码无需删表）。
