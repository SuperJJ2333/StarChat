# 在线状态任意入口修复 + 下载链接复制（debug 2092）

日期：2026-09-12。提交 `662c7152`；服务端镜像 `friend-detail-20260912`
（`ebbaa3eb…`，FROM b7ea31f0，仅覆盖 friendship service/api 两文件，
健康、可回退）；Mi 6 安装 `0.3.85-debug/2092`（固定签名重建管线，
27,305 类零变更）。

## 问题与修复

1. 在线状态只在“通讯录”路径生效；会话/朋友圈/搜索入口无数据，
   恒显示“暂无在线记录”。
   - 服务端新增 `GET /api/v1/friends/{friend_id}`：好友详情（含
     last_seen_at），非好友 404（4 个新测试全绿）。
   - `ContactProfilePage`（全部入口共用）打开即自取该接口：在线状态
     不再依赖入口数据新鲜度；非好友（404）隐藏状态行；本地构造的
     视图（lastSeenKnown=false）在自取落地前也隐藏，杜绝误报
     “暂无在线记录”；失败静默保留现有内容。
2. 个人信息页邀请码区“一键复制”改为两个下载链接复制按钮：
   一键复制安卓下载链接（…/download）、一键复制iOS下载链接
   （…/download?platform=ios），样式与邀请码入口一致，含成功/失败
   轻提示。

## 实机验证（Mi 6，生产 API）

- 通讯录 → 好友 feng567：服务端 `GET /friends/484ce553…` 200，
  资料页显示 **“2小时前在线”**（final-friend-profile2.png）。
- 个人信息页：两个下载链接按钮渲染正常（codecheck.png）。
- 排查记录：首次自取请求曾 401（恰逢服务端容器切换窗口的令牌边界），
  属瞬时；重启后 200。此前截图中的“小彭/我的iPhone”是通讯录“群聊/
  会话”分区的条目而非好友列表，好友列表在其下方（clean-contacts2.png：
  一蓑烟雨/superJJ/Beya/你好）。

## 后续

- 全量正式版发布时该接口随镜像携带（已部署生产，无需额外操作）。
- 若需“暂无在线记录”仅在确为好友但无设备记录时出现：当前语义即为
  此（404 隐藏、好友无值显示暂无在线记录）。
