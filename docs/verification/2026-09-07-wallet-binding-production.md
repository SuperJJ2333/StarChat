# 钱包生产接入：本轮验证记录

状态：本地实现及受限验证完成，2026-09-07 13:48（香港时间）已部署资金关闭版本。详见[部署证据](2026-09-07-wallet-binding-deployment.md)和[运行手册](../runbooks/wallet-binding-deployment.md)。生产管理员页面验收待完成。

已实现：后台链上 summary/list/detail，固定快照分页、精确金额、授权访问、只读 SQLite、详情审计；钱包 10 USDT 最低额；一次性签名挑战、绑定历史与地址永久归属、滚动 30 天限制、受信任证据后激活的绑定核心；默认关闭的 REST 写入口和明确依赖状态。

领域和 Quality/Security 两轮复审通过。审阅中修复了详情请求竞态、详情审计对象不可追踪、绑定屏障来源证据未持久化及证据类型校验不足。

- 最低额 RED：2 failed / 2 passed；修改后钱包专项 92 passed / 3 skipped。
- 后台查询专项最终 14 passed；前端全部 31 passed。应用内浏览器隔离页面显示 PASS，六位金额、分页、详情已验证；这不代表真实管理员生产登录验收。
- 绑定核心最终 42 passed，受限接口 1 passed；独立 TronWeb 6.0.4 离线消息签名向量通过，随机测试密钥未保留。
- PostgreSQL 18：0041→0042 迁移、并发地址争用、归属/来源证据不可变触发器通过。备份恢复后版本为 0042，绑定记录数为 1。测试服务已停止；数据库与备份文件由本目录 gitignore 排除。
- PostgreSQL 初次运行遇到 Windows 子进程捕获管道未退出；改为控制程序输出到文件。其次测试夹具误用晚于核验时点的时间，按适配器约定使用传入服务端时点后通过。生产同版本 PostgreSQL 16 的后续 38 项隔离验证也已完成，见部署证据。
- 首轮 `scripts/verify.ps1` 完整 PASS；包含业务 API/Worker 523 passed / 30 skipped。绑定集成后的最终整套脚本也 PASS，业务 API/Worker 567 passed / 31 skipped，日志为 `artifacts/2026-09-07/wallet-binding-production/verify-final.log`。新增 PostgreSQL 测试在普通套件中跳过，在隔离 PG18 演练中已单独运行通过；不把其他可选跳过项声称为已执行。
- 合并后的专项核验 134 passed；新增后端文件 Ruff 检查通过；只读 Compose 叠加配置渲染通过；OpenAPI 与 UI 契约检查通过。
- 上游 Starlette/httpx 与既有 Pydantic 弃用告警已记录，未屏蔽。依赖缺失使用 Python 3.12 已安装的锁定库解决，不使用默认其他 Python 环境替代。

生产：此前 SSH 23421 握手超时，现已恢复直连。已完成服务器私有备份、生产同版本 PostgreSQL 16 隔离恢复与迁移（38 项检查）、新旧应用启动和回退组合验证，并已部署 API/Worker、0042 及三个后台 JS 文件。线上健康检查、只读挂载、观察查询和未登录 401 检查通过，资金开关保持关闭。

UI：管理端新增链上查询组件，复用现有管理样式；Figma 同步按用户此前暂缓指示登记为未同步，未宣称远端节点已修改。启动无头 Chrome 的命令被自动审批以“blocked by policy”拒绝；随后通过受支持的应用内浏览器完成隔离验证。

未完成：正式 MFA/账户权限/独立来源适配器、私人钱包客户端签名、充值意图归属入账、提现绑定目标及费用快照和生产管理员逐笔验收。真实充提未启用。
