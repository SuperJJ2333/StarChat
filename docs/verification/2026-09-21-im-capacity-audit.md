# 500人群聊容量验收状态

日期：2026-09-21。结论：**完整500人验收未完成、未判通过，也没有据本次小样证明500人不可实现。**

## 本次实际执行

本机独立Docker项目 `chatflow-capacity-74ccb27a809a`：Synapse 1.132.0补丁镜像、sync worker、PostgreSQL16.9、Redis7.4.2、Nginx；只发布loopback，不读取生产.env或生产账号，不修改默认Megolm轮换。Windows宿主32GB内存，预检时仅约4GB可用且有其他任务，Docker报告12CPU/约15.2GiB内存上限；不是独占性能基准。

| 层级 | 实际结果 | 不能推导的结论 |
|---|---|---|
| 媒体HTTP集成 | 同MXC跨用户复用、独立引用删除、保留blob重用、8路并发上传、隔离后拒绝重传均通过；关闭开关场景本次未跑 | 样本是4096字节opaque数据，不是图片/语音解密或300KB/10秒负载 |
| 真实SDK加密冒烟 | 2账号，2条双向文本，accepted2、密文收到4、解密4 | 不是500人压测 |
| 真实SDK重复收发 | 相同2账号保留设备密钥与本地库，20条双向文本，accepted20、密文收到40、解密40 | 40包含每条消息发送者自己的同步回显及另1名接收者；不是40个外部接收者 |
| 工具安全/统计合同 | 最终7项单测通过：loopback、run身份、事件ID不能算解密、SDK禁止重定向、重复ID拒绝及固定请求量分母 | 不是移动端UI测试 |

加密步骤是实际上传设备公钥、同步设备列表、与本次受控SDK身份公钥精确比较后验证设备、Olm/Megolm分享、SDK加密发送、收到密文并匹配解密后的随机测试文本；没有通过手动复制房间密钥代替网络分享，没有ignore_unverified绕过。使用Python matrix-nio0.26.0/vodozemac0.10.0，而非产品Flutter SDK，因此不证明Flutter客户端互通已验收。

2条样本P95/P99均172ms；20条样本P95=968ms/P99=1031ms。起点为主机调用SDK send，终点为SDK解密回调，包含顺序发送与批次同步调度；小样本、含自身回显、没有UI，也不是5条/秒恒定负载。这些分位数只作冒烟观察，不用于产品SLO判定。统计以(event_id,client)去重，未证明UI无重复展示。

证据：[2条汇总](artifacts/2026-09-21/im-architecture-audit/e2ee-2-message-summary.json)、[20条汇总](artifacts/2026-09-21/im-architecture-audit/e2ee-20-message-summary.json)、[runner](artifacts/2026-09-21/im-architecture-audit/e2ee_probe.py)、[合同测试](artifacts/2026-09-21/im-architecture-audit/test_e2ee_probe.py)、[媒体集成](artifacts/2026-09-21/im-architecture-audit/media-integration.log)。

第一次SDK初始化遇到Windows信任文件名中冒号导致OSError；保留原设备数据库并改用SqliteStore安全文件名后成功。此为本次测试工具兼容性问题，不能算产品缺陷，也不掩盖初次未完成。设备密钥库仅在私有run目录保存。

网络实测后，质量审查补强了重复event_id拒绝和固定分母判断；7项回归通过但没有重新启动网络测试，原始摘要保持原样。两次样本accepted恰为请求的2/20，不受该边界问题影响。本任务容器和网络已停止/移除，私有数据保留，其他本地服务未停止。

## 尚未满足的用户验收项

| 项目 | 状态与原因 |
|---|---|
| 50/200/500真实SDK同时在线 | 未执行；当前可用物理内存约4GB、非独占；小样runner不是持续在线负载发生器 |
| 20–50活跃、常态5条/秒、峰20条/秒 | 未实现定速负载调度/活跃子集，未执行 |
| 90%文字/5%300KB图片/5%10秒语音 | 未实现媒体生成、上传下载解密对账，未执行 |
| 三轮30分钟+1小时 | 未执行；现有k6单阶段上限30分钟，且不做真实E2EE；新probe最多100条，仅短冒烟 |
| 断网、重连、成员/密钥变化与60秒补齐 | 单元测试有局部覆盖，真实SDK群压测未执行 |
| UI可见P95/P99、打开、输入、滚动SLO | 未执行；需产品release/profile构建、端侧打点与三类设备 |
| 零丢失/重复展示/长期稳定性 | 仅22条小样的接收与解密对账通过，不能推广为长时/大群保证 |

已异步向用户询问独立测试主机及主流Android/iPhone位置；未获资源前不把本机负载结果伪称生产定容。资源到位也仍需实现持续混合负载runner，并经过小规模正确性校验，不能直接重跑当前probe宣布达标。

## 恢复执行

1. 选择独占被测主机及独立发生器，记录CPU/内存/磁盘/网络与镜像digest；三类真机使用绑定源码的release/profile包。
2. 在同一SDK工具中补受控活跃子集、固定速率/峰值、真实图片和语音加解密、每消息×接收者对账、冷/暖/重连编排；不改变产品协议。2→50→200→500逐级验证资源余量与正确性，任一级失败停止升档。
3. 再执行用户批准的150分钟500档持续负载及设备指标；失败原始记录保留，监测发生器自身CPU/RSS避免误归因。
4. 独立报告服务端接受、密文收到、成功解密、Flutter显示、推送到达与附件完成；不能把长轮询耗时当消息时延。

完整批准目标见[计划](../superpowers/plans/2026-09-21-im-architecture-audit.md)，历史失败数据见[2026-09-10报告](2026-09-10-media-capacity-runtime.md)。
