# 2161 用户反馈修复：生产已发布，Mi6 2162已安装

代码提交 `a1afa2b635ccda5b4fb7a6f490011ab0e3bd1ec1`。350项镜像源码与该提交逐项一致；其他任务未提交变更未纳入发布。

基线 `7140ace2`，隔离目录 `.worktrees/support-feedback-20260923`。本批部署及 APK 安装沿用用户明确授权；其他任务的红包变更未纳入候选。先按规格核对，再由独立审查检查安全与恢复。受保护行为决策见 ADR-0082。

## 需求与结果

| ID | 改动 | 实施验证 | 发布验证 |
|---|---|---|---|
| F1 | 客服开通邮件从误用钱包告警契约改为专用 OTP 邮件 | 真实 SMTP 适配器＋替身传输回归；旧 wallet alert 校验保留 | API/worker已切换；未代发真实邮件 |
| F2 | 用户充值不填凭证；绑定付款来源＋官方地址＋金额＋时间窗口自动关联 | 归属/歧义/取消/失效绑定/到期/网络失败；关联不入账 | 已上线；未发生真实资金操作 |
| F2a | 客服默认本次参考汇率及 ±1%/±5% | BigInt 固定基准、半入舍入，过期/缺失无假默认；既有审批不变 | 后台资源已发布，双侧HTTPS/SHA通过 |
| F3 | 20 点钻低于 10 USDT 提现门槛，中文 Toast；明确拒绝后改额重试 | 实际未知结果保留幂等意图，避免重复冻结 | Mi6已安装2162；业务待用户复验 |
| F4 | 手机输入实时校验/绿色验证码按钮；未知号 OTP＋邀请码＋条款后开户 | 复用注册限频、账号/邀请事务；设备绑定短期票据；旧待验证账号拒绝跨流程激活 | Mi6与后端均更新；真实开户待用户复验 |

用户澄清：仅 APP 已绑定钱包付款；新用户仍需邀请码；汇率仅供参考，实际到账以客服处理为准。已入账未登记不重新占用凭证，恢复登记不二次发币；链上查询不可用不阻塞已执行结果恢复。

## 验证证据

- 充值/worker 组合：129 passed；随后独立两文件检查发现测试导入顺序问题，改为建表前注册依赖后 11 passed。生产代码未因此改动。
- 充值隔离 PostgreSQL：两线程、独立数据库 session 同选一笔凭证，仅一方关联，0 点钻自动入账；最终 1 passed，无警告。此项不是跨进程压测。
- 手机开户/票据隔离 PostgreSQL 及专项、独立复审见 `2026-09-23-phone-onboarding.md` 和本任务 `artifacts/.../phone/`。
- Flutter analyze 无问题；首次长路径全量 3883 passed / 1 failed（Windows Directory.list PathNotFound）。映射同一源码到短路径 R: 后单文件7通过、完整 **3884 passed**；无产品代码变化、无删测试或跳过失败。
- HTML 最终 **274 passed / 0 failed**；UI 契约 **32 components / 398 screens PASS**，未新建组件。真实浏览器发现并修复 `data-eligible` 被严格组件拒绝及属性变化不刷新按钮的问题；回归增加属性白名单和挂载后变化测试。
- 浏览器本地演示：`http://127.0.0.1:8153/index.html?screen=phone-login-phone-error` 有效手机号后按钮 enabled、颜色 rgb(7,193,96)，未同意条款有明确反馈；`recharge-directory-directory` 仅金额→地址→等待系统核对；`wallet-withdrawal-default` 的20点钻触发最低10USDT中文Toast。均为演示数据，不是生产资金验收。Figma 已退役：本批仅更新 HTML demo 和 Flutter。
- 后续门禁独立执行已通过：mobile108 passed/1 skipped、UI契约、API import、267文件AST、0087单头/全链离线迁移、OpenAPI和Compose。不是完整verify退出0。
- 完整 verify：后端原运行 **16 failed / 2626 passed / 67 skipped，3193.45秒，exit1**；16项均位于matrix_login_token、matrix_login_broker、profile_api，最终夹具SHA对应49项修正回归通过，另有储备夹具7项通过。原进程在修改前已加载旧模块，不能将补充证据冒充原verify退出0。按门禁证据复用规则保留原失败+受影响复测+后续门禁独立exit0，未再跑相同未变输入的整套回归；已暴露测试收集时固定NOW造成JWT过期的失败，修正仅涉及测试夹具，56项专项通过，未放宽鉴权/储备门槛。见 `2026-09-23-test-collection-clock.md`。预检首次因隔离目录缺 `.env` 失败，随后仅复制 `.env.example` 测试占位；一次因注册限频补正主动中止，不视为通过。最终日志 `artifacts/2026-09-23/support-feedback/verify.log`。

## 发布准备与边界

生产基线 schema0087；仅11个 API/worker Python文件增量。候选镜像350个源码文件逐项SHA核对通过；配置和关闭开关保持。生产备份留宿主机0700发布目录；隔离恢复136表/215631行，经候选 Alembic no-op 后原数据相同。第一次恢复在 PostgreSQL 初始化完成前连接目标库失败，改为确认目标库存在后恢复通过，不操作生产库。

后台只发布 `src/admin-recharge-panel.js`。生产设计图库与本地历史版本不同，未覆盖无关图库资源；本地HTML演示随本批代码保留。已完成API/worker及该后台文件切换；生产主机和工作站jumper两侧HTTPS证书验证通过，健康接口200，受保护接口未授权401，后台资源SHA一致。工作站首查因本地SOCKS未启动返回curl7，启动专用loopback隧道后复测通过，保留两次日志。

APK：0.4.3+2162 Debug，固定证书延续；源码构建/常规重建/签名已完成，独立重解包通过：27317个类和339项native/assets保持，清单语义一致；已于2026-09-23 16:57:54+08:00保留数据覆盖安装Mi6；读回2162/Debug、设备内APK SHA一致、启动Status ok且进程存在。安装前一度offline→unauthorized，用户恢复USB调试授权后安装成功；没有卸载或清数据。APK SHA256 `3460cb5005c6a13032d7a97419814cd4d638fdc0a690f5758a6c89ee50aec5f1`，固定证书 `75b31c66476cd8e2c9319551b49405a1de1e5c23e9a0dbdcc9eb76b52ba61fff`。构建使用主目录缓存前逐个核对并复制15个本批移动端文件，其他任务文件未覆盖；最终提交仅包含本批清单。

真实客服邮箱送达、真实新号短信开户、真实链上到账与客服拨款仍由用户实测；本批没有代操作真实资金、没有清除 Mi6 数据，不把替身或演示结果当作这些真实链路已验。

## 时间与交接

整个任务起始准确时刻未完整采样，不推断总耗时。最终后端 verify 子进程开始：2026-09-23 15:57:43+08:00。Flutter首次全量5分25秒、短路径最终全量5分29秒；并行墙钟不求和。16:12:33观察到APK源码构建已完成，进入重建验证。

下一步：用户按下方入口复验真实邮箱、手机号开户和钱包订单；不将本地替身测试、只读生产检查或成功启动等同真实业务验收。

## 用户真机复验入口（尚未代验）

1. 客服后台首次开通重新获取邮箱验证码，确认收件和后续验证；旧Outbox发布状态不能替代邮件送达证据。
2. 充值只填写金额，下一步展示官方地址/二维码。从APP中已绑定的钱包按该金额转入后，检查系统确认到账，再由客服结算；确认客户端不要求txid，客服默认参考汇率及±1%/±5%按钮基准固定。请勿将演示页状态作为到账证据。
3. 提现输入20点钻应出现最低10USDT提示；更换金额后检查新的参考应付和请求金额，真实出款仍按既有客服流程。
4. 手机登录输入无效/有效大陆手机号，检查验证码按钮状态；同意条款后发码。已有账号正常登录，新号码仍须有效邀请码；未完成的旧注册须回原注册流程。

## 实际发布证据

- API：`sha256:18e73b6eed03565b683751405b8d1a8b6dcba1732743b08ed4cc7d0ea3c70ad0`。
- Worker：`sha256:914ad6bb5e4250521bd9a0d71968f56e88dd46cd675d00032301850972618f7c`。
- schema仍为0087，无生产迁移；候选真实配置/关闭开关保持。2026-09-23 16:57:05+08观察两服务healthy、restart0、日志Traceback/maintenance_failed计数0，其他21容器ID和启动时间不变。
- 发布前另留`database-precutover.dump`，格式可读取；更早备份已完成隔离恢复比对。所有备份、原镜像/Compose回退及静态备份留服务器0700发布目录 `/opt/starchat/releases/support-feedback-20260923`，未下载敏感数据。
- 发布日志：`artifacts/2026-09-23/support-feedback/release/`；安装、签名和重建记录：`artifacts/2026-09-23/support-feedback/android-2162/`。未更新官网正式分发或iOS，未推送Git远端。
- 原全量的1条Starlette/httpx弃用提示和Getui旧Pydantic配置提示来自既有依赖，未在本批更换依赖；67个skip保留原执行口径，专门执行的PG场景另列，不将全部skip视为已验。
- 整合时发现main的共享OpenAPI又包含其他任务的帧诊断改动，仅将本批候选OpenAPI内容入索引提交，工作区中的帧诊断差异保留。未覆盖或发布该并行改动。
