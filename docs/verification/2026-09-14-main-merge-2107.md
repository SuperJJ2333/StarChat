# 2026-09-14 main 合并后 Mi 6 Debug 2107 交付验证

## 范围

- 将 `codex/finance-history-2103-20260913`（已含 history-latency、media-interactions）合并入 main（merge `1001a6fe`），随后补齐合并修复（`07d88973`：账单/收款 identityCache、拍一拍限流改为按房间静态配额、私聊标题官方客服角标接线）。
- 恢复合并前暂存的在途工作流并落地提交（`628be215`：support identity、manual deposit cases、wallet conversion、admin 支持面板、ADR-0069/0070、迁移 0065/0066）。`docs/verification/archives/` 下约 12 GiB 本地 ZIP/APK 证据不入库（.gitignore 排除，文件保留本机）。
- 全量 Flutter 测试 2662 通过（含拍一拍重进限流、转账收款单、私聊标题角标用例）。
- origin/main 已推送至 `628be215`（GitHub 直连；本机代理 127.0.0.1:7897 TLS 中断，已绕过）。

## 构建（遵循 docs/runbooks/android-apk-rebuild.md）

- Flutter ARM64 debug：`--flavor standard --target-platform android-arm64 --build-name 0.3.88-debug --build-number 2107`，三项 HTTPS dart-define（BUSINESS_API/MATRIX_HOMESERVER/GETUI 均为 https://liuhetong888.com），`--android-project-arg chatflowParallelDebug=false`。
- Apktool 2.12.1 解包/重建；build-tools 36.0.0 `zipalign -P 16 -f 4`；固定身份签名（证书 SHA256 `75b31c66476cd8e2c9319551b49405a1de1e5c23e9a0dbdcc9eb76b52ba61fff`）。
- 验证：清单语义一致、zipalign 复检通过、aapt badging `com.liuhetong.mobile` / versionCode 2107 / versionName 0.3.88-debug / application-debuggable / arm64-v8a、apksigner 固定证书通过。
- SHA256：源 `D675EAE5415E94EABA1EDB51793E40E1E20D0D977A7373B7C7692BC2713C2DE3`；最终重建 `E0342EC4CB7EACE5FFCD05C164E98DC20939556284321A5C3CC2C228147E64FC`。

## Mi 6（cbd0156b）安装

- MIUI 拦截 shell 安装（INSTALL_FAILED_USER_RESTRICTED），使用 Magisk root `pm install -r -t` 完成 2105→2106 同款保留数据覆盖安装到 2107；未卸载、未清数据。
- 读取已安装版本 `versionCode=2107 / versionName=0.3.88-debug`；拉回 base.apk SHA256 与最终重建包一致（`mi6-installed-2107.apk`）。
- 已通过 LAUNCHER intent 启动应用。

## 环境备注

- `~/.gradle/gradle.properties` 中遗留 `chatflowParallelDebug=true` 曾导致误产出 `com.liuhetong.mobile.debug` 平行包；已改回 false 并删除该误装包。
- `.worktrees` 六个已合并工作树已删除（个别空目录被句柄占用暂留）。
- 其余说明：本机代理至 GitHub TLS 中断，推送改走直连。
