# docs/verification — 验证证据结构与保留策略

**status:** Current；**owner:** 项目维护者（当次任务负责人执行，用户批准提前清理）；**last_verified:** 2026-09-10（策略澄清，不代表所有工件已备份）。[文档导航](../README.md)

## 现行保留与归档规则（优先于下方历史正文）

2026-09-10 补充授权：用户明确要求同哈希安装包去重、核验后删除构建中间包、重新检查当天完成任务目录。已证明 SHA256 相同的副本和 ZIP 有效载荷完全等同于已验签最终包的 unsigned/aligned 中间包可直接移除；保留包、完整哈希及旧路径映射见[本轮清理记录](2026-09-10-docs-package-cleanup.md)。内容不同的 source.apk 继续作为构建基线保留。当天生成目录仍按无损归档门禁处理，不因日期统一排除。

保留期限继续采用 2026-09-03 的默认 **30 / 90 天**，本次不缩短默认期限。下方旧表与执行约定完整保留用于追溯；冲突之处以本节为准。

| 角色 | 默认规则 |
|---|---|
| 财务、账本、红包对账、审计、E2EE 边界证据 | 永久保留；不得因期限到期或里程碑上线删除唯一内容 |
| 发布、回滚记录、最终包与关键诊断样本 | 随发布归档至产品下线；原包、源码基线、哈希、签名、工具、参数、日志和恢复说明成套保存 |
| UI 截图 / Figma QA / 录屏 | 至少 90 天且对应里程碑已上线，两条件均满足后再评估；同名或标有 final 不能证明重复，须人工核对页面、设备、账号和状态 |
| 代码快照、baseline / rollback、解包导出及缓存 | 默认至少 30 天；到期仅触发评估，不自动删除。Git 不一定包含忽略或未提交文件，须逐文件核对唯一内容及恢复来源 |
| 一次性脚本 | 跟随所属主题；被现行 runbook 调用的脚本属于活跃工具，保留可用路径，提取至 scripts 并同步引用后才可处理旧副本 |

### 执行顺序与例外

1. 当次负责人列出精确路径、角色、日期、关联报告、源码基线、依赖与保留类别，检查活动任务及引用；不使用目录名或扩展名直接推断可删。结论报告 `.md` 持续保留。
2. 未满 30 / 90 天的提前清理必须获得用户明确批准并记录范围。此次用户于 2026-09-10 批准[执行计划](../superpowers/plans/2026-09-10-docs-cleanup-execution.md)中的提前清理；不是对未来工件的持续授权。
3. 对拟移除的散文件先在 `docs/verification/archives/<日期>-<主题>/`（或经批准的受控库）无损归档，记录逐文件 SHA256、相对路径、大小和归档地址；重新读取归档每个成员核验哈希，检查原目录未变化，才可删除对应散文件。归档或复核失败则保留原目录。
4. 保留归档 manifest、恢复说明与核验结果。唯一源码/回滚快照还需验证可恢复性；被忽略的 `source.zip` 或 branch-consolidation 备份不能仅凭 Git bundle 声称已恢复。长期财务 / E2EE / 发布证据即使归档也保留全部字节。
5. 本次 `archives/2026-09-10-docs-cleanup/` 是**本地受控归档，不是异地备份**。已有唯一备份仅登记长期归档；没有外部备份目的地和恢复验证，不能宣称具备异地容灾或删除唯一副本。
6. 每批清理的日期报告记录用户授权、精确清单、归档哈希、恢复验证、删除结果、空间统计及未解决事项。类别不明则保留；不要将原始混合主题整目录视为缓存。

新增目录：`archives/<日期>-<主题>/` 保存归档与 manifest；`artifacts/<日期>/<主题>/` 继续保存当次原始产物。2026-08-24 和 2026-08-28 的历史候选截至 2026-09-10 分别仅 17 / 13 天，尚未达到默认 30 天，必须按本次批准的例外及归档门禁处理。

## 2026-09-03 原始策略与目录说明（Historical）

## 目录结构

```text
docs/verification/
├── <YYYY-MM-DD>-<topic>.md            # 单次验证的结论报告（日期前缀命名）
├── README.md                          # 本文件
├── screenshots/                       # 手工 UI 验证截图（按主题分子目录）
└── artifacts/
    └── <YYYY-MM-DD>/
        └── <topic>/                   # 当日某主题的原始产物（截图、日志、导出等）
```

## 放置规则（来自 AGENTS.md，重申）

1. 临时验证产物**只能**放在 `docs/verification/artifacts/<YYYY-MM-DD>/`（或 `docs/verification/` 下其他具名子目录）；仓库根目录不得出现 `MODIFIED_FILE`、`DIFF_FILE`、`VERIFICATION.txt`、`ROLLBACK.sh` 等验证产物。
2. 报告（`.md`）放 `docs/verification/` 根并用日期-主题前缀命名；原始大文件放对应 `artifacts/<日期>/<主题>/`。
3. 禁止在 `docs/verification` 之外（例如演示站或 app 目录内）建立平行的 verification 目录。

## 保留策略（2026-09-03 制定）

| 证据类型 | 保留期限 | 说明 |
|---|---|---|
| 财务/账本/红包对账、审计、E2EE 边界类验证证据 | **永久** | 属合规与审计链条，禁止清理 |
| 发布/回滚操作记录（ROLLBACK.sh、发布脚本副本、APK 审计） | 随发布归档，保留至产品下线 | 每次发布保留一份即可，重复的中间版本可清 |
| UI 截图 / Figma QA / 演示录屏等大体积媒体 | 自日期起 **90 天**，对应里程碑上线后即可清理 | 同一主题的多份重复截图只保留最终版 |
| 代码快照类证据（baseline/rollback 整目录拷贝、APK 解包 res 导出） | 自日期起 **30 天** | 仅供当次比对，git 历史可回溯，过期即清 |
| 一次性调试脚本（`*.ps1`/`*.py` 临时 dump） | 随所属主题证据一同清理 | 不作为长期工具维护；长期工具应上移 `scripts/` |

执行约定：

- 清理只删除 `artifacts/` 下的原始产物，**不删除**结论报告 `.md`（报告是证据索引）。
- 清理财务/审计/发布类证据前必须人工确认类别；拿不准时保留。
- 现存已知的清理候选（按上表口径）：`artifacts/2026-08-24/public-apk-audit-1/`（APK 整包 res 导出，代码快照类）、`artifacts/2026-08-28/admin-modernization/MODIFIED_FILE/`（整树快照，代码快照类）中与 git 历史重复的部分。
- 每次大批量清理在 `docs/verification/` 下新增一条日期前缀的清理记录（列出被删主题与理由）。
