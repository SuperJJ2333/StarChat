<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
		APNs 推送 entitlement 模板（不直接生效）。

		激活步骤（需要 Apple Developer 账号与推送证书/密钥）：
		1. Apple Developer → Identifiers → App ID 开启 Push Notifications。
		2. 生成 APNs Auth Key（.p8，推荐）或证书；密钥只存放在服务器
		   Sygnal 配置中，绝不提交进仓库。
		3. 复制本文件为 ios/Runner/Runner.entitlements（或合并既有文件），
		   aps-environment 按构建目标选择：
		     - development：Xcode 开发构建
		     - production：TestFlight / App Store
		   两种环境可用不同 configuration 区分。
		4. Xcode 项目 → Runner target → Signing & Capabilities：
		   确认 "Code Sign Entitlements" 指向该文件并勾选
		   Push Notifications capability。
		5. 来电（VoIP）需要 PushKit + CallKit 与 com.apple.developer.usernotifications
		   时间敏感通知等额外能力，属独立后续工作（docs/PUSH_SETUP.md §来电）。
	-->
	<key>aps-environment</key>
	<string>development</string>
</dict>
</plist>
