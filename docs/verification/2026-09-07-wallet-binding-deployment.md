# 钱包资金关闭版本：生产部署证据

部署时间：2026-09-07 05:48:47 UTC（香港时间 13:48:47）。发布成功，生产管理员登录后的页面验收尚未完成；不可将本记录理解为真实充提启用验收。

后续验收：用户已确认两笔链上记录及各自详情正常显示，生产页面人工验收通过；自动化浏览器逐笔验收仍受连接问题限制。0.3.50 正式更新随后发布，见[正式更新证据](2026-09-07-official-wallet-update.md)。真实充提仍未启用。

## 发布及恢复

- 发布源包 SHA256：`0c078e29d39f4d5ba609487302ff05d53a208d9979c7fac29598e9f24da1d093`。
- API 镜像：`sha256:11c037b1e657cd3e9e598e9b583891610fc48f5d5f977a9d068ac00eccb27a25`。
- Worker 镜像：`sha256:397748326c643baafc0ffc3b43f7b37075de73175e1d1ce39e3c42d8e135769f`。
- 生产迁移：`0041_wallet_operations` → `0042_wallet_binding`。
- 私有备份：服务器 `/opt/starchat-backups/binding-20260907T054044Z-f6fedf`；包含数据库、配置、旧镜像信息及原前端。数据库和凭据未下载。
- 使用生产同一 PostgreSQL 16 镜像，在无外部网络的容器中恢复备份，迁移后账本记录数未变；38 项数据库、并发、权限及只读查询检查通过。
- 新旧 API/Worker 启动检查通过。旧应用保留 0042 结构，挂载独立新版预检的回退组合已验证；Worker 隔离启动使用空测试库，避免处理复制的真实业务任务。
- 发布脚本经领域和安全审阅，跟踪迁移容器、核对实际迁移版本、保护配置哈希；失败时保留扩展数据并恢复前端。

## 线上核验

- API、Worker、原 TronGrid 观察器健康；其他容器身份未变化。
- 预检为 `RELEASE_READY_FUNDS_DISABLED`；观察目录只读挂载。
- 公网 readiness HTTP 200；未登录 summary/list HTTP 401。
- 三个后台模块公网内容哈希与发布源一致。
- 生产 API 内只读查询能读取现有 2 笔事件及详情，观察器状态 OK，对账为 `SOURCE_MATCHED`。核验时观察成功记录约 11 秒前，扫描检查点约 122 秒前。
- 数据来自 TronGrid 单一来源，`coverage_complete=false`、`financial_writes_enabled=false`；不能宣称全历史完整、独立链上最终性已验证或用户已自动入账。

## 待完成与操作边界

用户选择内置浏览器登录生产后台。工具能确认后台标签标题和 URL，但页面读取连续超时，尚未获取已登录会话完成逐笔 UI 验收。未创建生产测试身份或提取用户令牌。

真实绑定依赖、客户端签名、充值归属入账、提现目标/费用快照及独立签名等仍按运行手册保持未完成，资金入口不开放。本次没有构建或替换 MI 6 APK。

机器可读证据位于 `artifacts/2026-09-07/wallet-binding-deployment/`：`prepared.json`、`deployed.json`、`postdeploy.json`；核验程序为 `postdeploy_check.py`。详细诊断及数据库备份仅保存在服务器私有目录。
