# UI 评审验证记录 — 通讯录四项视觉修正（2026-09-18）

**范围：** `apps/mobile_flutter` 通讯录相关界面（好友列表、通讯录顶部入口、「新的朋友」请求行、「通过朋友验证」页）+ HTML 设计演示 `frontend/`。
**不属于本次范围：** 业务逻辑、网络/权限语义、Matrix 协议、账本与钱包；`lib/features/matrix/**`、`lib/core/**` 未改动（仅同步了两条断言实现细节的既有测试，见 §6）。
**视觉源：** `frontend/` 下的 HTML demo。**Figma 已退役：本次变更仅更新 HTML demo（`frontend/index.html` → `?module=contacts`、`?screen=contacts-verify-pending|added|rejected`、`?screen=foundation-components-catalog`）。**

---

## 1. 改动清单（需求 → 文件:行）

### 需求 1：好友列表背景色 + 好友之间的渐隐分割线（比朋友圈实心分割线更低不透明度、两端渐隐到透明）

| 位置 | 关键内容 |
|---|---|
| `apps/mobile_flutter/lib/ui/foundation/wechat_tokens.dart:113` | 新增 `WeChatDividerTokens`：`hairline=1.0`、`edgeStartStop=0.0`、`coreStart=0.18`、`coreEnd=0.82`、`edgeEndStop=1.0`、`edgeAlpha=0.0`、`centerAlpha=0.5` |
| `apps/mobile_flutter/lib/ui/components/wechat_gradient_divider.dart:14` | **新建共享组件** `WeChatGradientDivider`：`LinearGradient(centerLeft→centerRight, [edge, core, core, edge])`，色源 `WeChatColors.resolve(context, WeChatColors.divider)`（浅 `#D9D9D9` / 深 `#2C2C2C`），无硬编码颜色 |
| `apps/mobile_flutter/lib/ui/components/wechat_contact_tile.dart:43` | 好友行新增 `ColoredBox(key: 'wechat-contact-elevated-surface', color: WeChatColors.elevatedSurface(context))`（浅 `#FFFFFF` / 深 `#232323`，`UI_DESIGN.md` §2）|
| `apps/mobile_flutter/lib/ui/components/wechat_contact_tile.dart:78` | 行底 `WeChatGradientDivider(key: 'wechat-contact-divider')`；行高仍恒为 `contactTileHeight`（56dp），分割线画在行内、不改变行高，字母索引偏移量不受影响 |
| `apps/mobile_flutter/lib/features/contacts/contacts_page.dart:459` | 分组内 `showDivider: i < grouped[label]!.length - 1`：只在好友之间画线，组末由分组标题分隔 |
| `apps/mobile_flutter/lib/ui/components/wechat_list_tile.dart:122` | 入口行同样使用共享分割线（`showDivider: true`） |

### 需求 2：通讯录顶部「新的朋友 / 群聊 / 标签」文字与 icon 对齐

根因（读 Flutter 源码确认）：`WeChatListTile` 委托 `CupertinoListTile`，其内容列是 `Column(mainAxisAlignment: MainAxisAlignment.spaceBetween)`（`packages/flutter/lib/src/cupertino/list_tile.dart:339-357`），在固定高度行里把单行标题推到行顶；同时 leading 固定 28dp 盒子把 40dp 头像压扁。

| 位置 | 关键内容 |
|---|---|
| `apps/mobile_flutter/lib/ui/components/wechat_list_tile.dart:70-130` | 改为显式 `Stack(alignment: center)` + `Row(crossAxisAlignment: center)` + `Column(mainAxisSize: min, mainAxisAlignment: center)`：任意行高下 leading 与文案共用同一垂直中心 |
| `apps/mobile_flutter/lib/ui/components/wechat_list_tile.dart:22-42` | 新增 `showDivider`（默认 false）、`leadingSize`（默认 28）、`leadingToTitle`（默认 `WeChatSpacing.lg`） |
| `apps/mobile_flutter/lib/features/contacts/contacts_page.dart:363,412,424` | 三个入口行传 `leadingSize: WeChatDimensions.contactAvatar`(40) + `leadingToTitle: WeChatSpacing.md`(12)：与好友行共用 68dp 文案水平网格（`16 + 40 + 12`），与 demo `.c-list-tile` 的 `--size-avatar-message`+`--space-md` 一致 |

### 需求 3：「新的朋友」请求行错位（昵称贴顶、招呼贴底）

| 位置 | 关键内容 |
|---|---|
| `apps/mobile_flutter/lib/features/contacts/contacts_page.dart:1883-1931` | `_FriendRequestTile` 去掉 `Center`+`CupertinoListTile` 组合，直接用 `WeChatListTile(leadingSize: contactAvatar, leadingToTitle: md, showDivider: true)`；昵称与招呼内容作为整体与 40dp 头像垂直居中，行底加共享分割线 |
| `apps/mobile_flutter/lib/features/contacts/contacts_page.dart:1931` | 行高常量 `_friendRequestTileHeight = 68.0`（与 demo 请求行一致） |

### 需求 4：「通过朋友验证」页

| 位置 | 关键内容 |
|---|---|
| `apps/mobile_flutter/lib/features/contacts/friend_request_review_page.dart:176-196` | 「打开聊天」改为品牌填充按钮：`color: WeChatColors.brandPrimary`、白字 16sp、`WeChatRadius.dialog` 圆角、高 48dp、`busy` 时 `CupertinoActivityIndicator`（disabled/loading 可见） |
| `apps/mobile_flutter/lib/features/contacts/friend_request_review_page.dart:146-174` | 状态行改为徽标：`已添加` 用 `WeChatColors.brandTint` 淡底 + `brandPrimary` 粗体；其余状态用 `CupertinoColors.tertiarySystemFill.resolveFrom(context)` + `textSecondary`，深浅色都按主题解析 |
| `apps/mobile_flutter/lib/ui/foundation/wechat_tokens.dart:26` | 新增 token `brandTint = Color(0x1F07C160)`（≈12% alpha，深浅底都可读） |

## 2. Token / 共享组件 / registry / HTML demo 改动位置

**新增 token（Flutter ↔ HTML 同名同值）**

| Flutter | HTML (`frontend/src/styles/tokens.css`) |
|---|---|
| `WeChatDividerTokens.hairline = 1.0` | `--divider-fade` 渐变高度 1px（`:252`、`:263`） |
| `WeChatDividerTokens.centerAlpha = 0.5` | `--divider-fade-alpha: 0.5`（`:137`） |
| `WeChatDividerTokens.edgeAlpha = 0.0` | `--divider-fade-edge-alpha: 0`（`:138`） |
| `WeChatDividerTokens.coreStart = 0.18` | `--divider-fade-core-start: 18%`（`:139`） |
| `WeChatDividerTokens.coreEnd = 0.82` | `--divider-fade-core-end: 82%`（`:140`） |
| `WeChatColors.brandTint = Color(0x1F07C160)` | `--color-brand-tint: rgb(7 193 96 / 12%)`（`:143`） |

渐变本体：`.ui-screen { --divider-fade: linear-gradient(90deg, rgb(217 217 217 / edge) 0%, ...) }`（浅色）与 `.ui-screen[data-theme="dark"]`（`#2c2c2c`，同样 alpha/stops）。

**共享组件（新建，全局统一使用）**
- Flutter：`apps/mobile_flutter/lib/ui/components/wechat_gradient_divider.dart` → `WeChatGradientDivider(height, indent, endIndent)`
- HTML：`frontend/src/components/divider.js` → `app-divider`（root class `.c-gradient-divider`，`frontend/src/styles/components.css:221`）
- 使用点（HTML）：`.c-list-tile`（`components.css:204-213`）与 `.c-contact-row`（`primitives.css:409-421`）由实心 `border-bottom: 1px solid var(--color-divider)` 改为 `background-image: var(--divider-fade)` + `background-size: 100% var(--size-hairline)`，行高不变

**Registry**（`packages/ui-contracts/changliao-component-registry.json`）
- 新组件 `gradient-divider`：`flutter.name=WeChatGradientDivider`、`file=apps/mobile_flutter/lib/ui/components/wechat_gradient_divider.dart`、props `height/indent/endIndent`、variants `theme[light,dark]/width[full-bleed,indented]`、states `default`、html tag `app-gradient-divider`、tokens `dividerHairline/dividerCenterAlpha/dividerEdgeAlpha/dividerCoreStart/dividerCoreEnd/divider`
- `contact-tile` 更新：props += `showDivider`；variants `background[surfaceElevated-light, surfaceElevated-dark]`、`divider[gradient,none]`；states `default/pressed/disabled`
- `tokenMappings` 新增分组 `dividers`（7 项）；`colors` += `brandTint`
- `tokenParity` 新增 5 条（分割线 4 项 + `brandTint`）
- `screens.expectedCount` 369 → **372**（新增 `contacts-verify-pending|added|rejected`）
- 新增修订块 `contactsUiReview20260918`（需求、token、组件、screen 与验证摘要）
- 同步更新 `tests/mobile/test_ui_component_registry.py`（expectedCount 372、PASS 文案 32 components）

**HTML demo**
- `frontend/src/catalog/contracts.js:32` 新增 `app-gradient-divider` 契约
- `frontend/src/components/register.js` 注册 `AppDivider`
- `frontend/src/catalog/screens.js:129` 注册 3 个新 frame：`contacts-verify-pending` / `contacts-verify-added` / `contacts-verify-rejected`
- `frontend/src/screens/contacts.js:109-141` 新增「通过朋友验证」renderer（头像/昵称/打招呼卡/品牌填充动作/状态徽标）
- `frontend/src/styles/primitives.css:456-535` 新增该页样式（`.c-verify-action`、`.c-verify-status`、`.p-contacts-verify__content`）
- 新测试 `frontend/tests/gradient-divider.test.mjs`（5 条：token 两端透明/中部低 alpha、行表面不再用实心边框、与 Flutter token 对齐、`app-gradient-divider` 契约与注册、3 个新 frame 已登记）

## 3. 红 → 绿证据

**红 1（行为级，先写测试）** `flutter test test/features/contacts/contacts_ui_review_20260918_test.dart`

```
00:00 +0: 需求2 通讯录入口行文字与 icon 垂直居中且与好友行同一水平网格
Expected: a value less than <1>   Actual: <16.0>       （实测差值 16.0dp）
00:00 +0 -1: 需求1 好友列表使用 surfaceElevated 背景色并按组画渐隐分割线
Expected: exactly 3 matching candidates
  Actual: Found 0 widgets with key [<'wechat-contact-elevated-surface'>]
00:00 +0 -3: 需求3 新的朋友请求行昵称与招呼内容都与头像垂直对齐
Expected: <40.0>   Actual: <28.0>                     （头像被 28dp leading 盒子压扁）
00:00 +0 -4: 需求4 通过朋友验证页打开聊天有品牌底色，已添加有醒目文字背景
Expected: Color:<brandPrimary>   Actual: <null>        （「打开聊天」无背景色）
00:00 +0 -5: Some tests failed.
```

**红 2（共享组件缺失，编译期红）** `flutter test test/ui/wechat_gradient_divider_test.dart`

```
Error: Undefined name 'WeChatDividerTokens'.        （×7）
Error: Method not found: 'WeChatGradientDivider'.   （×6）
Error: No named parameter with the name 'showDivider'.（×2）
00:00 +0 -1: Some tests failed.
```

**绿 1** `flutter test test/features/contacts/contacts_ui_review_20260918_test.dart test/ui/wechat_gradient_divider_test.dart --timeout 120s`

```
00:00 +10: All tests passed!
```

**绿 2（受影响既有套件）** `flutter test test/features/contacts test/ui --timeout 120s`

```
00:34 +608: All tests passed!
```

**绿 3（全量）** `flutter test --timeout 120s` → `02:42 +3231: All tests passed!`
（首次全量运行出现 2 处既有断言失败，仅断言 `CupertinoListTile` 这一实现细节，已按“新行为是刻意的”更新，见 §6；更新后全绿。）

**绿 4（HTML demo 真实浏览器）**
- `node docs/verification/artifacts/2026-09-18/demo-contacts-check.mjs` → `Browser demo check: PASS (contacts frames 27, verify pending/added/rejected, shared app-gradient-divider)`
- `pwsh -NoProfile -File docs/verification/artifacts/2026-09-18/divider-pixel-check.ps1` → 截屏 `?screen=contacts-index-default&capture=1` 后逐行扫描像素：

```
image=393x1200 visibleRows=5 fadedEndRows=5 solidEndRows=0
  y midDelta edgeDelta maxDelta rowBackground
159    18.00      1.00    18.00 #FEFEFE
215    18.00      1.00    18.00 #FEFEFE
271    18.00      1.00    18.00 #FEFEFE
408    18.00      1.00    18.00 #FEFEFE
570    18.00      1.00    18.00 #FEFEFE
Divider pixel check: PASS (faded divider rows=5, max mid contrast=18, solid divider contrast=38)
```

即：分割线中部对比度 18 ≈ `0.5 × 38`（低于实心 `#d9d9d9` on `#ffffff` 的 38），左右端点对比度 1（≈透明），三个入口行（y=159/215/271，间距正好 56dp）与好友行都符合。

## 4. 门禁结果（真实输出）

| 命令 | 结果 |
|---|---|
| `cd apps/mobile_flutter && flutter analyze lib test` | `No issues found! (ran in 6.6s)`，exit 0 |
| `flutter test --timeout 120s`（全量） | `+3231: All tests passed!`，exit 0 |
| `flutter test test/features/contacts test/ui` | `+608: All tests passed!` |
| `python scripts/verify_ui_contract.py` | `UI contract drift: PASS (32 components, 372 screens)` |
| `cd frontend && npm test` | `tests 214 / pass 214 / fail 0`（基线 209 → +5 新测试） |
| `frontend` 浏览器 demo 校验（自建脚本，见 §3 绿 4） | PASS |
| `pwsh -NoProfile -File scripts/verify.ps1` | **在 `tests/mobile` 步骤失败（与本改动无关，见下）**；其余步骤结果见下 |
| ├ 已执行并通过 | Repository/Deployment policy、Template unit、Render smoke、Infra（`tests/infra`）、Getui bridge、Matrix Bot、Business API+Worker（`2067 passed, 58 skipped`）、Business API import smoke、Python AST、Alembic（单一 head，offline upgrade OK）、OpenAPI drift、Docker Compose render（exit 0） |
| └ 失败 | `tests/mobile`：`test_android_ci_workflow.py::test_android_ci_workflow_exists_and_pins_flutter` — `assert "run: flutter test" in ci` 失败 |

**非本任务失败的责任范围（附复现证据）**
- `.github/workflows/android-ci.yml` 当前用 `run: |` + `flutter test 2>&1 | tee "$log"`（第 95-105 行），不再包含字面量 `run: flutter test`。
- `git status --porcelain -- .github` 为空（工作区未修改该文件），`git status --porcelain -- tests/mobile/test_android_ci_workflow.py` 为空（测试文件也未修改）；该文件最后一次提交是 `bd28dbff 2026-09-18 ci(flutter): diagnose the real failing test instead of guessing from log noise`。
- 单独复现：`py -3.12 -m pytest tests/mobile/test_android_ci_workflow.py -q` → `1 failed, 8 passed`（失败点只读该未修改文件，与本次 UI 改动无交集）。
- 由于 `verify.ps1` 在首个失败步骤抛错，其后的步骤未在脚本内执行；已逐条手工补跑，全部通过（UI contract PASS、Business API import PASS、Alembic 1 head + offline upgrade exit 0、OpenAPI PASS、`docker compose config --quiet` exit 0）。

**`npm run test:browser`（非本次要求门禁，附加自查）**：失败于 `tests/admin-chain-browser.html`（`#result=detail`，详情弹层未在断言时刻出现）。已用**原始 `tokens.css`（`git show HEAD:...`）复跑同一脚本**，失败完全一致 → 该失败与本改动无关，属既有问题；未修改任何相关文件。

## 5. 功能闭环结论

- 本次仅改视觉/布局/对齐/状态呈现，**未改**任何 gateway、路由或状态机：`通过验证/拒绝` 仍只在「通过朋友验证」页点击时经 `_resolve` 调用 `POST /friends/requests/{id}/accept|reject`；`打开聊天` 仍调用 `_openAcceptedRequest`（先 `GET /friends` 校验当前好友关系权威，再建立/取得加密私聊并发送接受招呼）。
- 已有闭环测试继续通过（全量 `flutter test` 3231 全绿，含 `test/features/contacts/contact_flow_test.dart`、`friend_acceptance_retry_test.dart`、`test/features/matrix/direct_chat_entry_test.dart`（真实网关注入路径））。
- 状态可见性：请求页 loading/success/failure/empty/disabled 未改动；本次新增的可见状态为「已添加」徽标（`brandTint` + `brandPrimary` 粗体）与「打开聊天」的品牌填充 + `busy` 加载态 + `onPressed: null` 禁用态。
- 视觉一致性：好友行、请求行、入口行、HTML demo 的 `.c-list-tile` / `.c-contact-row` 全部使用同一共享渐隐分割线与 `surfaceElevated` 背景。

## 6. 更新过的既有测试（逐条原因）

1. `apps/mobile_flutter/test/features/matrix/direct_chat_info_test.dart`（`聊天信息页「清空聊天记录」文字居中`）
   原断言用 `find.ancestor(of: label, matching: find.byType(CupertinoListTile))` 取行容器。本次为修正「文字与 icon 不对齐」刻意让 `WeChatListTile` 不再委托 `CupertinoListTile`（其 `spaceBetween` 内容列是错位根因），因此改为 `find.byType(WeChatListTile)`。
   **行为断言未放松**：`(label 中心 x − 行中心 x).abs() < 1.0`（文字必须相对行居中）原样保留并通过 —— 说明居中行为本身未被破坏。
2. `apps/mobile_flutter/test/features/matrix/group_chat_info_test.dart`（同名用例）：同上，仅替换行容器查找类型，居中阈值断言不变。
3. `tests/mobile/test_ui_component_registry.py`：`screens.expectedCount` 369 → 372、以及 `verify()` 的 PASS 文案 `(32 components, 372 screens)` —— 因为本次刻意新增 3 个 HTML demo frame（`contacts-verify-*`）与 1 个组件（`gradient-divider`）。

未删除任何断言；未修改 `lib/features/matrix/**`、`lib/core/**`。

## 7. 未完成 / 待确认

- `npm run test:browser` 的既有失败（admin 链上流水详情弹层）不在本次范围，未修复；如需处理应另立任务。
- `verify.ps1` 的既有失败（`android-ci.yml` 与 `test_android_ci_workflow.py` 漂移，最后一次相关提交 `bd28dbff`）不在本次范围，未修复。
- HTML demo 的深色效果仅通过 token 定义（`.ui-screen[data-theme="dark"]`）与 Flutter 深色 widget 测试覆盖；深色截屏未逐像素校验（控制台字体/渲染差异会引入噪声），如需可作为后续补充。
