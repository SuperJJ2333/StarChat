# 来电名称、通知合并、视频体验及 MI 6 调试安装

执行用户明确需求：备注/昵称优先；系统通知最多三个；未授权提示；视频压缩及首帧封面；进度条、倍速与图片同款下载/转发；转发统一选择聊天页；构建 debug 并安装到 MI 6（cbd0156b）。沿用本会话先功能验证、暂缓 Figma 的授权。

拥有文件：来电显示名解析和 app_home 装配、call_ui_manager；notification 下渠道定义/权限提示和相应 Dart/Android 渠道创建点；video_transcode、media_message_service、video_poster_extractor、room_page、encrypted_media_view、wechat_video_message 及对应测试。保留之前未提交的修复，不修改统计助手 HTML。

1. 复现解析器冷启动装配、渠道过多和视频文件绕过压缩/封面问题，测试先红后绿。
2. 同步复用已创建的名称解析器，安全用户名兜底；三渠道复用现有消息/铃声/静默 ID，删除明确废弃渠道；缺权限持续显示可操作提示，不自动替用户授权。
3. 相册、拍摄和文件共用压缩策略，优先 480p、24fps，大文件降档；首帧优先，损坏帧才回退。视频下载保存系统相册；图片与视频转发复用现有选择聊天路由。
4. 聚焦测试、静态分析、项目验证及源码复核；保留总验证已有通话源码检查失败证据。
5. 构建无额外保护的 arm64 debug APK，校验设备和签名，保留数据覆盖安装并启动到 MI 6；若签名不兼容则不卸载清数据，记录实际阻碍。
