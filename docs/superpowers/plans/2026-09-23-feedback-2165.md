# 2165反馈修复计划

用户2026-09-23六项反馈授权修复既有功能，沿用既有相册/缓存/组件与Android交付边界；不重设计产品，不自动删除历史公告，不发布iOS。基线9cfcd00c，隔离候选 .worktrees/mobile-feedback-2165；根目录三个新未追踪JSON属于其他任务，保留。

1. F1：旧公告沿实际旧写入格式、SDK取事件/密钥恢复分类追踪。可兼容数据优先兼容；确认不可读时允许有权限用户明确重新发布/替换，不自动删除旧历史或发送明文。实际缺密钥若无持钥设备不得伪称恢复。
2. F2：动态表情与普通消息统一群主/管理员头衔渲染与权限来源。
3. F3：聊天信息页测量入页请求与同步加载，复用房间/账户缓存先显示本地快照，后台刷新；保留成员变更失效与权限实时校验。
4. F4：朋友圈GIF与已正常的评论GIF链路对照，保持原始动画数据及共享缓存。
5. F5：用真实Flutter请求序列验证后端Pydantic及媒体上传/草稿/发表，不以mock绕开契约；修复视频封面，限制20MiB不变。
6. F6：核对真实短信模板与APP文案的区别；不把页面品牌当成短信正文修改。仅在供应商支持且模板审核通过时切换实际模板，不伪造通过。

分工：legacy_announcement_fix负责F1；announcement_retry负责F2/F3；moments_live_fix负责F4/F5；主代理负责F6、HTML/registry、真实服务配置核对、整合/验收/交付。各代理先红后绿、独立文件所有权，主代理规格/质量安全复审。修复完成执行适用完整门禁并保存失败、skip、delta；完整端到端契约先于长门禁。任何实际安装/部署需写清版本与证据；SMS审核缺口不能阻塞其他修复。
