# 其余优化：按现有实现修订的增量执行计划

授权：用户于 2026-09-23 要求执行其余优化。原专项是排期草案；本计划落实可直接执行的增量，并保留受保护设计的审批边界。工作树 `.worktrees/remaining-opt`，基线 `7140ace247d1874fea0285914c5696e3c0212370`，继承已验证红包查询过滤改动。

## 批次与文件所有权

1. 图片：朋友圈列表按逻辑尺寸与 DPR 限制解码，保持磁盘缓存、账户隔离、重试和原图查看行为。所有权 `wechat_moment_image_grid.dart`、新增 thumbnail helper、相关 moments 图片测试。先验证原实现缺少尺寸限制，再实现并回归。
2. SDK 上传：主文件已成功而缩略图暂时失败时，仅重试缩略图；维持加密描述、事务 ID、事件单次发送。所有权 `third_party/matrix/lib/src/room.dart`、`CHATFLOW_PATCH.md`、`content_addressed_media_test.dart`。先验证主文件重复上传的失败测试。
3. 帧预算诊断：在现有有界、限流、无内容遥测中补充聚合帧预算统计；保留 250ms 严重卡顿事件，不将管线总时长冒充掉帧。所有权 diagnostics 客户端/服务端及其测试、OpenAPI。接口兼容和旧服务端行为需先验证。
4. 安全与容灾：形成具体受保护设计和缺口台账，完成领域与质量/安全审查后提交明确审批；不擅自改变 ADR0058 无 TOTP 用户提现策略、ADR0062 单设备策略或 E2EE 密钥保留。
5. 文本 Outbox：将既有三次服务器重试预算和 Retry-After 截止时间持久化；SQLite v1→v2 仅加列，保留旧行与 txid。前台/后台共享策略、CAS 结算与到期认领，失败认领不得当成功，观察超时不计费/不释放未决请求。手动重试可重置本条预算。所有权 core/outbox 的 model/store/manager/scheduler、新 retry policy、room_timeline_controller 及相关五组测试。必须验证真实 SQLite 升级/重启、竞争认领、页面销毁迟到结果、账号隔离、未知结果与三次上限。
6. 门禁夹具修复：完整门禁暴露 `tests/business_api/identity/test_profile_api.py` 在 collection 时固定 NOW，超过15分钟才执行会发出过期令牌；单跑13通过、模拟延迟16分钟红测401确认。仅将测试令牌签发时间改为实际夹具执行时刻，增加回归，生产鉴权不变。该测试文件归主代理。

每批先红后绿；独立文件可并行，Flutter 工具串行。先规格审查，再质量安全审查。全部代码整合后预检并执行一次适用完整门禁，未变输入复用已有证据。未授权生产发布。

## 原 16 批对应关系

| 原批 | 当前事实与处置 |
| --- | --- |
| 1 | 已完成 PostgreSQL 终态红包查询过滤；Redis 异步资金结算未实施，需独立 ADR |
| 2–4 | TOTP/设备/恢复已有部分实现；新增策略、跨域吊销与社交恢复补设计审批 |
| 5–7 | 更新接口和诊断已存在；多入口须同一逻辑 homeserver，重建库需密钥保留证明，补具体设计 |
| 8 | 已有三个缓存域；实施真实缩略图解码缺口，保留现有 TTL/容量 |
| 9 | 聊天已有完整加密下载缓存；不能把未完整校验的加密分段当可播放明文 |
| 10 | 实际 SDK 为 KV 存储，不能套用不存在的事件列索引；清理需独立安全设计 |
| 11 | 已有三次重试预算；已修复上传阶段复用与文本跨重启预算；附件进程重启上传恢复仍为缺口 |
| 12 | 已读聚合需身份去重、成员资格及隐私协议，不实施裸 INCR |
| 13 | 三秒合并与单条零延迟互相矛盾；需明确首条立即、后续窗口策略及可靠投递 |
| 14 | 红包真实 PostgreSQL 基线已完成；千人 E2EE 压测仍需隔离环境和测试账号 |
| 15 | 已有严重卡顿上报；补帧预算聚合 |
| 16 | 原定独立立项；账号授权、翻译明文边界、群机器人 E2EE 权限须分别设计 |

不得将设计完成、已有能力或未执行压测写成新增功能已交付。审计时 ADR 已占用至 0081，集成时主目录又有并行 0082；迁移至 0087，不能采用原草案建议编号。
