# 图片贴边与查看器适配

用户已授权两项图片布局修复及 Mi 6 debug 覆盖测试。改动范围：ContainImageBubble、ImageViewerPage、必要的房间图片接线和对应回归测试。设备实际已安装 0.3.56-debug/2058（含钱包调试代码），因此打包沿用主工作区现有源码，避免用较早正式源码回退钱包功能。只修改两个图片 UI 文件与测试，构建前后核对来源哈希，既有钱包文件不改动。

1. 用真实 4:3、16:9 图片复现收发头像到图片边缘距离；确认未知尺寸占位和实际尺寸的影响。
2. 气泡保持等比完整显示，靠头像一侧对齐。大图使用全视口等比适配，支持双击缩放；不通过拉伸或默认裁切掩盖比例差异。
3. 红/绿测试：收发间距、预览小图全屏适配、旋屏、缩放复位，以及现有 GIF、缓存和下载/转发回归。
4. 按既有规范构建 ARM64 debug、Apktool 重建、校验语义和资产、匹配设备现有 debug 签名、递增版本并 adb install -r 保留数据。

Figma 能力在本任务中未提供，无法同步设计稿；不伪造 Figma 修改记录。此限制记入交付证据，继续完成用户已授权的代码和设备测试。

