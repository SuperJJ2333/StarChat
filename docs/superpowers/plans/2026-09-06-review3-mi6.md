# Third review fixes and Mi 6 debug delivery

User authorized reviewing 81554a9, fixing remaining defects and installing debug on Mi 6.

1. Reproduce pagination loading-state races, cache replacement/invalidation races and disk enumeration failures with tests.
2. Own chat_search_page.dart, video_poster_session_cache.dart, room_page.dart and focused tests. Correct stale handling and isolate poster storage from generic media-cache filename transformations and eviction. Use a session-only encrypted disk store with an in-memory key; no server/E2EE protocol changes.
3. Verify actual image loading preserves animation, including MIME/name mismatch. Preserve full image content.
4. Run focused tests, full Flutter analyze/test and repository verify, recording any unrelated failures accurately. Independent specification and quality review.
5. Increment version above installed Mi 6 versionCode 2043, build standard ARM64 debug with existing HTTPS API/Matrix/Getui settings. Follow android-apk-rebuild.md, preserve signer 75b31c66…fff, verify DEX/manifest/native assets, then adb install -r (no uninstall or data clear), launch and check crashes.

Verification artifacts remain under docs/verification/artifacts/2026-09-06/review3-mi6/.

User explicitly approved using the existing Mi 6 debug identity (SHA256 34999c8b561affc263f11df0a3865e8c03c0386997a8c37bd12110380e5bc1f1) for this debug delivery, preserving data; this is a one-build exception to the standard signer, not a change to release signing policy. The matching local debug key was verified before signing.

Verification portability fixes are included: normalize CRLF for source/policy assertions and wait until the cache loader actually starts in the in-flight eviction test.
