# 后台剩余需求完整交付计划

用户于2026-09-10确认：固化跨会话网络/后台workflow，全部修复剩余五项并部署。沿用已确认的admin-wallet-v2-design，转入做充值补入账，转出做提现核对。钱包60分钟、后台48小时已上线，保留。

## 任务与所有权

1. Root：持久skill、AGENTS路由、SSH/公网验证助手、验收台账；独立worktree避免并行修改。
2. financial implementer：钱包补入账/提现核对应用服务、API、新增迁移、资金测试与ADR。不得编辑main/admin.py/core/config.py/frontend；通过root集成router及配置。不得在生产实际加款或改写原订单事实。
3. Root：点钻流水查询/分类与界面、链上置顶与模态、事故列表筛选/排序/模态、钱包视觉、刷新状态按钮、API集成与查询投影、OpenAPI、HTMLdemo及验收。
4. 独立评审：先需求/领域，再质量安全；针对资金路径PG并发、故障和幂等检查。修复所有阻断。
5. Root：快速迁移/契约检查先行，专项+完整verify与前端检查按独立资源并行；保留日志和耗时，失败分类而非缩减需求。最新生产基线精确增量，备份/隔离恢复/迁移/回退，跳板上传和双端公网验证。

## 验收台账（最终逐项填证据，不以方案冒充实现）

| ID | 验收项 | 状态与证据 |
|---|---|---|
| W1 | 默认跳板、跨会话可发现skill和后台发布workflow | 已固化并用于发布；[工作流](../../runbooks/admin-production-workflow.md)、AGENTS.md、scripts/starchat-server.ps1 |
| L1 | 畅聊号/用户名分列、时间/账号/昵称/邮箱组合筛选 | 已实现并发布；[交付§1](../../verification/2026-09-10-admin-completion.md#1-点钻流水l1l2l3) |
| L2 | 类型场景/分配枚举、中文原因、缺失异常 | 已实现；历史room_id证据不足返回UNKNOWN，不能宣称历史GROUP已可信回填；见交付§1 |
| L3 | 分页、重置、筛选失败/详情/返回状态保留 | 已验证分页/重置/失败保留；本次无账本详情接口，跨模块离开返回状态恢复未验证，见交付§1、§7 |
| A1 | 60分钟模态验证保留，明确刷新当前操作状态 | 已保留并验证；无自动重放、到期销毁敏感模态；见交付§2 |
| C1 | 链上模块置顶，完整详情模态/焦点/分页/滚动保留 | 已实现实际详情字段、关闭列表保留及焦点恢复，真实浏览器PASS；不将草案所有详情扩展字段算已实现，见交付§3 |
| I1 | 事故列表分页排序过滤，模态时间线/影响/关联 | 已实现实际状态/等级/代码/排序与明确关联；复杂时间/用户筛选未实现，时间线截断提示已补；见交付§3、§7 |
| V1 | 视觉层级/金额地址/空加载错误/小屏/HTML demo | 已实现并有HTML真实浏览器验收；390px文档无横向溢出，卡片358px，已补证，见交付§3 |
| F1 | 转入候选/预检/二次确认/执行/查询，权限审计幂等 | 已发布受保护公共API；领域/QS/真实PG通过；未代管理员执行实际资金补录，见交付§4 |
| F2 | 转出提现核对，禁止充值，复用现有结算 | 已实现并验证；SUBMITTED为候选受理，最终结算由既有reconciler完成，见交付§4 |
| X1 | 指定hash复查、原因/影响/修复脚本/预防/自动回归 | 根因证据与新增金融回归已记录；CLI与集成23项通过，独立校时健康；真实付款归属仍需管理员确认，见交付§5 |
| D1 | 最新基线部署、真实容器/恢复/PG与生产证据 | 主任务确认84edda0271…c5d8、0064、10静态上线，隔离恢复PASS/无关容器不变；双端公网与未授权拒绝证据已归档，完整verify PASS，见交付§6 |

本台账与[实际交付记录](../../verification/2026-09-10-admin-completion.md)共同定义已验证范围。历史设计草案的额外能力不得借此台账推定为已实现；导航返回为草案额外能力，未纳入已完成声明；小屏与CLI已补证。

历史问题的真实资金补录必须由页面管理员对准确订单和证据明确确认；交付实现与可执行受保护脚本，禁止为完成验收伪造生产管理员凭证、强制归属或绕过资金控制。新功能遭真实时序争议/不健康时应给可读的阻断原因，不把拒绝校验当作bug关闭。
