# Four fixes and Redmi 2083 implementation plan

Authorization: user explicitly requests implementation and acceptance of comment privacy, adaptive compact top menu, ordinary-member announcements, and persistent lazy image caching, followed by Redmi Debug delivery. Their supplied requirements are the approved behavior; no additional feature approval is inferred necessary. Work directly on main, preserving the prior only-main request.

Design: enforce comment visibility before API projection, sharing the policy across feed/detail/pagination/replies; keep owner behavior intact. Measure menu content and text scale instead of fixed container width; normal current labels must fit within 75% of the previous 176 width. Preserve tap height and full text. If content cannot mathematically fit the viewport without wrapping/clipping, use an explicit scrollable content region rather than silently shrinking accessibility text. Announcements must separate membership-authorized reads from moderator-only writes and distinguish missing state from transport errors. Image identity follows trusted content hash + account isolation with URL fallback; reuse memory/disk data, bounded prefetch and concurrency, expiry/cleanup, stable placeholders and retry.

Ownership and steps:
- [x] comments agent: services/business-api/app/modules/moments/**, related tests and a dedicated verification note. Prove A/B/C/D/E policy red then green, direct reply and relation-change cases; identify deployment needs.
- [x] announcements agent: group announcement feature/gateways and focused tests; no changes to shared matrix_e2ee_client without coordinating. Establish read/write roles, empty-state and retry/account lifecycle tests.
- [x] media agent: ui/moments media widgets/cache and relevant tests; owns moments_page only for viewport prefetch/laziness and must coordinate before changes. Prove second entry has no duplicate HTTP request, expiry and URL updates, bounded work and offline failure/retry.
- [x] root: top_more_menu and HTML equivalent/tokens/tests, integration review, version 0.3.79 / 2083, full checks and delivery. Red/green content-size, scale, screen and tap target assertions.
- [x] Run focused tests, Flutter analyze and full tests, frontend tests and scripts/verify.ps1. Review specification compliance then security/quality.
- [x] Backend privacy fix must reach the running service to benefit the phone; prepare and verify a reversible targeted deployment if existing session authorization permits, otherwise clearly identify it as pending rather than implying APK-only fixes server authorization.
- [x] Rebuild ARM64 standard debug under android-apk-rebuild runbook, preserve required HTTPS defines and installed Redmi Debug signer for in-place upgrade, validate rebuilt payload/identity/signature/alignment, install -r and verify version/startup. No user messages or data clearing.

Artifacts: docs/verification/artifacts/2026-09-10/four-fixes-2083/. No credentials, plaintext message logs or generated APKs staged.
