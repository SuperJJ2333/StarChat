# 轻量发布门禁（用户已批准）
范围：统一双端发布记录；iOS XML生成及解析、官网/下载链接/安装入口一致；Android隔离；备份审计及失败回退；以HEAD替代整包回拉验包。用户确认2144签名已解决，不重新验包。
实现：scripts/release_metadata.py prepare/check/publish；服务器通过SettingService更新唯一平台。最终包签名作为发布人确认的交接记录，不声称本工具实际验签。旧脚本移除回拉部分；新版流程为权威入口。
测试先红后绿：非法XML、直链安装、跨平台、非法URL/版本、签名确认缺失、HEAD长度、过期页面、回退行为。
不改变当前生产发布设置，不触发构建、不下载安装包。
