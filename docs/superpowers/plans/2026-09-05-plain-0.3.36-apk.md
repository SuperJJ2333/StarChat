# 用户授权的普通 Release 测试包

用户要求重新构建 ChatFlow 0.3.36 ARM64，不反编译、不加壳、不添加构建防护。沿用先前明确授权的关闭 Dart/R8 混淆范围。

1. 固定当前已提交源码 b80049b3a062a07a02740bf553b026cdf5b2504b，在 docs/verification/plain36/src 导出独立副本。当前另有进行中的未提交通话修复，不纳入、不修改。
2. 副本关闭 Android minify/resource shrink，Flutter 不传 obfuscate/split-debug-info，并关闭图标 tree-shaking。无需解包或修改历史 APK。
3. 构建 standard release、ARM64、0.3.36+39，使用现有正式证书和生产 API/推送配置。不修改通信加密、身份鉴权或第三方 SDK 二进制。
4. 核验最终 APK 包内版本、架构、签名、非 debuggable 与哈希；单独提供本地测试文件，不发布。
