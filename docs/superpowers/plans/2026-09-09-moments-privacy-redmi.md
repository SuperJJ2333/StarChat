# 朋友圈权限与资料入口执行计划

依据：用户本轮五项明确要求及补充确认。关闭入口时同时阻止其他人查看，本人可管理；先完成实现和隔离账号测试，真机多账号隐私场景保留待验证。不发布正式更新、不推送iOS；在已连接Redmi Note 7验证重建后的Debug测试包。

采用现有微信式组件及共享头像缓存。评论点击回复保持不变；长按动态复制正文，作者多一个删除入口，删除二次确认。头像按业务用户ID打开资料；资料页朋友圈入口展示权限过滤后的最近照片/文字摘要，点击进入作者时间线，空内容展示空态。

服务端统一 VisibilityPolicy：本人可见；其他人必须为好友、未被任何适用黑名单或不给谁看规则排除、作者开启入口，并处于作者设置的范围。沿用已有范围ALL/3天/30天/183天，按UTC时间判定，不能错误使用查看者自己的范围过滤别人。所有列表、详情、互动、通知和摘要共用该规则。

所有人可见的资料预览响应只给出 entry_visible 及已授权条目，禁止通过摘要泄露不可见文字/图片。空朋友圈仍向允许访问的好友展示入口。新设置字段 optional，旧客户端更新已有字段不重置隐私；数据库采用新增列默认值和新增迁移，保留旧数据。

## 文件责任及步骤

- [x] 服务端任务：moments API、models/service/visibility、新0058迁移、OpenAPI及Python测试。先红后绿，验证三账号/屏蔽/边界/范围的作者方向/关闭开关/删除权限/摘要/失败输入。禁止触碰钱包/认证和生产数据。
- [x] 客户端任务：moments页面/操作菜单/作者时间线与资料摘要组件、contacts资料页、BusinessApiClient钱包外新增方法、组件注册。先补UI测试，再实现成功/失败/空态/保存状态，保留评论点击回复。
- [x] 先规格再质量安全复核；完整朋友圈和相关资料页测试、Flutter分析、契约/迁移检查。
- [x] 读取当前生产基线并准备可审阅部署候选；只有需要真机联网验证时，在验证迁移和回退后部署本次朋友圈变更，保留其他任务已发布代码。
- [x] 依APK runbook源码Debug→Apktool重建→对齐→稳定签名→验证，保护当前Redmi安装数据与签名连续性。记录逐项预期/实际/问题复现。
- [x] 保存来源、测试和真机证据，明确真实多账号场景尚待测试账号。Figma沿用此前暂缓同步安排，不虚报修改或截图；若本轮可用则同步既有节点。

临时工件仅放 `docs/verification/artifacts/2026-09-09/moments-privacy/`。继续在已有隔离worktree执行；主目录保持只读。执行使用subagent-driven-development，将独立服务端模块交给实现者，主任务负责Flutter和Redmi；同一文件不并行修改。

实际执行与待验证边界见 `docs/verification/2026-09-09-moments-privacy-redmi.md`。用户已确认将真实多账号真机场景保留待验证。
