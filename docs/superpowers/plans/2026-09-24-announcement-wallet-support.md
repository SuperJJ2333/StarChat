# 公告、钱包与客服身份展示修复

用户2026-09-24批准四项修改，并明确“今后公告不加密，旧公告由管理员重发”。交付沿用仅debug测试，不发布官网/更新弹窗。继续候选mobile-feedback-2165，保留上一轮已验证修改。

1. 公告：新公告普通Matrix状态/附件，权限仍以房间power levels校验；旧加密内容可读则兼容，缺密钥明确管理员重发，不删除旧事件。[ADR-0087](../../adr/0087-public-group-announcements.md)记录用户批准的公告明文例外；普通聊天不变。
2. 钱包提示为1点钻=1CNY；账单金额下显示权威交易后余额，两位小数；无法获得历史余额显示未知，禁止套用当前余额。
3. 客服头衔：账号隔离共享持久展示缓存，不按页面定时清空。按权威变更失效，补足断线/重启重新核对机制；缓存不是权限凭据。统一复用WeChatOfficialName，昵称溢出省略，聊天信息采用第二行。
4. 在我、联系人/聊天信息、群消息/成员、朋友圈动态/评论/个人动态等姓名场景接线。昵称或备注不得伪造客服身份。
5. 各域先红后绿，规格符合性后质量安全审查；更新HTML/registry，最终适用全量、analyze、debug重建验包、Mi6保留数据安装。既有未变服务端证据可复用，新增读投影须独立验证。不部署生产资金/权限改动。

文件归属：公告代理拥有公告service/page/tests/ADR；账单代理拥有钱包/点钻账单及读取投影；缓存代理拥有support repository/business client相关支持方法与测试；主代理拥有其余姓名UI、HTML/registry及交付记录。不得同时编辑同一文件。
