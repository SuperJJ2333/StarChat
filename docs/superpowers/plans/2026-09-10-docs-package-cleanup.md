# 安装包与当天完成任务清理

用户于 2026-09-10 明确授权：同哈希安装包去重、核验后删除构建中间包、重新检查当天已完成的任务目录。

在当前工作区执行文件整理，不修改产品行为，不提交其他任务的修改。范围为 docs/verification；保留原先归档与恢复测试目录。

1. 清点 APK/IPA，计算 SHA256；同哈希保留一份有版本名称的最终包或受保护样本，记录所有旧路径到保留路径的映射。
2. 中间包仅在同目录最终包可验证、ZIP 全部成员内容相同（签名元数据除外）时删除；若同目录最终包已在本轮同哈希去重，则通过删除日志的完整 SHA256 映射定位保留包，再逐成员比对并验签。原始 source.apk 具有不同内容时保留。保留签名最终包和构建记录。用户此次授权允许删除已证明冗余的包，无需另存相同内容的 ZIP。
3. 逐项审核 2026-09-10 已完成任务目录；活动任务和状态不明目录保留。已完成的生成子目录使用无损归档、逐文件哈希复核后删除。
4. 删除前重新核验源哈希、保留包哈希、绝对路径边界、链接与活动进程；记录删除结果。补充路径映射和报告，复测剩余体积。

负责人：本任务；独立审核负责当天目录评估与删除清单复核。证据位于 docs/verification/artifacts/2026-09-10/docs-package-cleanup/。

## 完成记录

已完成：111 个包（37 个同哈希副本、74 个已验证中间包）删除；16 个当天生成目录无损归档后移除。41 个保留包和 16 个归档最终哈希复核通过，未完成包清单为空。操作净减少 17.01 GiB，docs 剩余约 36.65 GiB。完整路径、理由和证据见 [执行报告](../../verification/2026-09-10-docs-package-cleanup.md)。本轮只整理本地工件和说明文档，没有提交、推送或修改产品行为。
