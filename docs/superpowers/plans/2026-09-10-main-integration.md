# 将已验证发布基线及分散修复整合到 main

授权：用户 2026-09-10 明确要求合并 main、提交，并处理其他分支同类遗漏。

1. 保存本地分支/工作树清单、未提交源文件快照与 SHA256，排除秘密、运行数据、构建缓存及大型工件。
2. 在独立整合工作树中从 main 汇入 redmi-polish 2077 与 mobile-parity 的独有提交。保留朋友圈 2077 行为、iOS 分发隔离及身份会话修复。
3. 审查 root 和旧 iOS 未提交改动，已集成或已被新实现替代的内容不覆盖新实现；只迁移尚未包含的有效修复。所有文件差异记入审计记录。
4. 迁移本次 @ 提醒、历史分页、日期与按钮修复及测试，与新版媒体、朋友圈、身份代码逐项合并。
5. 先规格符合性复核、再质量安全复核；运行 Flutter analyzer/full test、HTML tests、UI contract、仓库 verify。受保护领域沿用已批准实现，不趁合并更改契约。
6. 测试通过后提交，快进本地 main。用户未要求远端发布或新 APK，不修改线上更新配置。
7. 根目录切换到已整合 main，保留未纳入的本地生成工件及可恢复快照，避免再次从旧分支打包。旧工作树保留，逐一记录包含关系，不删除有未提交内容的目录。

责任文件：整合工作树中的合并冲突、遗漏修复、该计划及对应验证报告。并行子任务仅审查不同来源，不并发编辑共享文件。

## 并行任务保护补充

核实“实施媒体去重与千人群扩容”仍在原根目录运行并写入文件。合并在隔离工作树完成；main 将指向验证后的提交并在隔离工作树检出，原根目录不执行 stash/reset/switch，防止打断正在运行的任务。原根目录继续保留旧分支及未提交内容，后续构建应明确使用 main 检出目录。已采集源码快照、分支引用与增量清单，不自动删除其他分支或工作树。
