# ChatFlow performance implementation plan

> For agentic workers: use subagent-driven-development, test-first implementation, specification review then quality/security review.

Goal (latest user instruction): implement approved P0/P1/P2 performance changes on shared Android/iOS code and run available local automated verification. No automatic push, deployment, production restart or production database migration. User owns device acceptance. Earlier publication authority is superseded; stable encrypted media content hash remains the compatibility contract.
Architecture: retain Matrix SDK authoritative local history and E2EE; business profile/local feed snapshots publish independently; bounded shared media cache, local-first pages, incremental presentation. Reference ../specs/2026-09-11-chatflow-performance-plan.md in full.
Baseline: isolated codex/performance-20260911 combines wallet/chat 7bc73ec3 and main 0f65614d (iOS binding fix). No unrelated dirty main files imported.

Each task follows: inspect current path → failing regression with intended red result → implementation → focused tests/analyze → specification review → quality/security review → commit explicit owned files. Logs only docs/verification/artifacts/2026-09-11/performance/.

## Tasks

- [ ] PERF00 Baseline: production versions/settings and artifact references (read-only), tools/locks, deterministic 500-room/1000-member/50000-history workload; FrameTiming/cache/refresh counters without private data. Files: new test/performance fixtures and lib/core/performance telemetry; trace hooks added when each owner integrates. Distinguish desktop algorithm baseline and actual mobile frames.
- [x] PERF01 Avatar: profile_repository.dart, matrix_user_avatar.dart, avatar_url_resolver.dart, ui/foundation/avatar_cache.dart, corresponding tests. Independently publish profile and contacts; preserve scoped disk hydration/generation, tri-state removal and mutation ordering. Failed resolutions retry boundedly with lifecycle cancellation, credentials scoped/current, stable disk key; subscriber refresh after success. Test failure recovery unchanged props, profile succeeds/contacts fails, avatar removed/account swap.
- [x] PERF02 Offline chat: room_page.dart load sequence, dedicated load state helper if useful; tests. Local timeline displays despite markRead/emoji/reminder failure. Preserve empty/offline/retry, SDK local history and account boundaries; no hidden errors or network auth bypass.
- [x] PERF03 Input: room_draft_store.dart, wechat_composer.dart, room_page.dart tests. Typed draft copies before debounce, serialize at flush; preserve IME/composition and lifecycle flush. Ordinary input rebuilds no message rows; member search only @ path.
- [x] PERF04 Timeline: room_timeline_controller.dart, adapter interfaces only if needed, room_page.dart/tests. Stable ID index and local echo reconciliation, no-op suppression, incremental updates and bounded UI projection; SDK history order/gap/reply anchors/redactions preserved. Add large workload/cost assertions and no echo duplicate regression.
- [x] PERF05 Media: media_cache.dart, scheduler/budget helpers, image/video cells/poster cache tests. Shared bounded resource scheduling, requests join and visible priority promotion, account hash objects reused, video file path avoids full memory duplication, bounded decode/animation, offscreen cancel by ownership. Test known hash cross-account isolation, same-content roundtrip, cache clear/generation, concurrency and budgets.
- [x] PERF06 Moments: cache_repository.dart, moments_page.dart, persistence and tests. Persist viewed pages/cursors/tombstones using existing SQLite conventions, account/private invalidation, local-first offline pagination; no whole feed reset. Expansion/migration preserves existing cache.
- [x] PERF07 Lists/profile/viewers: matrix_home_page.dart, profile subscriptions, room/media viewers, tests. Dirty room projection and stable ordering; peridentity refresh; current image priority and adjacent bounded prefetch; one active video. Preserve navigation and room image order.
- [ ] PERF08 Verification: focused tests, shared full flutter suite/analyze, preflight then scripts/verify.ps1; Android/iOS platform compilation and performance scenarios. Capture red/green source identity, realistic caveats, review specification before quality. No passing claim from compilation alone.
- [ ] PERF09 Delivery (OUT OF CURRENT SCOPE; historical plan only, not authorization): re-read live versions and signing workflow; source builds, Android conventional rebuild/alignment/stable signing, iOS verified signing channel; immutable upload then separate platform manifests/settings/audit. HTTPS/SHA/range/rollback gates. Pending iOS external signature blocks only iOS publication, never replace it with unsigned IPA. Do not mark goal complete until requested platforms/requirements have sufficient evidence.

Ownership under latest authorization: Astra owns plan/task records, baseline review, local integration and acceptance. At most two explicitly configured Terra executors own disjoint modules; shared files/dependent changes and Flutter build outputs are serialized. Production operations are excluded.

## 2026-09-11 authoritative execution amendment: Astra / Terra
Latest user instruction supersedes earlier production authorization: local source changes and available automated verification only. No push, deployment, production restart or production database migration. Device acceptance belongs to user. PERF09 publication is out of current scope; historical publication entries below/above are not authorization.
Use existing performance worktree without clone/pull/reset or overwriting unrelated changes. Main configured model gpt-6-astra; explicitly request gpt-5.6-terra for execution subagents. At most two executors; shared files serialized. Parent owns plan, actual diff/call-chain review and acceptance; apply specification review before quality/security review.

| Batch / audit | Goal and dependency | Allowed files | Constraints | Acceptance / tests |
| --- | --- | --- | --- | --- |
| A / PERF04-Q1,Q2 | Review existing linear group notice correction before acceptance | Read-only parent review of matrix_e2ee_client timeline and room_timeline_window_test; fixes delegated if needed | SDK order, equal-time notice-first, existing local echo/recall/E2EE | Inspect 50k+1k fixture and actual red/green; timeline focused gate and analyze |
| B / PERF07-M1 | Stop redundant all-group member fetch on every sync; independent of A semantics, shared SDK file edited only by one Terra | matrix_e2ee_client.dart conversation member refresh, new narrowly scoped refresh policy helper, matrix_client_factory_test and new performance test | Public API compatible; retain cached members offline; membership changes/new rooms invalidate; account isolation; bounded retries; no production calls | Failing repeated-completed-refresh test, dirty/new/expired/failure/account cases; factory + conversation regressions; scoped analyze |
| C / PERF07-L1 | After B review, coalesce home snapshots and dirty-room projections | matrix_home_page.dart, conversation snapshot helpers/interfaces and scoped tests (exact files assigned at dispatch) | Preserve hidden/pin/unread/draft ordering and E2EE | Real home fixture, 500-room work counters, single-room changes, optimistic prefs and offline regression |
| D / PERF05,07 media/profile | Remaining UI leases, bounded adjacent prefetch, one-video lifecycle, peridentity subscription; divide into file-disjoint subtasks after audit | Exact media/avatar/viewer modules assigned after C | Hash/account integrity, encrypted history, existing navigation | Cancellation/priority/account/disposal tests, media reuse and viewer regressions |
| E / PERF00,08 | Consolidate full local gates and remaining realistic synthetic workload | Tests, evidence and verification configuration only as needed | Preserve Getui/E2EE/ledger precision/idempotency/audit; never weaken gates | Preflight verify.ps1, flutter tests/analyze, platform builds supported here; record unavailable native environments and user-only device acceptance |
## Current batch status and next bounded video task
- B / PERF07-M1: accepted local SPEC then QUALITY,67tests +2fileanalyze; superseded source may subsequently include C1, see saved reviewed input hashes.
- V1 / PERF07 viewers: accepted local SPEC then QUALITY after parent reads actual6files and stream fixture. terra-viewer-final-tests.log10pass; terra-viewer-final-analyze.log6files/noissues. Pub resolve occurred during this analyzer, pubspec.lock diff verified empty. Input hashes terra-viewer-reviewed-inputs.json. Firstframe listener release/dispose, source/account pending replacement, completedneighborreuse,50kneighboridentity bound<=2, two viewer entrypoints and stable publickey verified. Pending native transport cannot be aborted by removing CachedNetworkImageProvider listener; no claim of HTTP cancellation or full global media scheduler integration.
- A Q3: existing action-system boundary passed1test terra-q3-boundaries.log after ModernActionButton replacement keeping timeline-return-latest key/callback. Final combined RoomPage test blocked by C1 transient SDK constructor syntax; do not count failedcombined gate aspass. C1 corrected to private trusted constructor; integrate only after its stable source gate.
- C1 executing: SDK-owned immutable member projections reused, with local-state hydration invalidation and account/lifecycle/prune; public constructor remains defensive. C2 home coalescing/dirtyprojection still pending.
- D2 / PERF05-V2 now assigned same explicitly configured Terra viewer executor, owns only ui/chat/wechat_video_message.dart VideoViewerPage, optional ui/chat video-owner helper, video_viewer_cache_test/newwidgettests. Root verified no lifecycle observer; delayed init can start playback afterbackground, timer runs until dispose, perpage globalwakelock disable can conflict. Must add generation guards, oneactiveviewer, route/app pause, timer/wakelockownership; preserve manualpause/save/forward/speed/filecache interfaces. Tests heldinit/dispose/background/routecover/twoviewers/retry/manualpause, scopedanalyze. No SDK edits or device tests.
- D2 split: D2a owns new pure playback lease/wakelock helper and unit tests; D2b integrates only after parent SPEC/QUALITY acceptance. D2a tests held enable/replacement/old release, background cycling, failure recovery and last-owner disable. Preliminary VideoViewerPage changes remain unaccepted until D2b. C1 scale fixture passed; prune/resume/dynamic metadata assertions requested before C2.

- C1 accepted (factory70, scale1, scoped3fileanalysis; parent SPEC then QUALITY and reviewed source hashes). C2a next: coalesce home snapshot refresh bursts via bounded single-flight + at most one dirty trailing pass, preserve fresh follow-up events and stale-owner/dispose safety. Own matrix_home_page.dart, optional refresh coordinator helper, focused home test. Avoid arbitrary timing; held fake snapshot tests must show max concurrent1, bounded calls and final fresh result. C2b follows review: stable row projections/identity/title work with 500-room fixture. No shared SDK edits needed for C2a.
- A/B/C1 accepted local commit20d5c07e; V1 accepted local commitdb0b8575; no push. D2c extends original video scope with held play/init, two viewers, retry and cleanup assertions before entire D2 acceptance. Afterwards separately review screen-on ownership with CallPage (UI only) and GalleryVideoPreviewPage rendition/player lifecycle; no call protocol/E2EE changes.
- Planned P1 / PERF07 identity scope after active batch: ProfileRepository currently broadcasts contact updates; ContactProfilePage linearly searches contacts and rebuilds for unrelated users. Introduce narrowly keyed contact selection/subscription with lifecycle disposal, O(1) lookup and value comparison including explicit avatar removal. Integrate profile page without changing friend relationship or button callbacks. Own profile_repository.dart, contacts_page.dart ContactProfilePage region and focused identity/profile tests; no financial/Matrix state changes. Assert unrelated contact update does not rebuild selected profile; relevant nickname/remark/avatar removal updates; repository replacement/dispose releases listener; existing message/voice/video routes still pass. RoomPage peridentity refinements, if needed, assigned separately after review.
- C2a accepted local commit364de218 (behavioral red1vs3,9green,3analysisclean). C2b / PERF07-L2 owns matrix_home_page.dart, optional narrowly scoped conversation projection helper and test/performance/conversation_home_projection_test.dart plus existing home tests. Goals: explicit group names bypass eager full-member identity construction; unchanged room inputs reuse presentation and skip publication; one changed room reprojects only itself; stable visible row widgets avoid rebuild of unchanged siblings; group avatar preparation occurs for rendered rows; prune removed-room keys/caches. Maintain account/identity invalidation, decrypted preview, message edits/recall, unread/mute/pin/fold/hidden/order, mentions and theme dependencies. Compare500rooms/50groups×1000members with deterministic actual home counters, no-op and one changed room plus identity/metadata changes; preserve existing home/avatar/preferences tests. This depends on accepted C2a and cannot overlap its file owner.

## D2 accepted; D3 shared screen-on ownership
- Astra personally read final four-file diff, controller/Navigator fixtures, serial pump and red/green evidence. SPEC PASS then QUALITY PASS for bounded VideoViewerPage lifecycle scope. Correct route-pending regression pops while play is still held (red false, green true); repeated resume native peak drops 2 to 1. Final 27 tests and four-file analyzer pass. Reviewed SHA identities: astra-video-reviewed-inputs.json. Local commit 8c357742; no push or device test.
- D3 / PERF05-V3 depends on D2: eliminate independent global wakelock toggles between CallPage and video viewer. Allowed: new core screen-on lease helper, call_page.dart screen-on region only, wechat_video_message.dart coordinator wiring only, focused helper/call/video tests. Use process-wide set of owner tokens plus serialized desired-state pump; releasing one owner cannot disable another; last owner releases; platform errors cannot strand later state. Preserve VideoPlaybackLeaseCoordinator exclusive video token semantics, call lifecycle/protocol/renderers/E2EE, existing public APIs. Verify real CallPage/video overlap, duplicate release, held platform toggle, failure recovery, final disposal, existing 27-video and call tests; scoped analyzer. No production or device operations.
- C2b real SDK workload now accepted as one proof: 500 new room wrappers, shared member projections, no-op zero home projections, one member change only one room projection. Remaining row reuse/effective unread/same-event edit assertions assigned before acceptance.

### Remaining bounded integration order after C2b / D3
1. P1 / PERF07-I1: ProfileRepository keyed contact selection and ContactProfilePage listener ownership. Existing contactsByUserId already provides O(1) lookup; reuse it. Compare all ContactSummary fields including nudgeSuffix and ordered tags, preserve unknown versus explicit-null avatar. Notify selected consumers only on semantic change; detach on selected user/repository replacement and dispose. Do not change existing friend/message/voice/video contracts. Tests: unrelated update zero profile rebuild, changed remark/nickname/avatar removal, repository/user swap, dispose, existing contacts/identity suites. Files: profile_repository.dart, contacts_page.dart profile region, new focused subscription/profile tests. Execute only after home source acceptance.
2. D4 / PERF05-V4: GalleryVideoPreviewPage asynchronous rendition/player lifecycle shares exclusive video ownership after D3. Preserve rendition disposal and selection/transcode policy. Test held rendition/init/play during cover/background/dispose, current owner, retry and manual pause. Exact helper extraction approved before modifying accepted video source.
3. D5 / PERF05-M1: expose consumer lifetime to existing shared content-cache flights, not a second scheduler wrapping the same loader. Queued work cancels only after last consumer leaves; running native futures retain slots until completion. Current/adjacent gallery priority, generation/account clearing and shared consumer survival require integration regressions. Actual ContainImageBubble/ImageViewerPage paths (unused AnimatedImageCell is not evidence) need offscreen animation control. Design/refine exact files after D4 audit before execution.
- Additional inspected follow-up: ProfileRepository.precacheAvatarImages currently walks all contacts; MatrixHomePage starts it unawaited per open, app_home group-create route awaits it. Future bounded warmup must avoid all-contact decode before navigation, preserve visible avatar refresh and scope cancellation; no current performance claim for this remaining path.

## C2b accepted and P1 assigned
- Astra SPEC PASS then QUALITY PASS after reading actual signature/projection/row/navigation call chain and final real SDK workload, unread and same-ID edit fixtures. Named 500-room/50x1000-member workload skips all title-member identity walks, no-op adds zero projections/row builds, one group change only its row; 450 rooms are empty, not proven direct. Signatures still scan room metadata and canonicalize event content/preferences; this is not O(1) total SDK/home work or a native frame measurement.
- Valid behavior red omits effectiveUnread from signature: projected room absent, expected nonempty; restored bytes exact SHA and same test passes. Initial compiler-error red is not bug proof. Same-ID edit fixture checks preview refresh; sibling wrapper fixture does not prove reuse, real SDK workload supplies that evidence separately.
- Parent final command: flutter test --no-pub test/performance/conversation_home_projection_test.dart test/features/matrix/matrix_home_snapshot_refresh_test.dart test/features/matrix/matrix_conversation_avatar_test.dart test/features/matrix/conversation_preferences_test.dart --reporter expanded: 20pass exit0. Two-file analyzer noissues exit0,7.3s combined wall. Logs astra-home-final-gate.log / astra-home-final-analyze.log; hashes astra-home-reviewed-inputs.json. Commit explicit2files only, no push.
- P1 next bounded scope remains profile_repository.dart + contacts_page.dart ContactProfilePage region + focused selector/profile tests. D3 agent owns disjoint screen-on files. Shared Flutter slot serialized.

## D3 shared screen-on accepted (2026-09-11 approximately14:08 +08)
- Parent SPEC then QUALITY PASS after actual helper, call/video wiring and bidirectional widget review. Token-set ownership plus serial latest-state pump; stale/duplicate release cannot turn off another feature, final owner release disables. Call lifetime and video-exclusive control unchanged, no renderer/protocol edits.
- Direct original wiring rollback with final fixture gives Expectedtrue Actualfalse in both directions (d3-screen-on-direct-overlap-red.log). Initial red hung because fixture awaited Navigator.push; initial green4pass1fail also failed fixture playback, neither accepted. Subsequent final suite31pass exit0 (d3-screen-on-final-tests.log) and5fileanalyze clean (d3-screen-on-analyze.log). Last helper-only refinement verifies later owner enables after a platform failure;4pass rerun d3-screen-on-helper-final-tests.log. No full rerun needed for that pure helper assertion change. Final actualSHA saved astra-screen-on-reviewed-inputs.json.
- Five explicit source/test files locally committed, no push/device/production. Platform interface mocked; native device screen-on behavior remains user acceptance.

### D4a / PERF05-V4a exact extraction scope
- Dependency D3 accepted0f4d6396. First extract video activation ownership from private VideoViewerPage State to a reusable process-wide arbiter, without yet changing GalleryVideoPreviewPage. Allowed: new ui/chat/video_playback_arbiter.dart, wechat_video_message.dart activation ownership/wakelock wiring, focused arbiter test and existing video lifecycle tests as needed. No profile/home/call edits.
- Interface direction: latest activation reserves an owner/token synchronously; await previous owner pause; publish only if reservation remains current. Release checks its exact token, so old page disposal cannot invalidate a newer request. Preserve page-local cancellation revision and same-controller resume serialization. Share video screen-on demand through existing D3 owner, not new independent global platform toggles.
- Acceptance: held previous pause with third contender, canceled pending owner, old release after replacement, pause error and same-owner reentry safe; existing15 VideoViewer lifecycle regressions,5 video lease helper, call/video overlap2,video-cache7,scoped analysis. D4b integrates gallery rendition lifecycle only after this foundation passes parent review. Native calls/protocol/E2EE unaffected.

## P1 / PERF07-I1 accepted
- Astra SPEC PASS then QUALITY PASS after actual3files/callchain/tests/logreview. Existing contactsByUserId reused; selected ValueListenable compares all contact fields including nudgeSuffix/avatar known-null/ordered tags. Page rebinds repository/user, resets fallback on account repository replacement, keeps current contact in action callbacks. Global subscriptions and financial/Matrix logic unchanged.
- Review fixes: ValueNotifier self-dispose during notification violated Flutter assertion; custom read-only ValueListenable now synchronously detaches, snapshots listeners, skips removed callbacks and reports individual callback errors without aborting other delivery/persistence. Repository-first/widget-later disposal is tested; no permanently retained peruser selectors afterconsumer disposal.
- Valid old exact HEAD page global-listener behavioral red Expectedfalse Actualtrue on unrelated Bob update, then exact restored candidate green. Logs perf07-i1-old-global-listener-red.log, perf07-i1-restored-green.log; compile-only attempt retained separately and not counted. Final perf07-i1-final-47-tests.log contains49pass exit0 (filename stale, result authoritative); analyzer3filesclean exit0. Parent SHA checked against finalreported49-test input and saved astra-profile-reviewed-inputs.json before explicit3filecommit. No push/native/device/production operation.

### P2 / PERF07-I2 bounded avatar warmup
- Depends on P1 accepted f0a8dda5. Root confirmed precacheAvatarImages materializes every contact and decodes serially; home opens start this repeatedly, app_home group-create awaits it before acquiring chat lease. Exact scope: profile_repository.dart avatar warmup method only; matrix_home_page.dart _warmChatIdentity/call only; app_home.dart _createGroupChat identity warmup region only; focused new warmup/navigation tests. No picker/contact selector/financial/call changes.
- Cap warmup to at most9 unique avatars (own plus explicitly requested visible Matrix user IDs); default warms own only, never arbitrary all-contact list. Resolve each requested ID by existing O(1) map; stop scheduling after owner/repository generation changes or context unmount. Reuse image-provider cache identity. Pending provider transport may finish; no native cancellation claim.
- Home passes direct peer or first visible group avatar members without constructing all contact identities. Group-create navigation must not await all-contact preload/decode; cached identity renders immediately while bounded optional refresh runs safely, failure nonblocking. Keep Matrix room lease acquisition/revocation/navigation cleanup unchanged.
- Tests:50000 contacts work bounded<=9 provider requests, duplicate selected IDs dedup, unchanged provider identity reuse, context/owner invalidation stops remaining work, failed optional profile/avatar load does not block created-room navigation. Existing profile/home/avatar/app lifecycle tests +scopedanalyze; behavioral old all-contact/navigation wait red.

### D4a review correction and D5 dependency design
- D4a remains UNACCEPTED despite provisional45-test green: Astra found completed native pause failure followed by a later B activation loses A's stop barrier. Require actual widget proof in addition to overlapping held-failure/retry; failed native owner must remain retryable, but successful controller disposal must release retained failure callbacks. waitUntilReady failure must release its unsuccessful requester token. Existing successful-pause, pending third owner and CallPage screen-on contracts stay intact.
- I2 remains UNACCEPTED: production warmup is bounded, but actual AppHome group navigation must be proven independent of a held optional profile request. Approved minimal optional repository factory follows existing hydration/preload/disposal ownership; it must not bypass navigation, room leases, or API authorization in tests.
- D5a / PERF05-M1 foundation (after I2 releases its executor): new features/matrix/media_consumer_scope.dart and focused tests only. Generic owned flight tracks each caller independently and runs its child work under an aggregate consumer scope. Canceling one caller cannot cancel siblings; no caller scope means an explicit pin until completion. Last consumer cancellation closes the child scope. Priority promotion flows from live parent scopes to the shared child scope. Synchronous success/throw, disposal before start, completion cleanup and reentrant joining require deterministic tests. This helper adds no scheduler and no network policy.
- D5b depends on reviewed D5a: integrate MediaMemoryCache in-flight map AND loadMediaWithCache content flights with owned flights. Both must participate because legacy preview outer-Future dedup otherwise hides the second consumer from the SDK cache. The existing scheduler remains the sole native work admission gate (3 total/1video); cancellation removes queued last-owner work, running native futures keep their slots until completion. A newly arriving owner may rejoin still-running native work. Generation/identity guards prevent old cleanup removing a replacement flight or repopulating cleared caches. Maintain account/content hash verification and reference persistence; unscoped send/forward callers must remain pinned. Allowed exact files assigned after foundation review; add real nested preview/content/scheduler integration tests.
- D5c depends on D5b: RoomImageGalleryPage current/adjacent scopes with interactive/prefetch priorities, bounded previous/next prefetch and same-room stable anchor. Scope/account/source replacement invalidates old async pagination/preview results. ContainImageBubble and ImageViewerPage own actual consumer scopes and animation eligibility; preserve editing/forward/favorite/original image actions. Implement viewport/app/route eligibility on production Image widgets, not unused AnimatedImageCell. Removing an ImageStream listener is not proof of native download abortion.
- D5d telemetry hooks then E final gates: wire closed-enum memory/disk/download/join counters on real paths, retain disabled fast path and bounded samples. Distinguish disk hits, source downloads, immutable warm object reuse and native decoded/live/GPU memory. Complete Flutter suite/analyze and available Android compile only after latest owned sources stabilize; no device/production operation.

## I2 / PERF07-I2 accepted locally
- Astra SPEC PASS then QUALITY PASS after personally inspecting all5modified files, actual AppHome→MatrixHomePage callback→GroupChatPage.onCreated→RoomPage/lease fixture and avatar provider call chain. Warmup selects only own plus first9explicit candidates using existing maps and provider identity; the50kcontact fixture yields exactly u1..u8 because one of9candidateIDs is duplicated. No all-contact enumeration or navigation wait on optional network preload remains. Native in-flight image transport is not claimed canceled.
- Matrix owner captured before async group entry and rechecked after picker/name/cache/lease boundaries; existing lease cancellation/revocation and room callback contracts retained. Optional repository factory follows normal hydrate/preload/ownership, not an alternate navigation implementation.
- Valid oldawait behavioralred (GroupChatPage missing) exit1 in perf07-i2-group-await-red.log; finally restored app_home SHA A1D6BE0786248D271122FFE0B192564113A302468146661031D89AB282E8A9F3; actual restored GroupChatPage AND RoomPage while profileFuture held, then cleanup/noFlutterexception, namedgreen exit0 in perf07-i2-group-restored-green.log.
- Combined25tests exit0 perf07-i2-final-gate.log; final5fileanalyzer clean exit0 perf07-i2-final-analyze.log. Four initial analyzer import/nullassertion/braces diagnostics fixed with no behavior change;25test input hash file predates these cleanup-only changes. Parent explicitly reviewed that difference and saves authoritative current hashes astra-i2-reviewed-inputs.json. No separate runtime/provider-level decode count or Mi6 claim. No push/device/production operations.
- Next: D5a owned media-flight foundation; D4a video error/disposal correction remains unaccepted until its latestfullgate passes. Flutter output slot serialized.

## D4a / PERF05-V4a accepted locally
- Astra personally reviewed new arbiter and all page integration paths, pure9cases and actual19widget lifecycle cases, then SPEC PASS / QUALITY PASS. Parent matched all4actualfile hashes plus lock to Terra finalreport, saved astra-d4a-reviewed-inputs.json. Full relatedgate47pass exit0 (d4a-accepted-candidate-gate.log),4fileanalyze noissues exit0 (d4a-accepted-candidate-analyze.log). No reset hook used to hide retained ownership.
- Root causes corrected: old page released ownership before nativepause finished; shared lifecyclepause error could be swallowed before arbiter waiter observed it; canceled pending contender dropped previous stop barrier; completed pausefailure allowed a later viewer to start; disposal's latepause failure retained already-stopped owner. Stable owner delegate, raw single-flight pause, exactreservation cleanup, retryable failedstop and native-dispose-confirmed cleanup now cover these cases. Completedbarriers drop prior State references.
- Meaningful red: canceledpending→third readytrue vs expectedfalse (d4a-video-arbiter-cancel-red.log). Additional parent-requested diagnostic catches retry=true after returning-samepage disposal (d4a-arbiter-contamination-diagnostic.log); all19widgetteardowns now assert current/pending/retry empty. An early ticker diagnosis was rejected because it did not match the failing line. Two accidentally moved pump calls were restored to exact original timing; new disposalfixture uses keyedStack, verifies B initialized/zero plays and A disposed before releasing nativepause error. Failed intermediate18test/routefixture/analyzer attempts are not passing evidence; some candidate filenames were rerun, task transcript preserves prior failures.
- Maintains one exclusive VideoViewer playback token, shared CallPage/video screen-on leases, original filecache/manualpause/forward/retry/generation interfaces. Nativeplatform mocked; no realdevice frame/player acceptance or GalleryVideoPreviewPage integration claimed yet.
- Next bounded D4b: share process arbiter/video screen-on demand between VideoViewerPage and GalleryVideoPreviewPage; preserve gallery rendition transfer/disposal, progress, decoder fallback and selection UI. Execute after this accepted commit; D5a owns only its new flight/helper files independently.

### D4b first dispatch after08c7776b
- Owned exact modules: features/matrix/gallery_video_preview.dart; optional new ui/chat/shared_video_playback.dart for the SAME process arbiter and video-exclusive screen-on demand; wechat_video_message.dart wiring only; new test/features/matrix/gallery_video_lifecycle_test.dart and focused shared playback test. No call protocol, Matrix SDK, media cache, profile or financial edits.
- First bounded step: optional controllerFactory(File) seam; failing actual gallery widget cases for held rendition/initialize under background or disposal. Then generation/lifecycle integration, maintaining compressed rendition transfer/disposal, progress, selection and decoder static fallback. Later shared cross-viewer ownership/manual pause/retry tests must precede acceptance. No automatic reuse of VideoViewer UI inside gallery.
- D5a independent executor owns only new media_consumer_scope.dart and puretest. First behavioralred wrongly returns7 to canceledowner; full priority/cleanup/unscoped-pin cases still pending. Do not accept from singlegreen alone.

## D5a / PERF05-M1 foundation accepted7adfa803
- Astra SPEC PASS then QUALITY PASS after actual2file review: each scoped owner has separate completion/cancel handling, unscoped callers pin the source and share their Future, child scope receives strongest requested priority, inactive canceled flights reject rejoin, synchronous/reentrant source starts are registered before invocation, callbacks detach after normal/failure/cancel completion. Root verified default prefetch priority and a real synchronous throw rather than mislabeled Future.error.
- Behavioralred source returned7 to disposed A instead of MediaLoadCanceled (d5a-consumer-scope-red.log exit1); final9tests exit0 and2fileanalyzer exit0 (d5a-consumer-scope-final.log / d5a-consumer-scope-final-analyze.log). Actualsource+test+lock SHA matched reportedinputs and saved astra-d5a-reviewed-inputs.json. Helper only: no production consumer/scheduler integration or native HTTP cancellation claim yet.
- D5b1 exact next scope: features/matrix/media_cache.dart MediaMemoryCache in-flight block plus imports only; focused media_memory_cache tests. Replace raw future entries with OwnedMediaFlight, attach MediaConsumerScope.current explicitly, preserve warm immutable SynchronousFuture and unscoped sameFuture behavior. Register flight in map before join starts source; onInactive removes only identical flight. Before cache insertion validate generation AND flight still active, so canceled old source cannot overwrite a newer result. Closed scope must not create abandoned map entries. Existing clear-generation semantics and content/hash/account budgets remain intact.
- D5b1 tests: actualA/B scoped calls to same MediaMemoryCache, A cancel onlyA rejects whileB gets result and sourceChildScope staysactive; lastcancel followed by new samekey source then old completion cannot overwrite new; existing cache clear/immutability/budget tests. D5b2 later owns the SAME media_cache file for globalcontentflight/schedulerlease integration and nested preview proofs, so cannot execute inparallel with D5b1.

### D4b/D5b1 review checkpoint
- Astra found the Viewer→Gallery regression fixture removed the gallery (`page(true,false)`) while asserting the viewer had been removed. Correct fixture is `page(false,true)`; the earlier failure is not proof of a production handoff defect. D4b remains unaccepted pending bidirectional rerun and native dispose/late-pause/singleflight review cases.
- Actual gallery call-chain review found unconditional failed-pause clearing in disposal finally blocks and no complete activation singleflight. Terra owns focused corrections plus held-play/background/retry/disposal regressions. Native disposal failure must not be reported as confirmed stop; old failed pause after confirmed disposal must not strand new owners.
- D5b1 two-file candidate uses OwnedMediaFlight, identity-safe cleanup and generation+active guards. Initial executor reports11memory tests passed; parent has requested complete recorded red/green/analyzer/input identity before acceptance. Shared Flutter output slot remains serialized.

## D5b1 accepted9e8bae98; D5b2 integration next
- Astra personally reviewed two-file diff and actual nested cache call chain, SPEC PASS then QUALITY PASS. Memory flights now attach current scoped owner without letting A cancellation cancel B; closed cold scopes cannot start work; final owner removal invalidates exact map entry; late generation/inactive completion cannot overwrite newer cache bytes. Existing immutable warm SynchronousFuture and unscoped shared Future behavior retained.
-45focused cache/scope/budget/scheduler/preview tests and2fileanalyzer pass (d5b1-final-gate.log, d5b1-final-analyze.log). First controlledred had cleanup timeout and is superseded: d5b1-drained-red.log produces raw[6,6,6] instead of MediaLoadCanceled, exit1 without timeout; exact source restored, namedgreen exit0. Final fixture only moves assertions after source drainage. Parent actual hashes saved astra-d5b1-reviewed-inputs.json. No native cancellation claim yet.
- D5b2 owns media_cache.dart globalcontentflight/scheduler bridge, media_consumer_scope.dart priority Zone propagation, new media_consumer_integration_test.dart, and narrowly necessary room_image_preview_cache.dart cancellation guard. Replace raw shared content Futures with owned flights; one existing scheduler only. Bind cancellation and priority listeners to scheduler lease and remove in finally; inactive/old generation must not write memory or remove newer flight. Scope ambient priority must preserve prefetch and nested explicit interactive promotion. Preserve account namespaces, content validation, outgoing/source/message references, corruption recovery and cache-clear behavior.
- D5b2 tests must traverse nested RoomImagePreviewCache→MediaMemoryCache→contentcache→scheduler: A leaves/B survives, lastqueued owner cancels with no source call, activecancel/newsamekey joins one native source, prioritypromote, clear/account isolation. Pure test filesystem artifacts remain under allowed evidence directory. D4b owns Flutter outputslot initially; D5b2 prepares tests without running until slottransfer.

### D5c architecture decision for real image surfaces (not yet implementation)
- Follow D5b2 accepted scope/scheduler contract. Do not modify unused AnimatedImageCell and count it as production integration. Actual paths are ui/chat/contain_image_bubble.dart, ui/chat/encrypted_media_view.dart ImageViewerPage, ui/chat/room_image_gallery.dart, and their RoomPage construction sites.
- Introduce one narrowly scoped visibility/animation widget/helper: measure mounted cell bounds intersected with nearest scroll viewport after initial layout and coalesced scroll/layout changes; app/route/TickerMode eligibility also gates work. No per-frame timer or complete message-history scan. A process image-animation budget grants at most2 eligible GIF surfaces, using existing Flutter Image/TickerMode to suspend actual frame listeners. This limits active animations, not total native/GPU memory or native transport cancellation.
- Bubble: source identity must be account/room/event-or-content identity, not callback closure identity. Preserve outgoing local echo bytes, source dimensions, scrolling deferral and original GIF policy. ConsumerScope owns source intent; dispose/offscreen/source change cancels its participation; returning visible retries canceled work instead of displaying permanent error. Existing immutable memory/disk remains reusable.
- Room gallery: current image interactive, at most two adjacent image previews prefetch; owned scopes cancel outside ±1 or source/account generation. Keep stable current ID during earlier metadata prepend and source replacement; PageController ownership already exists. Do not prefetch all images or change editing/save/forward/favorite contracts. ImageViewerPage original request is interactive, guarded by source generation and owned scope, and canceled stale results cannot overwrite another image. Save actions need explicit operation ownership so route cover by permission dialogs is not treated as an unrelated page load failure.
- Tests before acceptance: actual production Image child TickerMode changes offscreen/background/covered and animation grant<=2; visibility return restores; pending bubble scope disposal preserves other consumers; gallery50k metadata stillloads<=current+2neighbor, indexpromotion, oldaccount heldcompletion rejected, boundaries/prepend/zoom/editor/forward/favorite regression. Synthetic lifecycle evidence is distinct from native GIF decoder/frame measurements on Mi6.

### D5c foundation split / PERF05-M2 decision
- Independent helper first, before real-surface wiring: ui/chat/media_activity.dart and test/ui/media_activity_test.dart only. Visibility derives from mounted RenderBox bounds intersected with nearest RenderAbstractViewport (confirmed available in Flutter3.44.9), display bounds, route-current/app-active and inherited TickerMode. Coalesce scroll/layout probes into one postframe callback per mounted surface; detach scroll/observer listeners on dependency change/dispose. No full-history scan or repeating timer.
- Helper exposes eligibility to a builder and supports a shared cap2 animation grant for GIFs. Current full-screen image has higher priority than chat thumbnails. Grant registration/removal is identity-owned; final owner release hands capacity to next eligible item. A placeholder is not sufficient proof: later wire actual Image widgets and verify TickerMode around their render paths.
- Initial-frame policy: a visible non-granted GIF must still obtain one first frame, then freeze; do not place Image behind disabled TickerMode before it ever yields a frame and leave blank bubbles. Builder may report first frame to the helper; any resulting state publication must be postframe-safe, not setState during Image.frameBuilder. Offscreen images do not decode first frames. Scope/network visibility and animation allowance are distinct, so static images are not blocked by GIF cap.
- Root inspected local Flutter image.dart: Image stops/listens to stream on TickerMode changes and keeps stream alive; this supplies frame suspension, not a claim of native request cancellation or instant codec destruction. Native TextureBox hitTestSelf is true; gallery fake players have no native Texture, hence approved opaque gesture surface plus fatal missed-tap tests.

## D4b accepted b3f0fff4
- Astra SPEC PASS then QUALITY PASS after actual5file diff/call-chain/fixture/log review. Gallery and chat viewer now use one process VideoPlaybackArbiter and video screen-on demand, maintaining independent CallPage demand. Gallery nativeplay activation is single-flight; app/route/manual intent and generation fence late callbacks. Compressed rendition selection/transfer/fallback interface retained; native controller stop/dispose failures remain retry barriers rather than allowing two active surfaces.
- Gallery per-controller disposal Expando and retiring-controller retry cover an empty-route gap and same-page retry without canceling the newer activation. Confirmed stopped controller is removed from retiring state. Exact reservation and lease cleanup survives rendition deletion errors; failures are explicitly reported. Opaque gallery playback hit testing makes actual taps reliable while fake native texture is absent.
- Related gate78pass exit0 (d4b-final-gate.log); after narrowly reviewed retiring-clear/braces/equivalent debuggetter and explicit fixture cleanup changes, current gallery9pass exit0 (d4b-gallery-cleanup-final.log), current5fileanalyzer clean exit0 (d4b-final-analyze-clean.log). Final parent hashes astra-d4b-reviewed-inputs.json; prior executor manifest is stale and not final identity. Underlying cache files were held stable during these gates; pending D5b2 cache integration separately remains unaccepted.
- Meaningful reds: baseline background initialization started playback; separated native-dispose failure permitted B.play Expected0 Actual1 (d4b-separated-dispose-red.log). Earlier wrong fixture removed gallery instead of viewer, broad replacement changed another test, tap-miss falsepass, lint/compiler attempts are not accepted proof. Fatal missed-tap warnings plus current manual pause test now pass. Gallery-owned rendition deletion is explicitly verified after retry cleanup. Not every gallery fixture has identical cleanup structure; later helper checks and existing lifecycle teardown cover the stated assertions, not an invented universal assertion count.
- No device playback/frame claim, production deployment, push or APK publication. D5c1 next exact independent ownership: new ui/chat/media_activity.dart and test/ui/media_activity_test.dart only; initially pure owner-based animation budget cap2 and prioritization. Actual viewport/image wiring waits for accepted helper and D5b2 contract. No production cache edits concurrent with video compilation.
## D5b2 accepted a14e2e1e; D5c1 accepted 59ed625e
- Astra inspected actual diffs, call chains, tests and evidence, SPEC PASS then QUALITY PASS for each bounded batch. D5b2 connects nested preview/memory/content owners to the single existing scheduler; one canceled consumer cannot cancel siblings, last queued cancellation prevents decrypt, running work keeps its slot and supports rejoin. Ambient and explicit priorities retain prefetch and promote live demand. Account/hash/generation and disk-corruption checks remain enforced.
- D5b2 actual gates: d5b2-full-cache-gate.log 57 pass; d5b2-priority-integration-final-green.log 15 pass; d5b2-content-final-green.log 31 pass; scoped analyzer clean. Behavioral reds cover canceled queued decrypt and wrong priority order. Extra content gate exposed an obsolete expectation that mutating caller bytes invalidates the defensive immutable copy; controlled baseline reproduced it, and only that assertion changed. Corruption and wrong-hash checks retained. Parent identities astra-d5b2-reviewed-inputs.json.
- D5c1 pure animation budget handles stable priority, revoke-before-grant, callback reentrancy and safe notifier disposal; maxActive validated. Behavioral red catches over-cap transition, concurrent modification and invalid capacity. Six tests pass and two-file analyzer clean; identities astra-d5c1-reviewed-inputs.json. This is a foundation only: actual Image/viewport wiring is still pending, no device animation acceptance claimed.
- D5c2 / PERF05-M2 owns new media_visibility.dart and its test only. Geometry intersects nearest viewport and display, with coalesced scroll/postframe probes plus route/app/TickerMode eligibility. No timers or plugin. Test mounted offscreen return, route cover/return, lifecycle and disposal. D5c3 / PERF07-G1 owns room_image_gallery.dart and gallery lifecycle tests, then narrowly approved RoomPage gallery metadata wiring. Current image interactive plus adjacent two prefetch owners; source/account identity and pagination epochs must preserve anchor and reject stale results. Same-file follow-ups and Flutter compilation inputs serialized.
- Current first gallery window candidate is not yet accepted: initial 50k metadata test passes, but swipe/disposal/error and source replacement tests remain. D5c4 actual image animation/load ownership and D5d diagnostic hooks follow reviewed foundations, then full local Flutter gates and available Android source compilation. No device, push, production or APK publication operations.
### D5c4 actual image integration design (pending foundations)
- Exact independent foundation ownership: new ui/chat/budgeted_media_image.dart and test/ui/budgeted_media_image_test.dart only. Shared MediaAnimationBudget defaults to two animated owners, viewer priority100 versus bubble0. Static images do not consume animation tokens. Visible over-budget animation must render a first frame and retain it before disabling Image TickerMode; initially hidden images need not create an Image. Source/provider replacement resets first-frame state. Safe queued notification updates prevent setState during another widget build/disposal.
- Following reviewed wrapper: ContainImageBubble owns an explicit consumer scope, source identity and viewport eligibility; cancel only its participation when hidden/disposed, retain completed bytes, retry canceled intent when visible, preserve deferred outgoing echo/cache/scroll-idle geometry behavior. RoomPage passes authenticated media identity at its two actual bubble construction sites. ImageViewerPage gets generation/source protection and scoped interactive original loads; explicit save operations retain their own intent through permission dialogs. Gallery passes current-page eligibility. Preserve edit/save/forward/favorite/zoom/navigation actions. Assign exact shared RoomPage edits serially after gallery wiring.
- Tests must assert actual Image descendant TickerMode and meaningful first-frame retention, not wrapper count, empty bodies, nonexistent assets or invalid PNG headers. The initially prepared animation tests were rejected on those grounds and do not provide acceptance evidence.
### D5c4 first-frame architecture refinement
Astra inspected the pinned Flutter3.44.9 SDK directly: Image.didChangeDependencies (image.dart1151-1156) listens while paused until the first frame exists; _handleImageFrame (1240-1252) retains that frame and stops its stream listener when paused. Android/iOS CI workflows also pin3.44.9. Therefore the wrapper should directly set TickerMode from visibility and budget; remove the redundant custom first-frame bypass and callback state. A visible denied image can rely on this SDK behavior to paint once and freeze. Acceptance must drive a real ImageInfo (precache/controlled stream) and inspect non-null RenderImage.image plus ticker=false. Merely calling frameBuilder is not proof of retained pixels. Initially hidden images remain uncreated until visible; source/provider changes invalidate retained display state. No Flutter SDK modification or native HTTP cancellation is involved.
## D5c3 accepted 438cef0d; next consumer wiring
- Astra SPEC PASS then QUALITY PASS after actual three-file diff/call-chain/tests/log review. Gallery retains only current and adjacent preview scopes, promotes reused current priority, cancels leaving/disposed owners, immediately handles unseen errors, and never starts transient out-of-window PageView children. Page scope is homeserver/currentUser/room; per-image identity uses authenticated _previewKey.identity. Identity-keyed descendant resets stale preview/original viewer state, while stable-id root supports history anchoring.
- didUpdate preserves gallery-fetched history provenance, honors authoritative parent metadata, handles empty/deleted/current/initial changes, and guards stale source pagination. Source replacement clears zoom and stale anchor locks. Real double-tap then source replacement verifies gallery scrolling unlocks. Failure retry rewarms current preview.
- Final gallery/image-viewer/preview-cache gate33pass EXIT0 (d5c3-gallery-imageviewer-cache-final-gate.log); three-file analyzer clean EXIT0; parent actual hashes matched final manifest, saved astra-d5c3-reviewed-inputs.json. Original source/history/window reds retained in d5c3-identity-red.log. Parent interim gate31pass1fail was a transient preview spinner; settled diagnostic checks actual target viewer and absence of ALL indicators, not just history indicator. Earlier weakened finder and compile attempts are not final acceptance evidence.
- D5c5a / PERF05-M3 exact next ownership: contain_image_bubble.dart consumer/source/visibility regions and focused new/existing bubble tests only. Optional sourceIdentity, visible owner scope for local probe and network, generation guards, canceled retry on visibility return, stable metadata geometry and existing outgoing defer/scroll-idle/refresh behavior. First add compatible declaration and prove behavioralred with existing four ownership fixtures. Actual animation wrapper wiring and two RoomPage identity sites follow separate accepted helper review. No Matrix/media-cache model edits needed.
- D5c4 remains pending final preemption evidence: shared default budget, real decoded first frame retained while ticker=false, animated/static transitions, source-key reset, and immediate high-priority viewer insertion must not briefly enable three tickers. Newly granted owner activation is scheduled with revocation of the former owner. No device frame/native transport claims.
### D5d / PERF00 observability exact follow-up
After consumer wiring stabilizes, instrument only closed counters on actual paths: MediaMemoryCache.get successful reuse (per cache layer), existing memory/content-flight joins (per layer), validated MediaCache.cached disk hits (per probe), and mediaDownload inside the scheduler-executed decrypt callback rather than queue admission. Counters are diagnostic events, not a logical-message cache-hit denominator; canceled queued tasks must increment no download. No user/content/URL/account identifiers, persisted telemetry or uploads. Keep disabled fast path and bounded existing samples; do not change integrity or scheduling.
WeChatComposer CupertinoTextField.onChanged can capture the first user-input callback until the next Flutter frame, coalesced per frame, with mounted/controller generation fences. Programmatic controller changes and focus must not count as typing; this is callback-to-frame latency, not IME-to-physical-display latency. Exact allowed files: media_cache.dart counter hooks only, wechat_composer.dart hook only, focused new telemetry integration tests and existing PerformanceMetrics tests. Default-disabled and --dart-define=CHATFLOW_PERFORMANCE_METRICS=true runs must both be explicit; do not skip enabled assertions and claim coverage. This does not require a new analytics service or platform dependency.
## D5c4 accepted e15836d2
Astra SPEC then QUALITY PASS after actual wrapper/token/Image call-chain and six test review. Shared cap2 includes default callers; higher-priority insertion initially produced three active tickers (d5c4-preemption-red.log EXIT1). New grant and prior revocation now apply in the same scheduled update. Paused visible images retain real decoded first frames through pinned Flutter3.44.9 Image behavior; tests precache a valid PNG and inspect RawImage/RenderImage, not a fabricated frame callback. Static/animated rebinds, provider-key replacement, hidden-before-first-visibility geometry and all fixture finally unmounts reviewed. This tests SDK image/ticker behavior, not native GIF frame rate or device memory.
Combined helper/activity/visibility16pass (d5c4-reviewed-candidate-gate.log); after final narrow geometry/brace/cleanup changes, current six wrapper tests pass (d5c4-final-geometry-tests.log) and two-file analyzer clean (d5c4-final-analyze-clean.log). Exact helper/test/lock hashes matched parent manifest astra-d5c4-reviewed-inputs.json. Older candidate failures and manual frame-callback attempts are not accepted final evidence. Wrapper foundation only until production bubble/viewer wiring lands.
D5c5a now may integrate this accepted wrapper at the actual ContainImageBubble Image site while implementing visibility/consumer ownership; RoomPage identity wiring follows that reviewed behavior. D5d executor prepares only actual memory-cache telemetry tests independently, without source edits or Flutter while bubble holds its compiler slot. Current checkpoint timing is recorded at append time; resumed review-loop start/durations remain unknown apart from command logs. No production, device, push or publication operations.
Checkpoint: 2026-09-11T16:37:30.2254583+08:00

## D5c5a accepted d691acd4; D5c5b and D5c6 next
Astra SPEC then QUALITY PASS after actual three-file diff, loader/scope/Image call-chain, six new lifecycle fixtures and final evidence identity review. ContainImageBubble now owns local-probe/source scopes only while viewport/app/route eligible, fences stale source generations, retries canceled demand and retains completed bytes. Actual BudgetedMediaImage uses the accepted shared cap; initial compressed cache bytes do not bypass visibility. Completed refreshFromSource media no longer reloads after visibility return. Source identity resets dimensions and prior display state, while outgoing defer/scroll-idle behavior stays intact.
Final focused45pass EXIT0 (d5c5-bubble-focused-gate.log), scoped analyzer clean EXIT0 (d5c5-bubble-analyze.log), actual three hashes matched in astra-d5c5-reviewed-inputs.json. Controlled behavioral red restored exact earlier candidate SHA; it is not the final hash. Existing warm-scrolling test now waits one visibility-layout pump, with loader0 and no spinner; no claim of literal initial-frame display before visibility is known. Rejected initialBytes visibility bypass is removed and a real initially offscreen warm GIF fixture catches it. No native device/frame-rate acceptance.
D5c5b / PERF05-M3 owns ONLY the two actual RoomPage ContainImageBubble sourceIdentity bindings and narrowly relevant tests. Use stable event plus authenticated preview identity, preserve all other callbacks/outgoing/cache policy. Shared RoomPage changes serialized; existing gallery scope already accepted.
D5c6 / PERF05-M4 follows: ImageViewerPage regions of encrypted_media_view.dart, room_image_gallery.dart visibility/current-viewer wiring, and focused viewer/gallery tests. Viewer original request gets interactive owned scope and source generation, canceled on hidden/route/app/source/dispose, stale bytes/dimensions cannot adopt; canceled demand can retry and is not permanent error. Explicit save has separate operation scope, survives permission-dialog lifecycle changes, and captures current source before permission await; source replacement/dispose invalidate it before saving. Edit cannot open a stale or preview fallback after canceled original. Actual image uses BudgetedMediaImage priority100; gallery grants current-page eligibility only. Gallery full-viewport MediaVisibility cancels pending preview participation while hidden, retains completed previews, rewarms only current±1 on return. Preserve history epochs, stable anchors, zoom/edit/forward/favorite actions; no new SDK/cache model changes. Add behavioral lifecycle/source/permission/current-only regressions before implementation and run existing viewer/gallery/preview/bubble gates. Flutter compiler inputs serialized with D5d media_cache hook edits.
D5d tests prepared only, production hooks unmodified. Parent caught Matcher parameter called with int before first compiler run; executor fixes it and adds queued-cancel no-download and controller-generation coverage. Final counters remain per-layer diagnostic events, not logical-message hit ratios. Next available compiler slot goes to D5d red and hook implementation while D5c6 prepares disjoint tests only.
Checkpoint: 2026-09-11T16:48:39.6318630+08:00

## D5c5b accepted e64d4b9a; D5d accepted b705350c
Astra SPEC then QUALITY PASS after inspecting both RoomPage call sites, roomLease.mediaCacheKey/preview identity chain and actual file hash. Only stable message/authenticated preview identity arguments changed. d5c5b-room-bubble-focused-gate.log17pass EXIT0 and scoped analyzer clean EXIT0; astra-d5c5b-reviewed-inputs.json matches final source.
D5d actual two-source diff and all ten integration fixtures reviewed: validated disk probes, successful memory get, existing memory/content flight joins, and scheduler-executed decrypt have closed counters; no identifiers/uploads, no cache policy/scheduler changes. Composer measures real onChanged→next Flutter frame only when enabled, one sample per controller generation/frame, old callbacks cannot clear a newer generation; programmatic/focus updates do not count. This is not IME→physical-display latency. Native frame/codec counters not invented from declared enums.
Behavioral red missing counters/samples: d5d-metrics-enabled-red.log EXIT1, six intended failures. Final d5d-final-enabled-green2.log10pass and d5d-final-disabled-green2.log10pass EXIT0, both contain exact command headers; d5d-final-focused-gate.log43pass covers final production hooks before one added content-flight test. Final four-file analyzer clean EXIT0; actual source/test/lock hashes all matched manifest in astra-d5d-reviewed-inputs.json. Content-flight fixture shares one held decrypt and asserts one join; last queued-owner cancellation asserts zero decrypt/download. Per-layer events are not a logical-message hit denominator.
D5c6a viewer executor now owns Flutter slot and ImageViewerPage/imports + existing/new focused viewer tests. D5c6b second Terra prepares only disjoint gallery visibility tests; no production edits or Flutter until slot transfer. Parent owns consolidated local acceptance report and final platform preflight. Android SDK36/build-tools36/JDK17 available; no iOS native environment or .env. No publication/device/production operations.
Checkpoint: 2026-09-11T16:55:49.2990770+08:00

## D5c6a accepted 05b7ebf9
Astra SPEC then QUALITY PASS after final actual three-file diff, original/edit/save call-chain,11new lifecycle fixtures, final existing-viewer regression and hash review. Original loads own interactive scope; source/request/visibility fences occur before dimension decoding and before adoption. Edit owns a separate revision so canceled held work releases its busy state and cannot open a stale editor; editor bytes/callbacks captured before route creation. Save has single-flight source-owned scope, captured inputs before permission, source/scope checks before native save and before success/error hint; hidden permission flow survives, source replacement/disposal cancels owner. Already-issued native save cannot be undone by canceling a Dart Future; no such claim.
Actual Image uses accepted wrapper priority100 and full-image geometry visibility; existing static2048/GIF720 decode bounds and contain/zoom/actions retained. Listener detachment applies only to source-reset; doubletap in/out remains observable, tested with real StatefulBuilder parent callback and scale3→1→3→source-reset1. Initial two-tap fixture expected two doubletaps and was rejected as test error, not production red. First two old image tests now wait one visibility pump; no blanket suppression of decoder/Flutter errors.
Final d5c6-viewer-final.log19pass EXIT0; d5c6-viewer-final-analyze.log3files clean EXIT0; all final source/test hashes matched astra-d5c6a-reviewed-inputs.json. Initial50-test green predates root-requested corrections and is not final identity. Full integration gate will follow gallery wiring. New tests prove held-save source replacement; disposal code cancels the same save owner, but no separate save-dispose widget fixture is claimed. Native platform remains mocked.
D5c6b now owns room_image_gallery.dart plus prepared visibility test and existing gallery test only if minimal lifecycle timing adjustment is necessary. Flutter slot transferred to that Terra; viewer files frozen. Next: bounded gallery lifecycle/source/current-only tests, then final source freeze and full local Flutter/available Android compilation. No device or production operations.
Checkpoint: 2026-09-11T17:08:31.5636132+08:00

### PERF07-G2 decoded-image gesture regression: architecture amendment
D5c6b six new visibility/empty cases and14legacy lifecycle cases pass, but actual decoded GIF drag remains on PageController.page2.0 (d5c6b-gallery-gesture-diagnostic.log EXIT1). Existing swipe test held preview futures, so no InteractiveViewer participated. Astra inspected pinned Flutter3.44.9 InteractiveViewer: ScaleGestureRecognizer callbacks remain active even when panEnabled=false; its accepted single-pointer pan does not reach PageView. This is a real integration defect, not grounds to weaken the actual gesture fixture or force controller jumps in tests.
Exact serial tasks: G2a viewer owner adds optional public onInteractionStart/Update/End passthrough to ImageViewerPage's existing InteractiveViewer, preserving standalone callers; no SDK/plugin/gesture replacement. G2b gallery owner bridges a single unzoomed horizontal interaction to PageController.position.drag and standard Drag.update/end/cancel, retaining PageScrollPhysics snapping/velocity/boundaries. PointerCount>=2 or zoomed cancels the paging drag and preserves InteractiveViewer pinch/pan. Drag owner remains stable across onPageChanged and must not be lost when the departing page stops being current. Source/initial replacement, history anchor changes, route/app hide and dispose cancel drag. Outer pointer-up/cancel fallback closes an orphaned drag if its child is evicted; normal ScaleEnd gets first chance to end with velocity. No manual timer/fullhistory scan, transport or cache policy changes.
Tests: actual completed-image partialdrag mounts current+neighbor with onlycurrent animationeligible, release snaps to next; zoomed onefinger pans image without paging; twofinger pinch stillchanges scale without paging; first/last bounds, hide/dispose/source duringdrag release, existing source/history/zoom/editor/forward suites. API sharedsource edits and Flutter slot serialized. Viewer bridge standalone19-test evidence may be reused with the later actual gallery integration gate only when source hashes match.
Checkpoint: 2026-09-11T17:16:20.6530388+08:00

## D5c6b / PERF07-G2 accepted 96b24e80; full gate triage
Astra SPEC then QUALITY PASS after actual three-file diff/call-chain and final gallery/viewer/lock identity comparison (astra-g2-reviewed-inputs.json). Gallery full-viewport visibility now covers empty/nonempty branches, cancels pending consumers while hidden, preserves settled previews, resumes only current±1, and passes source/current eligibility into real ImageViewerPage. Actual decoded-image gestures bridge InteractiveViewer callbacks to standard ScrollPosition.drag, preserve pinch/zoom pan, cancel on lifecycle/source/anchor changes and close orphaned drags after pointer release. A real decoded GIF reproduced the formerly hidden paging defect; held-preview legacy tests did not cover it.
Final g2b-gallery-visibility-strengthened.log9pass EXIT0, two-file analyzer clean; g2b-gallery-legacy-lifecycle.log14pass EXIT0. Earlier candidate green-named logs include failures and are not final proof. Viewer standalone G2a direct output was not persisted; earlier19-test log predates the optional callback bridge and is not claimed as final bridge evidence. Final actual gallery integration and upcoming full gate include the bridge.
Source frozen for parent full Flutter tests/analyze, run-final-flutter-gates.ps1 with all source/test/lock identities. Full suite currently found43 failures and one non-completing verification lifecycle fixture; not yet a completed gate. PERF08-R1 diagnosis concerns moments restart fixture using intentionally unavailable SQLite, outdated direct navigation source string, emoji fake digest, video fixture target sendability. PERF08-R2 owns image_edge_layout_test.dart only: wait real decode rather than fixed delay, keep all geometry assertions. Wallet source/test failures require baseline classification; no financial production changes authorized in this batch. No app edit/second Flutter process until first full gate exits. Parent owns subsequent full rerun and Android source compilation; no device or publication operations.

## R1/R2 accepted; final candidate7f41852a
R1 f2ccf235: Astra actual four-file diff, positive/negative fixture call chains and5source/lock identities checked (astra-r1-reviewed-inputs.json). Real SQLite restart/clear/account isolation retained with portable unique artifact DB and teardown; encrypted fake upload/decrypt content matches; target send permissions explicit; warmup source contract matches accepted API. Final functional30pass/analyzer4clean r1-final-v1 logs, then dart-format only in r1-format-v1-inputs.json; second full gate includes formatted identity. Initial predecessor import/wrong metadata/path/cleanup mistakes were rejected before acceptance. Nonessential predecessor r1-moments/moments.db remains ignored: attempted exact deletion rejected by automatic tool policy before execution, reason only 'blocked by policy'; no filesystem error or permission request inferred, no repeated deletion needed for task.
R2 7f41852a: SPEC then QUALITY PASS. All seven original geometry/zoom/rotation tests now await actual same bounded ImageProvider decoding after visibility publication. Only editor production change is mounted-if braces; no behavior change. Verification fake-async fixture previously stalled before its held suspend hook; temporary boundary logs narrowed it to lifecycle Future execution. Single-file native zero-event-turn variant v8 passed but subsequent direct-await variant v9 failed27pass1timeout, so they are not conflated. Final fixture constructs all lifecycle objects and begins suspend inside tester.runAsync, awaits real held hook, mounts/removes actual page, releases and awaits real suspension, then pumps UI; asserts factory1, listeners0 before cancellation, final listens==cancels and no Flutter exception. No arbitrary timer/poll helper/diagnostic prints retained; pertest bound30s.
Final exact-input r2-final-v12-flutter-test.log28pass EXIT0, r2-final-v11-flutter-analyze.log5items clean EXIT0; allfinal4source/lock hashes checked astra-r2-final-reviewed-inputs.json. Diagnostic Matrix source restored exactly E4562997908E57FA715F0ADF5B0A790143D6FA83A4E963BC44C122EC61001FA0, parent independently checked. Native event-loop fixture diagnosis does not change E2EE lifecycle production ordering or permissions. Earlier 'final' candidate-named failure logs remain red evidence, not passing claims.
Parent has frozen all app sources/tests and launched run-final-flutter-gates-v2.ps1; historical firstfull logs preserved. Android source compilation follows; no install/rebuild/distribution/signing changes, user device acceptance and nativeiOS constraints remain. Detailed119local doc links checked with zero broken links.

## Final local acceptance checkpoint
Astra personally reviewed actual cross-module call chains, candidate diff, final focused/full evidence and identities. Performance local implementation PERF01–PERF07 is complete; PERF00 device measurements and PERF08 overall acceptance remain partial. All15 performance-related firstfull failures resolved in secondfull;2199pass29walletfail, fullanalyze clean. Remaining wallet/API/worker/boundary failures are retained in report, not suppressed or converted to green. No financial/E2EE/Getui behavioral weakening. No automatic publication/device/production actions.
Android source gate exit0 in113.823s: standard debug arm64, public endpoint defines, JDK17 and SDK36. pubspec.lock unchanged SHA7B9D31B1CB4F394EDF5C2DA54778F00933D79591DF87E8C4ADDEA783D5A2A2B7; post-Android703input hashes unchanged. aapt2 badging exit0: com.liuhetong.mobile.debug,0.3.81/2085,arm64-v8a,debuggable. This is existing source version, not a new delivered version; build intermediate only, no conventional rebuild/stable delivery signing/install. APK149736725bytes SHA0BD2108EAFE437CF2D56B1E8EFEBBF5F9C5E21DF2C9C5FFBBB70864C03ED982A. Existing plugin KGP migration/obsolete API/unchecked warnings recorded, no dependency upgrade. Evidence: astra-android-source-build-result.json, astra-android-source-badging.log, astra-post-android-input-identity.json.
NativeiOS unavailable onWindows; live Mi6/iPhone FPS/IME/network/lifecycle acceptance belongs to user. Full scripts/verify.ps1 environment not present; independently runnable gates already executed/reused with recorded failures. Next executable work requires its own scope: reconcile old wallet regression fixtures, existing API/boundary failures with relevant module owner; run nativeiOS build in Xcode environment and user-device performance scenarios on a future authorized delivery. No further local source build is needed for these documentation-only final updates.
Preserved unrelated Getui demo generated changes and existing video_playback_arbiter.dart whitespace-only difference; no reset/overwrite. Detailed source/test inventory and119local links verified. Overall resumed active/repair elapsed time remains unknown; command-stage durations above are observed, not inferred.
