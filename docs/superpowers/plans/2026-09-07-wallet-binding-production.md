# 私人钱包绑定及后台流水生产接入

> 执行subagent-driven-development；用户已批准ADR-0012和部署，先测试、领域审阅，再安全审阅。真实签名与双源缺失不伪装为已经开放充提。

**Goal:** 接入用户钱包控制权绑定和规则，后台逐笔可查真实观察流水，部署可验证且可回退。
**Architecture:** 新绑定模块保存不可变版本和一次性挑战，TronGrid观察库通过受限只读公共查询服务接入RBAC管理API；前端真实调用，资金门禁保留。

**2026-09-07执行进度：** 后台链上查询及管理页面、10USDT规则、绑定核心与受限API已实现并完成两轮审阅；PG18及生产同版本PG16隔离验证通过。SSH恢复后已备份并于13:48（香港时间）完成0042/API/Worker/后台发布，线上健康和只读查询通过。真实适配器、充值意图和提现绑定目标仍未接完，生产管理员页面验收待用户登录。详见`docs/verification/2026-09-07-wallet-binding-deployment.md`，未完成集成不提前勾选。

- [ ] 绑定后端独立任务：新binding_models.py、binding.py、message_signature.py、wallet_binding.py路由及测试，下一可用迁移号；持久挑战、验签、30天、幂等/审计Outbox、在途禁止改绑、绑定区块PENDING状态不伪装ACTIVE。真实MFA与最终性依赖须明确保留。
- [ ] 后台查询独立任务：观察库只读查询、分页/筛选/详情、不可用明确错误；FINANCE_REVIEW/AUDIT权限、审计，金额字符串，流水不当作已入账。不能给客户端暴露读取文件路径能力。
- [ ] 前端独立任务：管理钱包页接真实链上流水列表、完整分页与详情/状态；复用现有安全渲染和登录，不用演示数据掩盖错误。Figma延用用户先前允许暂缓同步的例外，契约记录需如实更新。
- [ ] Root接线：合并路由/配置/OpenAPI、10USDT门槛及用户入口集成，更新锁定依赖，完成新PG迁移及生产隔离演练。
- [x] 全量/专项回归、领域后安全审阅；验证生产现状与私有备份后部署API/Worker及管理前端，只读挂载观察数据；不重置现有观察进度、不启用签名和自动入账。
- [ ] 实際后台登录与逐笔查询验收；生产无测试身份时使用已授权管理员会话，不伪造或导出用户令牌，缺失验证明确记录。

所有证据放docs/verification/artifacts/2026-09-07/wallet-binding-production；所有shell为pwsh7并设UTF8；不提交真实地址/私钥/数据库或密钥。工作区既有用户改动不覆盖、不重置。代码实施与外部钱包适配需以真实支持能力验收，不承诺未实现设备签名。

## 正式更新弹窗（用户于本次会话追加授权）

用户已明确选择面向正式用户发布，并沿用线上正式证书。先核验登录后台与发布包，再修改更新设置。本阶段仅拥有 `docs/verification/artifacts/2026-09-07/official-wallet-update/` 下打包/发布证据和对应文档，不改变资金门禁。

- [x] 读取线上发布基线：0.3.46 / build 49 / min 3，下载上一版并核验原发行证书。
- [x] 当前已审阅钱包源码专项测试 7 项通过，以 0.3.50 / build 52、standard/release/ARM64 和三项正式 HTTPS 参数构建。
- [x] 完成 Apktool 重建、固定发行签名、对齐、清单、代码及资产校验。
- [x] 验收生产后台链上列表和详情；由用户实测确认两笔列表和各自详情正常，自动浏览器验收受连接问题限制。
- [x] 上传不可变最终包，核验本地/服务器/公网完整 SHA256。
- [x] 以正式发布服务和审计原子更新五项设置，原子切换下载符号链接；2026-09-07 14:54 香港时间发布，5 条审计通过。
- [x] 回读客户端更新数据并记录结果。真实绑定和充提继续关闭，文案明确未开放。
