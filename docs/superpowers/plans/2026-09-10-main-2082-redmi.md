# Main 收敛与 Redmi Debug 2082

授权：用户明确要求合并其他分支到 main、提交、只保留 main，并在现有修改及 0.3.77-debug / 2081 基础上推送 Redmi。

1. 审计全部分支、工作树与未提交修改。已提交分支均为 main 祖先；旧 root / iOS 工作树内容按三方基线逐项迁移，保留所有原件快照，不以旧目录全量覆盖 main。
2. 使用 main 的 2081 会话协调、账号租约、媒体/朋友圈隐私和四项顶部菜单，迁入本次会话成员复验、明确 peer、错误提示、显示名、菜单宽度和朋友圈分页修复。合并 root 其他已批准且尚未包含的后台/监控增量；已被 main 新实现取代的旧稿仅保留快照。
3. 运行冲突相关回归、Flutter analyze/full test、HTML/UI contract、仓库 verify，并进行规格及质量/安全复核。
4. 递增 0.3.78+2082，从该 main 源码构建 ARM64 standard debug；保留三个 HTTPS define、并行 .debug 包名与现有固定签名。遵循 APK rebuild runbook，核实重建语义、资产、签名与对齐后 adb install -r Redmi，不卸载/清数据。
5. 暂存审查过的源码/文档，排除秘密和生成产物后提交 main。所有旧分支祖先关系确认后将保留工作树 detach，删除已合入分支，根目录切至 main；非代码原件与归档保留可恢复。本地与远端分支状况分别验证，不强推 main。

责任：主线程拥有整合工作树；独立审查只读。除用户指定设备安装外，不执行生产 API 部署或真实资金/聊天写入。
