# 客服订单工作台 UI 整理
用户2026-09-23八项明确要求为本批设计依据：先交付可交互HTML demo及实际共享页面实现，暂不自动发布新版到生产。
1. 顶部参考汇率与资产信息卡；右上角icon刷新，加载/成功/错误可见，保留过期标识。
2. 主区充值/提现业务切换，充值筛选“待处理请求”“我正在处理”，显著选中态及说明；用户显示权威昵称和畅聊号；主按钮“处理请求”，辅助状态放按钮下。
3. 普通客服不显示目录配置、群主转让工具和底层登记排障；订单查询保留，时间线进详情。管理员高级工具收纳在折叠区，沿用权限。
4. 铃铛未读提醒；初始化历史只设基线，新的充值/提现申请弹侧边通知，状态更新刷新订单但不当新申请；可关闭和已读，错误恢复/退出清理。
5. 使用当前admin-card/admin-filter/admin-primary/admin-secondary、主题token、原gateway/模态样式，不新增组件库或修改Flutter。
6. 根负责recharge/payout页面、CSS、home/dashboard接线、demo及浏览器验收；独立agent负责notification与订单用户只读投影；文件不交叉。
7. 红绿测试、完整frontend、UI/OpenAPI检查；verify按影响复用上轮未变后端输入，只对新增用户投影运行服务端相关门禁；保留真实退出码。用户要求demo，提供本地HTTP地址与交互说明；不发真实短信/不操作资金。
8. 用户追加确认：所有充值/提现处理操作进入原生操作弹窗，列表只保留摘要与入口；沿用 admin-proof-dialog。关闭不释放服务端处理权；保留汇率草稿，失败/成功提示在弹窗内可见，刷新与续租仍验证服务端权威状态。

## 用户追加授权：直接下发与发布
2026-09-23 用户明确要求已核验到账充值由客服直接下发，不再独立管理员审批；拒绝原因下拉默认用户未及时支付；完成后部署生产。限定取消该充值结算审批，保留到账、处理权、幂等账本与审计；旧调整复用并防二次入账。配套手机UI由独立计划覆盖。根负责工作台UI/集成发布，后端代理拥有recharge/finance域，移动代理拥有红包/充值UI，发布代理只准备工件脚本。先领域规格后安全评审、专项与实际PG验证、冻结清单，再生产增量部署；保留线上diagnostics。
