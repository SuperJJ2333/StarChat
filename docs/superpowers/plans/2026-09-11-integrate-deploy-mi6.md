# 合并 main、生产部署与 Mi 6 Debug 交付

## 授权与边界

2026-09-11 用户明确要求合并其余分支到 main、推送 Git、通过跳板机部署生产，并安装 Debug 到 Mi 6 由用户测试。该授权覆盖本次必要的提交、合并、推送、服务更新和保留数据覆盖安装。保持原有资金/E2EE/个推与审计约束，不覆盖未完成本地改动，不删除分支或工作树，不向正式用户推送 Debug 更新。

Astra 规划、检查实际差异及最终验收；已显式 gpt-5.6-terra 执行者负责独立预检、版本修改和交付脚本/定向验证。初始最多两执行者，同文件与 Flutter 编译槽串行。

## 实施批次与验收

- [ ] R1 分支审计：记录全部本地分支、工作树脏改动、origin/main；提交已审 finance 80 文件，不包含 Getui 生成物和无关格式改动。主 main 文档清理、锁文件和其他未提交内容独立保留。
- [ ] R2 集成：按祖先关系合并所有未纳入 main 的分支，冲突保留两侧业务能力；检查无遗漏分支，原有财务/E2EE约束不变。合并后按影响补验，已证实无关基线失败继续公开，不弱化断言。
- [ ] R3 固定候选：根据 Mi6/本地/生产版本选新的 Debug build，Terra只修改版本契约及必要交付脚本，Astra审查。提交main并正常push（无force），远端SHA核对。
- [ ] R4 生产：跳板只读现有容器/digest/Compose/schema，冻结清单和回退。基于当前镜像最小覆盖已审文件，备份留服务器0700目录，必要隔离测试；只更新明确涉及的服务和静态文件，检查健康/未授权拒绝/资源SHA/其他容器不变。
- [ ] R5 Android：最终源代码正常pub生成→ARM64 standard Debug源码构建→Apktool2.12.1→zipalign36→用户固定签名→清单/ABI/DEX/资产/证书/SHA核验→指定Mi6保留数据安装；不卸载、不清数据、不降级。功能/性能真机测试由用户负责。
- [ ] R6 记录：准确区分Git、生产服务、Debug已安装及用户待测试。记录命令、退出码、最终SHA/签名/版本、备份/回退、缺口与耗时；停止自己创建的临时资源。

## 已知基线与复用

finance 最终Flutter2310通过/29与performance基线一致的wallet失败；全analyze、ARM64源码编译通过；Node金融7与浏览器3、契约26/355通过。后端全量1829通过/11失败/52跳过，新增2 PIN夹具修正后8通过，其余9既有失败。生产Linux金融门禁优先隔离运行；未验证生产行为不写成已通过。若合并/候选改变相关输入，补跑相应测试。
