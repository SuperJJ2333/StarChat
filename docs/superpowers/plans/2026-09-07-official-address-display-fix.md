# 官方固定地址展示修复

沿用用户已批准的固定官方地址、TronGrid 只读监控和人工付款方案。用户要求获取充值地址返回官方地址及二维码；本次修复错误的旧托管接口接线，不开放资金。

1. root：新增经过真实会话校验的只读官方地址接口，从服务端保护配置获取并校验地址，返回网络、最低10 USDT、资金开关和说明；缺配置返回明确503，禁止缓存，不写账。
2. 实现代理：Flutter获取地址改新接口；加载互斥、清除失效旧值、Base58Check、地址/二维码/复制同源、资金未开放说明和可操作错误信息。复用现有QR依赖及UI组件。
3. root：更新HTML设计状态、registry与Figma export ledger；按用户此前明确授权暂不修改远端Figma。先功能规格，再安全审阅，聚焦测试与全仓检查。
4. root：保留生产现有镜像作为基线，仅叠加路由文件、旧wallet路由的挂载和Settings单个地址字段；先备份、旧镜像依赖/补丁隔离验收，再追加独立Compose层发布API。保留全部现有Compose层、旧资金门禁、Worker、数据库结构和历史账本。错误时还原原API镜像/Compose集合。
5. root：重建ARM64 debug 0.3.54-debug/2056，沿用MI6现有debug证书；代码/资产/清单/签名/设备hash核对后覆盖安装并实机点击验证。不得以已显示地址冒充入账已启用。

临时和验证文件仅位于 `docs/verification/artifacts/2026-09-07/official-address-fix/`。真实官方地址只放保护的部署配置，不进入源代码和测试夹具。
