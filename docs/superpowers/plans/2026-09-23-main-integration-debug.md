# 2026-09-23 main 整合与 Mi6 Debug 交付计划

用户明确批准：合并其他分支到 main、保持 Git 整洁、安装新 Debug 到真机。保留所有已有未提交工作及 Mi6 数据；不授权推送远端或重新部署生产。

1. 保存分支/工作树清单、tracked patch 和 untracked 备份，形成当前完成修复的提交。
2. 合并 phone-wallet-live-compat，再合并 Android 0.4.0 发布分支剩余提交；手动合并手机 UI 与 durable refresh、聊天可靠性，保留 schema0083 及现网后端。
3. 核对其余分支祖先关系。清理已合并且干净的本任务工作树；脏树保留，避免把旧审查版本覆盖新修复。
4. 对最终源码执行 Flutter 全量/analyze、受影响契约及完整仓库适用门禁。复用证据必须证明输入一致。
5. 冻结新版本与来源提交，固定签名重建 Debug APK，验证后覆盖安装 Mi6，保留数据。
6. 写明实际安装身份、测试与未验证项目，更新当前状态索引。
