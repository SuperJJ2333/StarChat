# 会话流畅性、群管理与群公告实施计划

状态：2026-09-06 用户逐项规格授权执行；独立 worktree，保留原目录钱包未提交修改。缺陷与新功能分开验证。

## 缺陷修复
1. 主任务 owns room_page.dart、room timeline send/scroll controllers 与测试：本地气泡同步插入；发送中无转圈/进度/禁用输入；发送锚定最新气泡在布局后立即可见；异步回执不重排/不延迟一秒；重试保持既有失败替换语义。
2. 图片任务 owns contain_image_bubble.dart、media_cache.dart 或新增 image缓存/布局辅助及测试；root 接 room_page。缓存命中同步渲染稳定尺寸；内存有界/本地持久缓存/在途去重；未缓存图片只在滚动停止后异步加载，已有缓存不闪占位，布局不得改变滚动锚点。
3. 相册任务 owns device_gallery_source.dart、相册选择页及测试；root 接 room_page 若必要。每次打开/恢复主动刷新媒体索引，系统变化事件刷新，时间倒序，先显示已有预览并后台刷新，权限撤销/部分授权正确，避免整页串行解码阻塞首屏。
4. 群管理任务 owns group_chat_info_controller/page 与相关 gateway、新公告服务/页面和测试；root 接 room_page。修复真正权限/状态/操作链路；仅群主能设管理员/转让/解散，群主管理员可管理其余项，普通成员拒绝，失败可见、写入后刷新、危险操作二次确认。

## 新功能
5. 管理任务实现图文混合群公告编辑/查看，使用现有端到端加密消息/附件承载内容；公开状态如需使用仅保存不含正文/附件密钥的引用，沿用 Matrix 权限，不改变金融/auth/E2EE协议。root 在 RoomPage 接独立公告栏，有效公告显示，空公告隐藏；普通成员只读，管理员可编辑。

## 验收与交付
测试先行，逐项红绿；root 集成并进行规格审查再质量审查，Flutter analyze/相关完整测试、UI contract、scripts/verify.ps1。使用真实 gateway，不留占位按钮。前端 HTML/registry/ledger同步既有组件，Figma无工具时如实记录远端未同步，不伪造PASS。本任务先交付代码与证据；不擅自修改其他工作目录版本号、签名或发布钱包代码。任何打包需沿用已确认不降级且签名一致的升级规则，未连接设备不声称真机通过。
