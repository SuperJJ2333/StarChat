# Mi6 chat reliability debug repair

User authorization: repair reported red-packet stale limit, switching-room failed bubbles and insufficient automatic identity recovery; build/install debug to Mi6 for personal testing. Latest constraint supersedes earlier publication authority: NO new public version/update popup. Existing financial authorization does not require changing balances/formulas; this scope changes client presentation/lifecycle only. Preserve installed data and verify device/package/signer before adb install -r. Device absent at first ADB enumeration; asked user to connect while independent code work continues.

Design: authoritative runtime red-packet limits load independently from balance; unknown/failure cannot masquerade as200, explicit retry and subsequent-entry refresh. Outbox send completion persists regardless of disposed page; logical historical-source pending status stays pending, actual failures stay failed, original transaction identity preserved. Identity repair uses existing authority/membership evidence with scheduled exponential backoff and network/resume triggers, bounded parallel per-peer recovery with incremental refresh; no speculative two-member/name merging or room deletion. Add targeted authoritative state hydration if needed to recover missing explicit room-kind evidence. Fixed signing and APK rebuild apply to debug package; no release settings publication. Telegram comparison uses public authoritative documentation; do not assert proprietary WeChat implementation.

Acceptance: redgreen regressions for each defect, focused then shared full analyze/tests and applicable repository checks; source/installed package evidence; installation blocks only if Mi6 unavailable or signature continuity cannot be preserved. No plaintext chat logging, secrets, message sends on behalf of user, or financial writes during verification. User performs actual messaging tests.
