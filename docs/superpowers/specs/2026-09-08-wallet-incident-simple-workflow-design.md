# 监控与事故简化设计（用户已确认）

用户于 2026-09-08 明确选择“采用这个简化方案（推荐）”。

## 问题与已核实事实

用户要求监控与事故尽可能简单，明确解释事故含义、复核失败原因及处理办法。事故 3f86ae92-038c-4816-b40d-b92a887bbb6e 的原始数据源异常发生于 2026-09-08 17:08:55 香港时间；23:01 检查时已为 ACKNOWLEDGED，版本 71。最近链上观测 SOURCE_MATCHED，业务扫描与源 checkpoint 一致。没有据此认定资金已恢复。

复核接口将所有 incomplete 结果统一转换为 WALLET_MONITOR_UNAVAILABLE，丢弃具体 codes/status；现有前端又套用付款结果未知提示。这使等待同步和真正故障无法区分。此前 P0 升级还会改变版本，导致旧页面提交冲突。

## 推荐交互

页面顶部展示“资金已暂停 / 已启用”、阻断原因、最近检查时间。每起事故用中文标题、首次发生时间、当前处理状态、影响范围与下一步说明呈现。技术代码、ID、版本、证据摘要、详细时间线放入可展开详情。历史故障与当前健康状态明确分开；缺失的历史诊断不得编造。

只保留两个用户动作：

1. **检查并处理事故**：确认接手事故并输入操作凭证一次；系统按事故实际状态完成确认、实时复核和结案。显示具体进度。该操作不会启用资金。自动填写固定、可审计的原因代码，不让用户手写。
2. **核验并恢复资金**：事故处理完成后单独显示，需要明确确认和操作凭证；按原有后端规则核验并恢复。未通过则展示中文阻断原因及具体下一步。

只处理阻断事故；manual_liquidity 策略下确切匹配的 MANUAL_BACKING_DEFICIT P1 提醒明确标注“不阻止恢复”，保留记录，不强制结案。

## 错误与证据

- WALLET_MONITOR_UNAVAILABLE 保留原有顶层错误码以兼容，增加白名单化结构字段，传递真实检查状态、reason codes、是否可稍后重试。前端显示“等待业务流水同步”等准确原因。
- 版本冲突表示此次提交未执行，自动刷新并提示状态已更新；只对确定未提交的冲突进行有上限的重新准备，不无限重试。
- 断网、超时和未知服务器响应保留该步骤幂等记录，先查询状态，禁止自动继续后续步骤。不得使用“禁止重复付款”描述不涉及付款的事故操作。
- 复核未完成记录 ERROR/WARNING/INFO 的正确级别、安全 reason codes、trace ID 与事故 ID，禁止输出凭证、全钱包地址或第三方秘密。
- 当前诊断只读展示数据源健康、证据时效、扫描进度和限制来源。读取不改变事故或资金状态。

## 安全边界

复用现有确认、复核、结案、恢复接口及幂等、审计、Outbox。每一步在最新服务器状态上执行；不直接写数据库或伪造 clearance digest。凭证仅在当前操作内存短暂使用，完成/失败即清除，不保存到日志或浏览器存储。近期验证成功后继续显示当前步骤，不自动重放资金动作。

保留单设备、48 小时绝对期限、操作密码、权限、实时证据、限制归属以及恢复前全部安全检查。事故处理和资金恢复不能合并为自动放行。

## 备选

- 最小改动：保留三个事故按钮，仅改中文解释、隐藏技术参数。成本较低，但仍需用户理解确认/复核/结案顺序。
- 推荐：两个主要动作，内部编排事故处理，恢复独立确认。交互简单且保留安全边界。
- 不采用：自动关闭所有事故并自动恢复资金，无法保留必要的人为接手和恢复授权。

## 验证与交付

先复现复核原因丢失、版本冲突和断网后错误提示，再测试逐状态处理、同一步重试幂等、未知结果不继续、成功结案不恢复资金、独立恢复完整核验。域审查先于质量/安全审查。执行仓库检查并保存非敏感验证与发布证据。远程 Figma 仍按既有用户批准暂缓，维护本地界面登记。生产仅在检查通过后按既有部署授权发布；不替用户输入操作凭证或解除生产暂停。
