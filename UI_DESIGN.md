# 畅聊 ChatFlow UI 设计规范

**版本：** 1.2
**生效日期：** 2026-08-20
**适用范围：** Flutter Android/iOS 客户端
**设计语言：** 微信式信息架构与交互，保留“畅聊”独立品牌

**品牌文案：** 产品显示名统一为“畅聊 ChatFlow”；受空间限制的应用内文案可使用“畅聊”；账号标识使用“畅聊号”；需要品牌限定时，朋友圈显示为“畅聊朋友圈”，点钻红包显示为“畅聊点钻红包”。内部包名、Matrix 标识、资产代码和现有资源文件名保持不变。

## 1. 基本原则

1. 信息优先：减少装饰，使用清晰层级、稳定位置和即时反馈。
2. 双端一致：Android 与 iOS 使用同一套 Flutter 视觉组件，系统能力与权限流程可按平台适配。
3. 安全边界不变：主题与组件不得改变 Matrix E2EE、业务 API 权威状态、账本、红包和钱包安全规则。
4. 无障碍：支持系统字号、语义标签、对比度与减少动态效果。
5. 组件优先：业务页面不得自行硬编码颜色、字号、间距、圆角或动画时长。

## 2. 颜色变量

| Token | 浅色 | 深色 | 用途 |
|---|---:|---:|---|
| `brandPrimary` | `#07C160` | `#07C160` | 图标、选中态、徽标、成功态 |
| `brandPressed` | `#06AD56` | `#06AD56` | 品牌图标按压态 |
| `pageBackground` | `#EDEDED` | `#111111` | 页面底色 |
| `surfacePrimary` | `#F7F7F7` | `#191919` | 导航栏、输入区、卡片 |
| `surfaceElevated` | `#FFFFFF` | `#232323` | 弹窗、浮层、列表单元 |
| `textPrimary` | `#191919` | `#F5F5F5` | 标题、正文 |
| `textSecondary` | `#888888` | `#B2B2B2` | 描述、时间戳 |
| `textTertiary` | `#B2B2B2` | `#7A7A7A` | 占位、禁用文本 |
| `divider` | `#D9D9D9` | `#2C2C2C` | 1 物理像素分隔线 |
| `danger` | `#FA5151` | `#FA5151` | 错误、取消、未读标记 |
| `warning` | `#FA9D3B` | `#FA9D3B` | 等待、红包 |
| `bubbleOutgoing` | `#95EC69` | `#3A6B36` | 自己的消息气泡 |
| `bubbleIncoming` | `#FFFFFF` | `#2C2C2C` | 对方消息气泡 |
| `scrim` | `#80000000` | `#99000000` | 弹层遮罩 |

禁止在业务页面使用上述 Token 之外的硬编码颜色。资产状态颜色只能通过语义 Token 选择，不得用颜色作为唯一状态信号。

### 2.1 强制页面配色映射

下表为私聊、群聊和底部主导航根页面的**固定颜色约束**。这些页面必须通过对应颜色变量引用，不得用 `surfaceElevated`、品牌色、图片取色或页面局部硬编码替代；本表指定的色值优先于通用亮暗主题映射。

| 页面/区域 | 必用颜色变量 | 固定色值 | 禁止替代 |
|---|---|---:|---|
| 私聊消息页顶部导航栏 | `chatNavigationBackground` | `#F7F7F7` | `surfaceElevated`、`pageBackground`、品牌色或自定义灰色 |
| 群聊消息页顶部导航栏 | `chatNavigationBackground` | `#F7F7F7` | `surfaceElevated`、`pageBackground`、品牌色或自定义灰色 |
| 私聊消息页整体背景 | `chatPageBackground` | `#EDEDED` | 白色、`surfacePrimary`、图片背景或自定义灰色 |
| 群聊消息页整体背景 | `chatPageBackground` | `#EDEDED` | 白色、`surfacePrimary`、图片背景或自定义灰色 |
| “消息”主导航根页面 | `tabRootPageBackground` | `#EDEDED` | `surfaceElevated`、白色或自定义灰色 |
| “通讯录”主导航根页面 | `tabRootPageBackground` | `#EDEDED` | `surfaceElevated`、白色或自定义灰色 |
| “发现”主导航根页面 | `tabRootPageBackground` | `#EDEDED` | `surfaceElevated`、白色或自定义灰色 |
| “我”主导航根页面 | `tabRootPageBackground` | `#EDEDED` | `surfaceElevated`、白色或自定义灰色 |

`WeChatAppTheme` 必须导出 `chatNavigationBackground`、`chatPageBackground` 和 `tabRootPageBackground`；`WeChatPageScaffold`、私聊页、群聊页及四个主导航根页只能消费这些变量。消息气泡、列表单元、输入区和浮层仍按其专用 Token 取色，不得反向改变页面整体或导航栏背景。

## 3. 字体与字号

默认字体使用平台系统字体；中文优先 PingFang SC（iOS）与 Noto Sans CJK/系统字体（Android），不捆绑字体文件。

| Token | 字号 | 字重 | 行高 | 用途 |
|---|---:|---:|---:|---|
| `display` | 28 | 600 | 36 | 余额、大数字 |
| `title1` | 22 | 600 | 30 | 大标题 |
| `title2` | 18 | 600 | 25 | 导航标题、弹窗标题 |
| `body` | 17 | 400 | 24 | 消息、主要正文 |
| `callout` | 16 | 400 | 22 | 按钮、列表标题 |
| `subhead` | 14 | 400 | 20 | 描述、状态 |
| `caption` | 12 | 400 | 17 | 时间戳、辅助说明 |
| `badge` | 11 | 500 | 14 | 未读数量 |

正文最低 14sp；支持系统文字缩放 0.85–1.4，资产数字采用等宽数字特性。

## 4. 间距与圆角

间距基于 4dp 网格：`xs=4`、`sm=8`、`md=12`、`lg=16`、`xl=24`、`xxl=32`。

圆角：

- 小标签与输入附件：4dp
- 输入框与现代操作按钮：14dp
- 消息气泡与列表卡片：8dp
- 红包卡片：10dp
- 底部操作面板：12dp（仅顶部）
- 弹窗：12dp
- 胶囊按钮：高度的一半

阴影只用于浮层：`0 4 16 rgba(0,0,0,0.12)`；普通列表和卡片不使用阴影。

## 5. 组件尺寸

- 顶部导航栏：44dp + 安全区；标题 17sp/600。
- 底部 TabBar：49dp + 安全区；图标 24dp，文字 10sp。
- 列表单元：最小 56dp；聊天会话单元 72dp。
- 头像：会话 48dp，消息 40dp，详情 64dp。
- 主按钮：高度 48dp，左右边距 16dp，圆角 14dp，白底细边框与轻阴影。
- 次按钮：高度 40dp。
- 文本输入区：最小 52dp，输入框最小 36dp。
- 点击区域：最小 44×44dp。
- 消息气泡：最大宽度为视口 72%，水平内边距 12dp、垂直 9dp。
- 语音条：高度 42dp，宽度随时长在 80–220dp 之间变化。
- 红包卡片：宽 236dp，高 96dp；金额与祝福语不得泄露到 Matrix 明文元数据。
- 弹窗：宽 280dp；操作按钮最小高 48dp。

## 6. 复用组件清单

| 组件 | 职责 | 关键参数 |
|---|---|---|
| `WeChatAppTheme` | 亮暗主题与语义 Token | brightness |
| `WeChatPageScaffold` | 页面背景、导航、安全区 | title, actions, child |
| `ModernActionButton` | 白底图文主/次/危险操作和加载态 | icon, label, kind, loading, onPressed |
| `WeChatListTile` | 统一列表单元 | leading, title, subtitle, trailing |
| `WeChatMessageBubble` | 文本消息及发送状态 | direction, content, status |
| `WeChatTimestamp` | 时间分组 | timestamp |
| `WeChatUnreadBadge` | 未读计数 | count, muted |
| `WeChatVoiceBubble` | 时长、波形、播放态 | duration, samples, playback |
| `WeChatRedPacketCard` | 红包卡片和状态 | greeting, state, packetId |
| `WeChatAttachmentTile` | 图片/文件上传状态 | name, progress, retry |
| `WeChatComposer` | 文本、附件、语音输入 | onSend, onPick, recorder |
| `WeChatStatusChip` | 钱包/提现状态 | semanticStatus |
| `WeChatDialog` | 确认、错误、详情 | title, content, actions |
| `WeChatToast` | 非阻塞反馈 | message, semanticType |
| `WeChatEmptyState` | 空状态与重试 | title, action |

所有组件位于 `apps/mobile_flutter/lib/ui/`，业务页面只能通过公开构造函数使用。组件支持 `key`、语义标签和必要的样式扩展，但不允许覆盖品牌与资产状态色。

## 7. 核心组件行为

### 消息

- 自己的消息靠右并使用 `bubbleOutgoing`，对方消息靠左并使用 `bubbleIncoming`。
- 发送中显示小型进度；失败显示红色重试图标；已发送不显示多余标记。
- 连续 5 分钟内的消息不重复显示时间戳；跨日显示完整日期。
- 未读标记最多显示 `99+`。

### 语音

- 按住开始；上滑超过 60dp进入取消区；松开发送；取消区松手丢弃。
- 少于 1 秒不发送并给出轻提示；单条上限 60 秒。
- 录音页显示秒数和 20–24 段归一化波形。
- 录制结束先进入本地试听状态，由用户发送或删除；不得自动上传。

### 附件

- 图片：JPEG/PNG/WebP，单文件上限 20MiB。
- 文件：允许列表由配置定义，默认 PDF/ZIP/TXT/DOCX/XLSX，单文件上限 100MiB。
- 权限按需请求；拒绝时显示设置入口。
- 上传展示 0–100% 进度；失败保留本地选择并允许重试。
- 明文只存在于设备内存/临时文件；发送走 Matrix SDK 的加密媒体路径，不进入业务 API。

### 红包

- 使用 `warning` 橙色卡片，底栏显示“畅聊点钻红包”。
- 状态包括：可领取、已领取、已领完、已过期、已撤回。
- 卡片 Matrix 事件只含红包 ID 和非敏感展示字段；金额、领取人和权威状态从业务 API 获取。

### 钱包

- 交易记录支持全部/充值/提现筛选。
- 状态：处理中、成功、失败；同时显示文字与图标。
- USDT 固定六位小数；地址默认中间省略，详情页才可复制完整地址。

## 8. 动效

- 按压反馈：80ms 降至 0.72 透明度，120ms 恢复。
- 页面转场：220ms 横向推入；返回手势可中断。
- 弹窗：180ms 淡入并从 0.96 缩放到 1.0。
- 消息发送：160ms 从输入区平移并淡入，不采用弹跳。
- 下拉刷新：跟手移动；触发后最短展示 300ms。
- 红包弹层：220ms 缩放淡入。
- 上传进度：线性更新，不对伪进度做循环动画。
- 系统开启减少动态效果时，取消位移、缩放和波形动画，只保留不超过 100ms 的淡入淡出。

## 9. 页面结构

底部导航固定为：消息、通讯录、发现、我。点钻、红包、钱包入口置于“我”和聊天业务卡片，不再作为四个并列资产 Tab；首轮迁移可暂保留资产入口，但最终必须收敛到上述信息架构。

登录、注册和邮箱验证页使用 `landing.png` 沉浸背景、半透明白色输入控件和白底图文操作按钮；错误显示在对应字段或表单下方并提供重试，不显示技术异常文本。

## 10. 使用与审查

- 新组件必须有 Widget 测试、亮暗主题快照或结构断言、无障碍语义断言。
- 代码审查搜索硬编码颜色、间距和字号；除 Token 定义外不得新增。
- 每次 UI 改动同时更新本文件和 `docs/verification/` 证据。
- 本文件优先于旧 Cupertino 主题规范；架构与安全规范仍以产品现代化设计为准。

### 10.1 配色规范检查清单

- [ ] `WeChatAppTheme` 中定义并导出 `chatNavigationBackground=#F7F7F7`、`chatPageBackground=#EDEDED`、`tabRootPageBackground=#EDEDED`。
- [ ] 私聊和群聊消息页顶部导航栏只引用 `chatNavigationBackground`，截图/Widget 测试断言其色值为 `#F7F7F7`。
- [ ] 私聊和群聊消息页的根 Scaffold/消息列表背景只引用 `chatPageBackground`，截图/Widget 测试断言其色值为 `#EDEDED`。
- [ ] “消息、通讯录、发现、我”四个主导航根页只引用 `tabRootPageBackground`，截图/Widget 测试断言其色值均为 `#EDEDED`。
- [ ] 新增或修改相关页面时，不得使用 `Color(0xFF...)`、`Color.fromARGB(...)`、十六进制字符串或第三方默认背景色绕过上述变量。
- [ ] 代码审查同时检查嵌套 `Scaffold`、`Container`、`CustomScrollView`、空状态和加载状态，确保它们没有覆盖根页面或导航栏规定的背景色。
- [ ] 亮暗主题、动态主题或远端配置不得覆盖本节八个固定区域的色值；变更本表需同步修改 Token、页面测试和本检查清单。

## 11. 通讯录组件与页面

### 11.1 信息架构

“通讯录”作为底部第二个 Tab，导航顺序固定为：新的朋友、群聊、标签、公众号/官方客服、按拼音分组的好友列表。右侧索引条宽 20dp，字号 10sp；当前索引使用 `brandPrimary`。

### 11.2 通讯录组件

| 组件 | 规格 |
|---|---|
| `WeChatContactTile` | 高 56dp，40dp 头像，昵称 16sp，底部分隔线左缩进 68dp |
| `WeChatContactIndex` | 右侧 A–Z 索引，触摸反馈显示 64dp 居中浮层 |
| `WeChatFriendRequestTile` | 高 68dp，状态按钮 64×32dp，支持接受/拒绝/已添加 |
| `WeChatContactTagChip` | 高 28dp，圆角 4dp，浅灰背景 |
| `WeChatPrivacyRow` | 朋友圈权限、黑名单及时间范围设置行 |

好友主页包含头像、展示名、畅聊号、签名、朋友圈预览、发消息及音视频入口。备注、标签、删除、拉黑及朋友圈权限统一置于右上角“更多”页面，破坏性操作使用 `danger`。

## 12. 朋友圈 UI

### 12.1 入口与时间线

朋友圈入口位于“发现”Tab 首组第一行，使用 24dp 相机/图片图标。存在新内容时，右侧显示最近发布者 32dp 头像和红点。

时间线顶部为 280dp 封面区：封面铺满、底部渐变遮罩、右下角显示 72dp 用户头像和白色昵称。列表背景使用 `surfaceElevated`，单条动态水平边距 12dp，左侧头像 42dp，正文区域与头像间距 10dp。

### 12.2 动态组件

| 组件 | 规格 |
|---|---|
| `WeChatMomentTile` | 发布者、正文、媒体、位置、时间、操作菜单及互动区 |
| `WeChatMomentImageGrid` | 1 张限制 240×240dp；2–4 张双列；5–9 张三列；间距 4dp |
| `WeChatMomentActionMenu` | 深色 `#4C4C4C` 面板，高 40dp，点赞/评论各 80dp |
| `WeChatMomentReactions` | `#F7F7F7` 背景，点赞与评论区，圆角 4dp |
| `WeChatMomentVisibilityIcon` | 12dp，表示公开、好友、部分可见、不给谁看、仅自己 |
| `WeChatMomentComposer` | 正文、九宫格、位置、提醒谁看、可见范围 |
| `WeChatMomentNotificationTile` | 48dp 头像、互动摘要、动态缩略图和时间 |

正文使用 16sp/23sp 行高；发布者名称使用 `#576B95`，该蓝色仅作为社交用户名与可点击文本 Token `socialLink`。位置、删除和时间使用 13sp。

### 12.3 发布页

- 纯文字或文字加 1–9 张图片；首版不展示视频入口。
- 正文输入区最小高 120dp，无外框。
- 图片单元按容器三等分，删除按钮为右上角 20dp 深色圆形。
- 底部设置项：所在位置、提醒谁看、谁可以看。
- 发布按钮 64×32dp；内容为空时禁用。
- 上传中离开页面需要确认；失败图片保留本地预览并显示重试。

### 12.4 详情、搜索与推荐

动态详情复用 `WeChatMomentTile`，下方完整展示点赞与评论。搜索页采用顶部搜索框和正文、用户、话题、位置、时间筛选；无权限内容不得短暂出现。

公开时间线顶部使用“推荐/最新”双段选择器，高 32dp；关闭个性化推荐后默认进入“最新”。推荐标记不得伪装成好友动态。

### 12.5 朋友圈设置

设置项包括：允许陌生人查看、朋友圈时间范围（全部、最近半年、最近一个月、最近三天）、不让他看、屏蔽他的朋友圈、关闭个性化推荐。时间范围选择使用底部操作面板并立即显示当前值。

### 12.6 内容治理状态

发布状态包括上传中、审核中、已发布、部分可见、已下架和发布失败。审核中使用灰色状态条；下架使用红色说明并提供申诉入口。好友内容和公开内容均接受基础安全检测，公开推荐仅包含审核通过内容。

## 13. 认证页面

- `liuhetong_logo.svg` 是 Android/iOS APP 图标唯一源文件。
- `landing.png` 是登录、注册和邮箱验证页统一背景。
- `ImmersiveAuthScaffold` 使用全屏 `cover`，焦点保持在背景上部品牌主体；键盘出现时只移动表单层。
- 登录字段：用户名/邮箱、密码；提供注册入口。
- 注册字段：用户名、邮箱、密码、强制邀请码；邀请码为空时提交不可用。
- 邮箱验证支持 6 位验证码、验证链接、60 秒重发与注册状态反馈。

### 13.1 激活码验证与使用状态

- 界面将邀请码展示为“激活码”；其合法性、使用次数和状态以业务 API 为唯一权威来源，客户端不得依据本地显示状态将其标记为已使用。
- 激活码仅在注册用户**通过激活码成功完成注册**时标记为“已使用”。成功定义为注册服务的创建用户、建立注册会话和激活码消费均在同一业务事务内提交成功；提交成功后才允许返回注册成功状态。
- 注册流程中的信息校验失败、网络错误、请求超时、服务端拒绝、邀请码失效/耗尽、邮箱验证未完成、创建账号失败或任何其他失败，均不得将激活码标记为“已使用”，也不得减少剩余使用次数。
- 激活码状态变更必须置于注册成功事务之后：事务提交失败必须回滚激活码消费和使用次数；在消费动作之前失败时状态保持不变。客户端重试使用同一幂等键时只能重放同一成功结果或同一失败结果，不得发生额外消费。
- 注册失败提示必须保留可重试的激活码输入，不显示“已使用”或其他与实际权威状态不一致的本地状态。

## 14. 现代操作按钮

| 类型 | 视觉 |
|---|---|
| 主操作 | 白底、1dp 中性边框、轻阴影、绿色图标、深色文字 |
| 次操作 | 透明背景、图标加文字 |
| 危险操作 | 白底、红色图标、红色文字 |
| 导航操作 | 44×44dp 点击区内的单一图标 |

禁止新增大面积纯绿填充按钮。本规范适用于认证、好友、朋友圈、点钻、红包、钱包和设置页面。按下即时缩放至 0.98；松开使用临界阻尼恢复；减少动态效果时仅改变透明度或颜色。

## 15. 用户资料与头像

- `UserIdentityHeader` 展示 72dp 头像、昵称、畅聊号和个性签名。
- 本人头像支持相册选择、正方形裁剪、预览、上传、修改和恢复默认头像。
- 默认头像使用昵称首字符与稳定浅色背景。
- 好友头像 48dp，资料页头像 72dp；加载失败必须回退默认头像。
- 邮箱只在本人资料页显示脱敏值。

### 15.1 聊天成员头像缓存

- 私聊和群聊消息页必须使用统一的 `AvatarCache`/头像提供器加载会话成员头像；页面不得直接为每个可见成员反复发起网络请求。
- 首次进入私聊或群聊时，先查内存缓存，再查磁盘缓存；两级均未命中时才请求网络。网络成功后同时写入内存和磁盘缓存，并以用户 ID、头像版本（或规范化头像 URL）和尺寸作为缓存键。
- 同一聊天页再次进入、列表滚动复用或应用前后台切换时，命中内存/有效磁盘缓存必须直接显示本地资源，避免重复网络加载；磁盘命中可先显示缓存内容，再后台按更新策略检查。
- 内存缓存采用 LRU，至少容纳最近 200 个头像条目；内存压力时可逐出但不得影响磁盘缓存。磁盘缓存有效期为 30 天，单条缓存使用服务器 `ETag`/头像版本和最后验证时间；过期条目可显示陈旧缓存并后台刷新，网络失败时继续显示陈旧缓存或默认头像。
- 头像版本或 URL 变更、个人资料上传/修改头像成功、联系人资料同步事件及服务器 `ETag` 变化，必须精确失效该用户的内存和磁盘条目，并拉取/写入新资源；不得清空无关成员的缓存。用户主动清除应用缓存时清除磁盘缓存，内存缓存随进程生命周期清除。
- 群聊成员头像按成员用户 ID 独立缓存；成员加入、离开或昵称变化不得使其他成员头像重新下载。加载中显示稳定默认头像，不得造成列表布局跳动。
- 缓存仅保存已授权展示的缩略头像字节和必要的版本/有效期元数据；不得把 Matrix 消息明文、访问令牌或其他用户资料写入头像缓存。

## 16. 好友主页与更多

- 通讯录展示名优先级：备注名 > 昵称 > 用户名；绝不显示内部 UUID。
- 好友主页主操作为白底图文“发消息、语音通话、视频通话”。
- 右上角 `ellipsis` 进入更多页，包含备注、标签、朋友圈权限、黑名单和删除好友。
- 发消息复用现有 E2EE Direct Chat；创建中显示不可重复点击的加载态。

## 17. “我”页面

- 顶部必须展示个人资料卡，点击头像或资料区进入编辑页。
- 功能组顺序：朋友圈、点钻、红包、钱包、设置。
- 设置页显式包含退出登录，使用白底危险操作行和二次确认。

## 18. 网络状态胶囊

`NetworkStatusCapsule` 仅在离线/重连时悬浮于导航栏下方，不占据整行，不推动页面主体或 TabBar。使用半透明材质、网络图标和短文案；点击触发立即重试，恢复联网后自动淡出。
